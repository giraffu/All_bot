# 04_BIZ_用户修为与身份权限体系

## 1. 业务定位

本体系把用户能力分为两条线：

- 修为境界：反映活跃度、签到与使用积累
- 会员身份：反映付费权益与高级能力准入

在当前实现中，这两条线不只影响任务优先级，也直接影响 Web 登录资格、视频档位、功能入口与会话续存。

## 2. 当前业务事实

- Web 登录并非全员开放。
- 允许 Web 登录的用户，需要满足“境界或身份”其中一项达到阈值。
- 用户即使已经拿到 JWT，后续若身份或境界下降，也会在 `get_current_user` 中被再次拦截。
- 因此权限体系已经从“发卡式授权”演进为“动态权限复核”。
- 身份过期会在 `PermissionIdentityPriorityService.get_user_identity()` 中自动视为 `外门弟子`；但少数接口若直接读取 `users.current_identity` 字段，仍需单独核对是否复用了动态身份口径。
- 排队优先级是“修为优先级 + 身份优先级”相加；新用户优先级、可信用户加成及各用量档位均来自统一等级权益配置。
- `low_trust_free_tier` 是实时计算的内部低信任免费层标记。默认口径仍为 `checkin_count > 7`、自身无任何 `SUCCESS` 订单，且不满足“受邀用户数 `>100` 且成功订单受邀用户去重占比 `>3%`”的高质量邀请者豁免；阈值、成功订单豁免和可信优先级加成现在由统一配置控制。手工赠送、礼品单和 0 元成功订单仍属于 `SUCCESS` 订单。
- 标准快速视频设置与最终提交都按统一配置中的修为、身份视频权益取并集，旧 callback 不能绕过保存后的分辨率和时长限制；LTX、SCAIL-2 等独立入口仍使用各自契约。
- 配置事实源为 `runtime_checkpoints.key='user_tier_policy_config:v1'`。Dashboard 的“等级权益配置”页面通过 `GET/PUT /api/user-tier-policy` 读取和保存；数据库无自定义记录或暂时不可用时，业务使用代码内安全默认值。

## 3. 两条等级线的作用

### 3.1 修为境界

- 影响排队优先级与部分功能可见性。
- 是零氪或轻氪用户进入 Web 与高级功能体系的重要路径之一。
- 当前自动晋级最高到 `元婴期`；`化神期`、`炼虚期`、`合体期`、`大乘期`、`渡劫期` 没有自动晋级入口，读取权益时兼容为元婴期档。
- 修为刷新入口为 `PermissionIdentityPriorityService.refresh_user_group()`；统一配置把晋级门槛保存为包含边界，例如旧规则“邀请 `>1`”保存为 `2`。签到服务在资格判定前也会刷新修为。

### 3.2 会员身份

- 影响高级分辨率、长视频、隐藏高消耗节点等能力开放。
- 影响充值发货后的身份到期时间与跨级折算。
- 当前仍是 `内门弟子`、`核心弟子`、`真传弟子` 三层结构。
- 身份月卡来自 canonical membership plan；纯灵石套餐 `duration_days == 0`，只增加 `credits`，不改变身份。
- 返佣兑换身份已有代码路径和固定选项，但运行时同时受 `MEMBERSHIP_SETTLEMENT_V2_ENABLED` 与 `AFFILIATE_MEMBERSHIP_REDEEM_ENABLED` 控制；未开启时 Web API 返回 404。

## 4. 当前代码权限矩阵

### 4.1 修为矩阵

| 修为 | 默认自动达成/晋级条件 | Web | 视频档位 | 签到基础灵石 | 闪回瓶 | 修为排队优先级 |
| :--- | :--- | :--- | :--- | ---: | ---: | :--- |
| `凡人` | 默认 | 否 | `512p` / `5s` | 外门不可签；内门以上可签基础 `10` | `8` | `0` |
| `练气期` | 已入频道 | 是 | `512p` / `5s` | `10` | `9` | `<10`：`+3`；`<50`：`+1` |
| `筑基期` | 邀请 `≥2`、签到 `≥4`、生成 `≥11` | 是 | 至 `720p` / `8s` | `12` | `10` | `<10`：`+5`；`<50`：`+1` |
| `金丹期` | 邀请 `≥11`、签到 `≥31`、生成 `≥101` | 是 | 至 `1024p` / `10s` | `15` | `12` | `<10`：`+8`；`<50`：`+3`；`<100`：`+1` |
| `元婴期` | 邀请 `≥101`、签到 `≥301`、生成 `≥1001` | 是 | 至 `1024p` / `10s` | `20` | `14` | `<10`：`+12`；`<50`：`+5`；`<100`：`+1` |
| 历史高阶修为 | 无自动晋级规则 | 按元婴期 | 按元婴期 | 按元婴期 | `14` | 按元婴期 |

### 4.2 身份矩阵

| 身份 | Web | 视频档位 | 签到加成 | 并发 | 收藏 | 闪回瓶 | 身份排队优先级 |
| :--- | :--- | :--- | ---: | ---: | ---: | ---: | :--- |
| `外门弟子` | 否 | `512p` / `5s` | `+0` | `3` | `100` | `8` | `0` |
| `内门弟子` | 是 | 至 `720p` / `8s` | `+30` | `5` | `300` | `10` | `<20`：`+20`；`<50`：`+8`；之后 `+1` |
| `核心弟子` | 是 | 至 `1024p` / `10s` | `+40` | `8` | `600` | `12` | `<30`：`+30`；`<60`：`+12`；之后 `+1` |
| `真传弟子` | 是 | 至 `1024p` / `10s` | `+50` | `12` | `1000` | `14` | `<40`：`+45`；`<70`：`+20`；之后 `+1` |

### 4.3 叠加与例外

- Web 准入：有效身份或修为任一档开启 Web 权益即可。
- 签到：先按持久化频道资格刷新修为；刷新后仍为凡人时，只有动态有效身份为 `内门弟子`、`核心弟子` 或 `真传弟子` 才能签到，外门仍须先入宗门。
- 标准快速视频：修为档位和身份档位取并集；例如 `练气期 + 核心弟子` 可显示 `1024p` 和 `10s`。
- 闪回瓶：修为容量与有效身份容量取较大值，不相加；修为默认 `8/9/10/12/14`，身份默认 `8/10/12/14`。历史接口返回实际容量，前端提示动态展示；R2 热缓存清理使用同一容量。
- 标准快速视频中 `1024p` 与 `10s` 互斥，选择其中一个时会自动避开另一个。
- LTX 视频设置当前硬编码 `1280x704`，时长显示 `5s`、`10s`、`15s`、`20s`，未按修为/身份做差异化。
- SCAIL-2 视频换脸 v10 固定 `512x896`，只开放 `5s=40` 灵石和 `8s=80` 灵石，未复用 `RESOLUTION_PERMISSIONS`。
- 低信任免费层例外：连续免费活跃、没有自身成功订单且缺少高质量邀请转化的用户不封号、不扣灵石；系统通过给非低信任用户统一 `+40` 的方式形成队列差距，低信任用户保留原本基础优先级但不获得该加成；Gallery 提示词解锁不再按该标记拦截。
- 高负载限制：容量公式仍为 `projected_pending > 50 × max(健康且 enabled 的可接单 Worker 数, 1)`；是否豁免由修为或有效身份配置中的“高压豁免”开关决定，指标异常仍 fail-open。
- 并发与收藏：均按动态有效身份读取统一配置；身份过期和未知身份回落外门，不再直接信任原始 `current_identity`。

## 5. 与认证系统的耦合点

- Telegram 登录后，不是所有用户都能进入 Web。
- 密码登录同样要复用相同的 Web 准入校验。
- 改密、降权、身份过期都会影响既有会话是否继续有效。
- `get_current_user` 会重新读取用户统计与当前身份/境界，继续执行动态权限复核。

## 6. 与商业化系统的耦合点

- 身份月卡购买成功后，权限立即变化。
- 纯灵石套餐只变更 `credits`，不变更身份。
- 纯灵石 canonical 套餐：
  - `200 Star 直购`：¥30 / 1.99 TON / 200 Stars，到账 `600` 灵石。
  - `500 Star 直购`：¥70 / 4.99 TON / 500 Stars，到账 `1800` 灵石。
  - `1000 Star 直购`：¥120 / 9.90 TON / 1000 Stars，到账 `4000` 灵石。
- 返佣兑换灵石已经上线，固定套餐为 `1/3/6/10/15/20 USDT -> 130/390/780/1800/2700/4000` 灵石。
- 返佣兑换身份已有服务与接口，选项为 `内门弟子 30 天`、`核心弟子 30 天`、`真传弟子 30 天`，但运行时需要同时打开 `MEMBERSHIP_SETTLEMENT_V2_ENABLED` 与 `AFFILIATE_MEMBERSHIP_REDEEM_ENABLED`。

## 7. 代码口径索引

- `src/services/user_tier_policy_service.py`：等级权益默认值、固定层级归一化、数据库读写与组合规则事实源。
- `dashboard/backend/routers/user_tier_policy.py` 与 `dashboard/frontend/src/components/UserTierPolicySettings.vue`：管理 API 与页面。
- `src/constants.py`：旧调用兼容默认值与 canonical 任务成本；不再是动态等级权益事实源。
- `src/services/permission_identity_priority_service.py`：修为刷新、身份过期回落、Web 准入判断、优先级计算。
- `src/services/permission_growth_channel_service.py`：签到奖励、邀请处理、频道/宗门状态同步。
- `src/core/billing_core.py`：高负载提交限制、统一并发锁、扣费与退款入口。
- `src/services/membership_plan_catalog.py`：canonical 月卡与纯灵石套餐。
- `src/services/affiliate_redeem_rules.py` 与 `src/services/affiliate_redeem_service.py`：返佣兑换灵石/身份规则与幂等履约。
- `src/web_api/dependencies.py`：Web 请求动态权限复核。
- `src/web_api/services/users_history_service.py` / `users_history_mutation_service.py`：动态闪回瓶容量和有效身份收藏上限。

## 8. 文档维护口径

- 不要再把“Web 入口”写成金丹期以上专属固定规则，实际以代码中的允许组/允许身份集合为准。
- 不要把权限体系只描述成任务调度权重，它已经同时影响登录、会话、可用功能与前端展示。
- 不要把标准快速视频档位误写成所有视频入口的全局强校验；LTX、视频换脸等入口当前存在独立实现。
- 若修改统一配置 schema、默认权益或组合规则，必须同步本文档及 focused tests。
