# Web VPS 退役记录

2026-07-27，旧 Web/Nginx VPS 与两个直连域名完成退役。正式、测试 Web 分别固定由 Cloudflare Pages 承载，媒体只通过 R2/S3 URL 交付；仓库同步删除 VPS Nginx 模板、SSH 发布脚本、前端相对媒体路径拼接和后端旧对象存储客户端。

退役前只读核对显示该节点已离线约 14 天，最近 100 条正式可见历史均解析为 R2 签名 URL。历史 IP、旧域名和旧操作手册不再作为现行知识维护；需要取证时以本归档提交前的 Git 历史和 Cloudflare 审计日志为准。
