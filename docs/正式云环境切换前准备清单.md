# 正式云环境切换前准备清单

最后修订：2026-06-07 16:59 CST

本清单只覆盖维护窗口前准备，不授权正式切流、不启动云 Bot polling、不停止本地正式服务。正式切换仍以用户明确确认的低峰维护窗口为准。

## 0. 云测试环境退役

- [x] 已执行 `scripts/cleanup_cloud_test_for_prod.sh --dry-run`，确认清理对象只包含云测试资源。
- [x] 执行退役时必须显式使用 `scripts/cleanup_cloud_test_for_prod.sh --execute`。
- [x] 云端 `cloud-*-test` 容器已删除。
- [x] 本地主服务器 `cloud-comfy-agent-test-*` 容器已删除，避免测试 worker 继续占用正式 GPU。
- [x] DigitalOcean PostgreSQL `bot_db_test` 已删除，避免云端继续被误用为测试库。
- [x] DigitalOcean Valkey DB3/DB4 已 `FLUSHDB`，云测试队列、锁、heartbeat 和运行态已清空。
- [x] R2 `user-data-test` 按本轮决策保留，不在退役脚本中删除。
- [x] `web-test.aivison.it.com` 静态站和 Nginx 配置按本轮决策保留；API upstream 可能不可用。

2026-06-06 20:00 CST dry-run 结果：

- 云端将清理 `cloud-web-api-test`、`cloud-tg-bot-test`、`cloud-dashboard-backend-test`、`cloud-central-api-test`、`cloud-imgproxy-test`。
- 本地主服务器将清理 `cloud-comfy-agent-test-1` 到 `cloud-comfy-agent-test-7`。
- `bot_db_test` 存在，dry-run 时仍有 10 个连接；真实执行会先停止 `cloud-web-api-test`，再用一次性容器终止连接并删除测试库。
- Valkey DB3/DB4 dry-run key 数分别为 7 和 23；真实执行会对 DB3/DB4 执行 `FLUSHDB`。
- 本地正式 `tg-bot`、`web-api`、`payment-api`、`backend_api_1`、`comfy-agent-*` 保持运行。

2026-06-06 20:49 CST execute 结果：

- 云端 `cloud-*-test` 容器为空，本地主服务器 `cloud-comfy-agent-test-*` 为空。
- `bot_db_test` 独立复核为不存在。
- Valkey DB3/DB4 独立复核 `DBSIZE=0`。
- 本地正式 `tg-bot`、`web-api`、`payment-api`、`backend_api_1`、`comfy-agent-*` 保持运行。

## 1. 配置与脚本门禁

- [x] `.env.cloud.prod` 已由 `.env.cloud.prod.example` 生成，且所有占位值已替换。
- [x] `.env.cloud.prod` 无重复 key，`BOT_TYPE=PROD`，`REDIS_PREFIX=prod_bot_`。
- [x] `API_TOKEN` 与 `AUTH_TOKEN` 完全一致。
- [x] `AGENT_SECRET_TOKEN` 在云 Central 和本地云正式 worker 中一致。
- [x] `JWT_SECRET_KEY` 已纳入 `.env.cloud.prod`、`.env.cloud.prod.example` 与 cloud-prod preflight 必填检查。
- [x] 在线对象存储桶统一为 `user-data-prod`：`MINIO_BUCKET`、`MINIO_INPUT_BUCKET`、`MINIO_RESULT_BUCKET`、`MINIO_TEMPLATE_BUCKET`、`R2_BUCKET`。
- [x] `MINIO_SECURE=true`，`MINIO_PUBLIC_URL=`，`R2_PUBLIC_DOMAIN=https://r2.aivison.it.com`。
- [x] `.dockerignore` 已忽略 `.env.*`，避免 root Docker image COPY 私有云正式环境变量。
- [x] 云端可执行 `docker compose --env-file .env.cloud.prod -f deploy/docker-compose-cloud-prod.yml config`，真实输出不得贴到日志或聊天中。
- [x] 本地主服务器可执行 `docker-compose --env-file .env.cloud.prod -f workers/docker-compose-cloud-prod-worker.yml config`，真实输出不得贴到日志或聊天中。

2026-06-06 20:49 CST 预置结果：

- 云端 `.env.cloud.prod` 已补齐为正式库 `bot_db_prod`、Valkey DB1/DB2、R2 `user-data-prod` 口径，文件权限为 `600`。
- 云端 `scripts/safe_deploy_cloud_prod.sh --preflight-only --skip-network-checks` 通过，compose config 可渲染。
- 云端已构建 `central-api-prod`、`web-api-prod`、`payment-api-prod`、`dashboard-backend-prod` 镜像；未启动任何 `cloud-*-prod` 容器，未启动 `cloud-tg-bot-prod`。

2026-06-06 21:06 CST R2 补充结果：

- 新的 Cloudflare API token 已验证可管理 R2 bucket 权限；`user-data-prod` 已创建或确认存在。
- `user-data-prod` CORS 已配置，允许 `https://web.aivison.it.com` 与 `https://web-test.aivison.it.com` 的 `GET/PUT/HEAD`。
- 已用该 token 派生 R2 S3 凭证并更新本地、云端 `.env.cloud.prod`；文件权限保持 `600`，未打印密钥值。
- 云端 `scripts/safe_deploy_cloud_prod.sh --preflight-only` 完整通过，R2 list/head 与 Telegram Local Bot API 探测通过。
- 本地主服务器 `scripts/start_cloud_prod_worker.sh --preflight-only --skip-network-checks` 通过，确认 7 个云正式 worker compose 可渲染，且未启动 worker。

推荐先运行：

```bash
scripts/safe_deploy_cloud_prod.sh --preflight-only
scripts/start_cloud_prod_worker.sh --preflight-only
scripts/stop_local_prod_entry_preserve.sh --dry-run --include-workers --include-test-workers
```

## 2. 云控制面预启动门禁

- [x] 已确认 DigitalOcean PostgreSQL、Valkey、R2、Tailscale IP、R2 CORS 均为正式资源。
- [x] 云正式 compose 只启动 `central-api-prod`、`web-api-prod`、`payment-api-prod`、`dashboard-backend-prod`、`imgproxy-prod`。
- [x] `cloud-tg-bot-prod` 仍在 `bot` profile 中，未启动 polling。
- [x] `payment-api-prod` 已纳入云正式控制面，监听 `8021`。
- [x] 云测试退役后，正式 `8000/8003/8021/8043/8084` 端口不再需要避让云测试控制面。
- [x] 云端能访问 Telegram Local Bot API `69.63.220.115:8081/8082`，但未启动 Bot。
- [x] DB 连接池预算符合：Web API 4 workers * `(2+1)`，Bot `(3+2)`，Payment `(2+1)`，Dashboard `(1+0)`；本阶段未启动 Bot，实际预算不包含 Bot 连接。

2026-06-07 01:31 CST 云正式控制面预启动结果：

- 云端 `scripts/safe_deploy_cloud_prod.sh --preflight-only` 通过，R2 list 与 Telegram Local Bot API 探测正常。
- 首次预启动发现 Web API 缺少 `JWT_SECRET_KEY` 会触发安全拒绝启动；已从本地正式私有 `.env` 安全补入本地/云端 `.env.cloud.prod`，并把 `JWT_SECRET_KEY` 加入 `.env.cloud.prod.example`、`scripts/safe_deploy_cloud_prod.sh` 必填检查和 Web API compose 环境。
- 云端执行 `scripts/safe_deploy_cloud_prod.sh --start-control-plane --with-db-upgrade` 成功，Alembic head 检查为单 head，`upgrade head` 幂等通过。
- 当前云端 `cloud-central-api-prod`、`cloud-web-api-prod`、`cloud-payment-api-prod`、`cloud-dashboard-backend-prod`、`cloud-imgproxy-prod` 均为 `healthy`。
- 独立健康检查通过：`8003/health=200`、`8000/api/health=200`、`8021/pay/result=200`、`8043/api/health=200`。
- `cloud-tg-bot-prod` 未运行，边缘 `web.aivison.it.com` / `rmb.aivison.it.com` 未切流，未 reload Nginx。

预启动命令只在确认 dry-run 数据恢复通过后执行：

```bash
scripts/safe_deploy_cloud_prod.sh --start-control-plane --with-db-upgrade
```

健康检查：

```bash
curl -fsS http://100.107.220.127:8003/health
curl -fsS http://100.107.220.127:8000/api/health
curl -fsS http://100.107.220.127:8021/pay/result
curl -fsS http://100.107.220.127:8043/api/health
```

## 3. 数据与 R2 迁移演练

- [x] 本地生产 `bot_db` 一致性 dump 已恢复到云 dry-run 库或云正式库的演练周期。
- [x] `alembic heads` 只有 1 个 head。
- [x] `alembic upgrade head` 成功，`alembic current` 为 `0f4e7b9c1a23`。
- [x] 核心表行数与本地生产快照对齐。
- [x] R2 `user-data-prod` 已配置 CORS，允许 `https://web.aivison.it.com` 与 `https://web-test.aivison.it.com` 的 `GET/PUT/HEAD`。
- [x] `bot-data`、`comfyui-temp`、`bot-template` 样本对象已通过 S3 API 镜像到 R2 根路径，未额外添加源桶名前缀。
- [x] 已完成同名 object key 冲突扫描。
- [x] `scripts/backfill_history_r2_objects.py` dry-run 覆盖 Web 视频、Web 图片、Bot 历史样本。
- [x] `history/{task_id}/original.ext` 可见集合原文件小批 apply 验证成功。
- [x] 缩略图小批 copy/generate apply 验证成功。

2026-06-06 20:49 CST DB dry-run 结果：

- 已从本地 `bot_db` 生成 `pg_dump -Fc`：`backups/cloud-prod-dryrun/bot_db_prod_dryrun_20260606_203712.dump`，大小约 473MiB，SHA256 `49b7bea92103deab1ed99efe6b51ecbe47a85ed4dfc00fac68a71c7e817fe3ff`。
- 已创建云端 `bot_db_prod` 并完成 `pg_restore --clean --if-exists --no-owner --no-privileges`，恢复命令退出码为 0。
- 云端执行 `alembic heads` 为单 head `0f4e7b9c1a23`，`alembic upgrade head` 成功，`alembic current` 为 `0f4e7b9c1a23 (head)`。
- 云库恢复后抽样行数：`users=125198`、`history=1702156`、`orders=8109`、`user_logs=2455313`、`worker_logs=1481136`、`runtime_checkpoints=0`。
- 本地正式库在 dump 后继续写入，20:49 CST 当前行数已略有增长；该差异属于 dry-run 快照漂移，正式维护窗口仍需最终 dump/restore。

2026-06-06 21:06 CST R2 预迁移结果：

- 已对 `bot-data`、`comfyui-temp`、`bot-template` 各复制 3 个样本对象到 R2 `user-data-prod` 根路径，并完成 HEAD 验证。
- 已启动全量 R2 预镜像后台容器 `allbot-r2-premirror-20260606-210547`，当前从 `bot-data` 开始；命令使用 `mc mirror --overwrite --summary --max-workers 8`，不使用删除同步。
- 后台镜像进度可用 `docker logs -f allbot-r2-premirror-20260606-210547` 查看；若正式服务资源紧张，可先 `docker stop allbot-r2-premirror-20260606-210547`，后续重新运行会覆盖续传。
- 历史 URL dry-run 已覆盖 Web 视频 30 条、Web 图片 69 条、Bot 历史 100 条；结果显示 Web 视频将上传原始媒体 30 个、缩略图复制 29 个并生成 1 个，Web 图片将上传 69 个并复制 69 个缩略图，Bot 历史将上传 100 个并生成 100 个缩略图，失败数均为 0。
- 本阶段未执行历史对象 apply；正式维护前仍需小批 apply 验证 `history/{task_id}/original.ext` 与缩略图 key。

2026-06-06 21:39 CST R2 优化调整结果：

- 基于生产库引用盘点，`comfyui-temp` 当前 182GiB 存量中实际仍可追踪并存在的引用对象只有 187 个，约 73.21MiB；最近 30 天没有新的裸结果文件解析到 `comfyui-temp`。
- 已停止并移除旧三桶全量镜像容器 `allbot-r2-premirror-20260606-210547`，避免继续全量上传 `comfyui-temp` 残留对象。
- 已启动优化版主镜像容器 `allbot-r2-premirror-main-20260606-213421`，仅全量镜像 `bot-data` 与 `bot-template`，仍使用 `mc mirror --overwrite --summary --max-workers 8`，不使用删除同步。
- 已完成 `comfyui-temp` 引用 key 精准迁移：候选 21,024 个，复制并 R2 HEAD 校验 187 个，源端不存在 20,837 个，复制总量 73.21MiB。
- 精准迁移清单保存在 `backups/cloud-prod-dryrun/comfyui-temp-referenced-r2-migration-20260606-213529.tsv`，不包含密钥。
- 当前主镜像进度可用 `docker logs -f allbot-r2-premirror-main-20260606-213421` 查看；若正式服务资源紧张，可先 `docker stop allbot-r2-premirror-main-20260606-213421`。

2026-06-06 23:19 CST R2 策略切换结果：

- 已停止并移除 `allbot-r2-premirror-main-20260606-213421`，不再继续全量镜像 `bot-data`。
- 新策略为：`bot-template` 继续作为小体量静态模板桶迁移；`bot-data` 不再全量强搬，改为预热用户可见集合，并在云正式 Web API 启用只读 legacy MinIO fallback。
- 已在代码中加入 R2 full-path 候选 key：除 `history/{task_id}/original.ext` 与 basename 外，还会探测原始 object key，例如 `123/output_images/task.png`。此前已镜像到 R2 根路径的对象可以直接命中。
- 已加入可选 `LEGACY_MINIO_*` 变量；仅 Web API / Dashboard 读取路径使用，worker 仍只写 R2 `user-data-prod`，避免新数据写回旧 MinIO。
- `scripts/backfill_history_r2_objects.py` 已支持 `--visible-scope user-visible --source-storage legacy --concurrency N`，用于预热最近 8 条闪回瓶、投稿、历史收藏、广场 like/apply 与提示词解锁相关历史。
- 正式维护窗口前仍需填写并验证 `.env.cloud.prod` 中的 `LEGACY_MINIO_ENDPOINT`、`LEGACY_MINIO_PUBLIC_URL`、`LEGACY_MINIO_ACCESS_KEY`、`LEGACY_MINIO_SECRET_KEY`。这些值不得打印到日志或聊天。
- 已将相关代码、compose、脚本和文档同步到云端；云端 `web-api-prod` 与 `dashboard-backend-prod` 镜像已重建成功，未启动任何云正式容器。
- 已修正 `LEGACY_MINIO_ENDPOINT` 为云端可达的 Tailscale MinIO 地址 `100.99.254.53:9000`，`LEGACY_MINIO_PUBLIC_URL` 保持 `https://assets.aivison.it.com`。
- 云端使用一次性 `web-api-prod` 容器完成 `--visible-scope user-visible --source-storage legacy --limit 3` dry-run：扫描 3 条，原文件均为 `would_upload`，源端缺失数 0；3 条缩略图源端缺失，后续按“先原文件、再缩略图生成批次”处理。

2026-06-07 00:13 CST 可见集合原文件小批 apply 结果：

- 云端使用一次性 `web-api-prod` 容器执行 `--visible-scope user-visible --source-storage legacy --media-only --limit 500 --apply --concurrency 4`，未启动 `cloud-*-prod` 服务容器，未启动 `cloud-tg-bot-prod`。
- 主批次扫描 500 条可见历史原文件，成功上传 499 个到 R2 `history/{task_id}/original.ext`；1 个视频对象因 legacy MinIO 读取链路 `IncompleteRead` 失败。
- 已对失败 task `a66fae80-63e2-475d-970c-d628096ce9d5` 单独重试 `--media-only --apply --concurrency 1`，重试成功，最终本批原文件失败数归零。
- 本批缩略图按 `--media-only` 策略全部跳过，缩略图 copy/generate 仍留待后续小批 apply。
- 执行后复核云端无 `cloud-*-prod`、`cloud-*-test` 或 one-off run 残留容器；本地正式 `tg-bot`、`web-api`、`payment-api`、`backend_api_1`、`comfy-agent-*` 保持运行。

2026-06-07 00:54 CST 缩略图小批 apply 与 URL 验收结果：

- 云端一次性 `web-api-prod` 容器执行 legacy 缩略图 copy-only apply：`--visible-scope user-visible --source-storage legacy --limit 500 --apply --concurrency 4`。扫描 500 条，原文件已存在 500 个，缩略图从 legacy 成功复制 206 个，legacy 源缩略图缺失 294 个，失败数 0。
- 已扩展 `scripts/backfill_history_r2_objects.py`，支持在原文件已预热到 R2 后用 `--source-storage current --generate-missing-thumbnails` 从 R2 原文件生成标准缩略图；legacy 源批量生成仍受保护拦截，避免直接对旧 MinIO 大批量拉取生成。
- 云端一次性 `web-api-prod` 容器执行缩略图 generate 小批 apply：`--visible-scope user-visible --source-storage current --limit 50 --generate-missing-thumbnails --apply --concurrency 2`。扫描 50 条，缩略图已存在 14 个，新生成 36 个，失败数 0。
- 验收发现部分 R2 对象 S3 `HEAD` 成功，但 `https://r2.aivison.it.com/history/...` 公网自定义域名返回 404。历史详情与缩略图读路径已增加“公网快探测失败但 R2 S3 命中时返回 R2 S3 短签 URL”的兜底；Web owner `/result` 延迟敏感路径仍保持原策略，不把视频结果过早改成 fallback。
- Web 历史详情抽样 3 条通过：媒体与缩略图均可读，返回 R2 S3 短签域名 `c7220eb751acc6f7ab8255b4a0394ef3.r2.cloudflarestorage.com`，HTTP `206`。
- Wan22 预览抽样 2 条通过：媒体与缩略图均为 R2 S3 短签 HTTP `206`，`last_frame` 为 `assets.aivison.it.com` HTTP `200`。
- 当前 active Gallery 中默认会返回 `input_file_url` 的 `face_swap/face_video` 样本数为 0；已用 one-off forced-input 方式验证 3 条 active Gallery 输入图，均从 legacy `assets.aivison.it.com` 返回 HTTP `200`。该验证不改变线上默认 apply-context 策略。
- 本轮仅使用一次性容器执行脚本和验收；执行后云端无 `cloud-*-prod`、`cloud-*-test` 或 one-off run 残留容器，本地正式 `tg-bot`、`web-api`、`payment-api`、`backend_api_1`、`comfy-agent-*` 保持运行。

2026-06-07 01:31 CST object key 冲突扫描结果：

- 对本地 MinIO 源桶 `bot-data`、`comfyui-temp`、`bot-template` 做全量只读 key 扫描，目标是确认三桶镜像到 R2 `user-data-prod` 根路径时不会互相覆盖。
- 扫描对象数：`bot-data=3,759,920`、`comfyui-temp=146,935`、`bot-template=7,401`。
- 跨桶同名 key 冲突数为 `0`，`max_same_key_group_size=1`。
- 扫描摘要保存在 `backups/cloud-prod-dryrun/object-key-collision-scan-20260607-011217/summary.json`，冲突明细文件为空：`collisions.tsv`。

2026-06-07 11:25 CST 维护窗口前最终存储门禁复核：

- 云端 `cloud-web-api-prod` 内复核 `alembic heads` 与 `alembic current`，当前 revision 仍为 `0f4e7b9c1a23 (head)`。
- R2/legacy URL 读路径只读验收通过：Web owner `/api/tasks/{task_id}/result` 返回 `success` 且结果 URL 可读；历史详情媒体与缩略图可读；Gallery apply-context 输入图从 legacy `assets.aivison.it.com` 可读；Wan22 预览媒体与缩略图可读。
- `bot-template` 最终样本复核未通过：本地 `bot-template` 前 30 个源对象在 R2 `user-data-prod` 中只命中 3 个；扩展到前 200 个源对象的 30 个分散样本时只命中 1 个。缺失集中在 `quick_face/` 模板前缀，且不是误加 `bot-template/` 前缀导致。
- 本轮严格保持只读，没有继续向 R2 写入或删除对象。进入维护窗口前仍需补做 `bot-template` 模板桶迁移或明确接受 quick_face 模板能力风险。

2026-06-07 11:55 CST `bot-template` 补迁移结果：

- 已按同 key 策略把本地 `bot-template/<object key>` 补迁移到 R2 `user-data-prod/<object key>`，不添加源桶名前缀，不做删除同步。
- 源桶对象数 `7401`，源端总量 `2540152416` bytes；本轮复制缺失对象 `7398` 个，已有同尺寸对象跳过 `3` 个，尺寸不一致覆盖 `0` 个，失败 `0` 个，实际写入 `2539957777` bytes。
- 迁移报告目录：`backups/cloud-prod-dryrun/bot-template-r2-migration-20260607-112345/`；摘要文件为 `summary.json`，明细文件为 `migration_manifest.tsv`，错误日志为空。
- 迁移后全量 HEAD 复核通过：`7401/7401` 个源对象在 R2 `user-data-prod` 中同 key、同 size 命中，`missing=0`、`size_mismatch=0`、`head_errors=0`。
- `quick_face/` 前缀复核通过：源端 `311` 个对象，R2 同 key、同 size 命中 `311` 个。
- 原 11:25 CST 的 `bot-template quick_face/` 阻断项已解除。

历史公网 URL dry-run：

```bash
set -a
source .env.cloud.prod
export BOT_TYPE=PROD
set +a

python scripts/backfill_history_r2_objects.py --source web --media-type video --limit 100
python scripts/backfill_history_r2_objects.py --source web --media-type image --limit 100
python scripts/backfill_history_r2_objects.py --source bot --media-type all --limit 100
```

可见集合预热 dry-run / apply 模板：

```bash
set -a
source .env.cloud.prod
export BOT_TYPE=PROD
set +a

python scripts/backfill_history_r2_objects.py \
  --visible-scope user-visible \
  --source-storage legacy \
  --limit 500

python scripts/backfill_history_r2_objects.py \
  --visible-scope user-visible \
  --source-storage legacy \
  --apply \
  --concurrency 4
```

## 4. Worker 与任务链门禁

- [x] 首发只迁移 7 个 `cloud-prod-comfy-agent-*`，不迁移 `worker_remote_01/02`。
- [x] 已确认 `worker_remote_01/02` 不承载首发独占任务类型；若存在缺口，必须暂停对应能力或重新分配 worker。
- [x] `cloud-comfy-agent-test-*` 已停止或限流，不抢正式 GPU。
- [x] 本地云正式 worker compose 的 `SUPPORTED_TASK_TYPES` 与当前正式 7 个本地 worker 一致。
- [x] 预启动后 `/system/workers` 可看到 7 个 `cloud_prod_worker_*` heartbeat，且健康态不是 `error` 或 `quarantined`。
- [x] Web 视频 `/result`、历史详情、Gallery apply-context、Wan22 链条预览和缩略图均返回可读 URL。
  - 2026-06-07 00:54 CST 已完成历史详情、Gallery apply-context URL builder、Wan22 预览和缩略图抽样可读验收。
  - 2026-06-07 01:41 CST 已完成云 Web API 真实 HTTP `/api/tasks/{task_id}/result` 验收：Web 视频返回 `success`，结果 URL 为 `r2.aivison.it.com/history/.../original.mp4`，未停在 `pending_result`。

启动 worker 只在云 Central 预启动并验证后执行：

```bash
scripts/start_cloud_prod_worker.sh --start
```

2026-06-07 01:31 CST worker 任务类型缺口确认：

- 静态对比 `workers/docker-compose.yml` 与 `workers/docker-compose-cloud-prod-worker.yml`，7 个首发 `cloud-prod-comfy-agent-*` 的 `SUPPORTED_TASK_TYPES` 与当前本地正式 7 个 `comfy-agent-*` 完全一致。
- union 能力一致：`face_swap`、`face_video`、`i2i_draw`、`i2i_pro`、`image_to_video`、`img2img`、`img2img_lora`、`ltx_video`、`t2i-pornmaster-turbo`、`video_edit`、`video_insert`、`wan22_video_v2`。
- 当前本地 Central 仍看到 `worker_remote_01` 报 `img2img`、`worker_remote_02` 报 `face_swap`；这两个类型都已由首发 7 个 worker 覆盖，没有独占任务类型缺口。
- 本地主服务器执行 `scripts/start_cloud_prod_worker.sh --preflight-only` 通过，确认可访问云 Central 与 R2；未启动任何 `cloud-prod-comfy-agent-*` worker。

2026-06-07 01:41 CST 云正式 worker 启动与 heartbeat 验收：

- 本地主服务器执行 `scripts/start_cloud_prod_worker.sh --start`，已启动 7 个 `cloud-prod-comfy-agent-*`，未停止本地正式 `comfy-agent-*`。
- 云 Central `/system/workers` 返回 7 个 `cloud_prod_worker_01` 到 `cloud_prod_worker_07`，全部 `status=idle`，`last_error` 为空，`current_task_id=null`，无 `error`/`quarantined`。
- 7 个 worker 容器 `RestartCount=0`；非敏感环境变量复核为 `MASTER_API_URL=http://100.107.220.127:8003`、`MINIO_*_BUCKET=user-data-prod`、`MINIO_SECURE=true`。
- 启动早期 `cloud-prod-comfy-agent-1/3` 各出现一次 `Connection to master failed: Server disconnected without sending a response`，随后 heartbeat 正常；最近 30 秒错误日志为空。
- 云正式 worker 已接入云 Central，但本轮未提交生成任务，不主动占用 GPU 跑任务。

2026-06-07 11:07 CST 维护窗口前最终 worker 复核：

- 云 Central `/system/workers` 返回 `count=7`，7 个 `cloud_prod_worker_01` 到 `cloud_prod_worker_07` 均为 `status=idle`。
- 7 个 worker 的 `last_seen` 均在 15 秒内，`consecutive_failures=0`，`current_task_id=null`，`last_error=''`，`quarantined_until=null`。
- 本地主服务器 `cloud-prod-comfy-agent-1..7` 均为 `Up`，`RestartCount=0`；最近 3 分钟日志显示 heartbeat、task pop 与 ComfyUI `system_stats` 正常。
- 11:01 CST 前后曾出现短暂 `Connection to master failed`，随后自动恢复；当前不阻断门禁，但建议维护窗口切流前再复核一次最近 5 分钟日志。

## 5. 支付与维护风险确认

- [x] 本轮不新增 `PAYMENT_CREATION_DISABLED`。
- [x] 已记录当前维护窗口策略：窗口开始后只开启 Web 维护状态，不立即停止本地 Bot 或旧 worker，等待当前任务队列自然跑完。
- [x] 停 Bot 时点和支付风险口径已确认：仅靠 Web `/app/MAINTENANCE` 不作为 Bot 端支付冻结保证；本轮接受维护等待期间少量 Bot RMB/Stars 新订单风险，最终 dump 前再停止本地 Bot 和旧写入口。
- [x] 准备阶段 `orders` 中 `PENDING`/`CREATED` 待处理订单当前快照已只读导出；维护窗口最终 dump 前仍需重新导出一次。
- [x] `rmb.aivison.it.com` 独立边缘模板已准备，但尚未 reload。
- [x] 已确认 RMB 入口首选继续使用 Cloudflare Tunnel 改回源，不走边缘 Nginx RMB 证书方案。
- [x] Cloudflare Tunnel 切云/回滚脚本已准备并 dry-run 通过，真实执行留到维护窗口。
- [ ] 维护窗口内执行 RMB tunnel 切云前，必须先开启维护、等待当前队列归零、停止本地 Bot/旧写入口、最终 dump/restore、确认云 Payment API 健康。
- [ ] 小额 RMB/TON 真实支付验收留到正式切换后，不在准备阶段执行。

待处理订单快照参考：

这里不是一个现成后台功能或固定脚本；当前口径是对本地正式库 `orders` 表执行只读查询，导出 `status IN ('PENDING', 'CREATED')` 的订单快照。历史导出文件沿用了 `unreturned_orders.csv` 文件名，但语义应理解为“待处理订单快照”。

```sql
SELECT id, business_order_id, order_id AS public_order_id, payment_channel, status, created_at
FROM orders
WHERE status IN ('PENDING', 'CREATED')
ORDER BY created_at DESC;
```

2026-06-07 10:05 CST 准备阶段只读导出结果：

- 本地正式库 `orders` 中 `status IN ('PENDING', 'CREATED')` 当前共有 3295 条。
- 分布为 `PENDING/RMB=3209`、`PENDING/TON=86`，未发现 `CREATED` 行。
- 明细文件：`backups/cloud-prod-dryrun/pending-orders-20260607-100533/unreturned_orders.csv`。
- 摘要文件：`backups/cloud-prod-dryrun/pending-orders-20260607-100533/summary.json`。
- 这是维护窗口前快照，不是最终切换清单；正式最终 dump 前必须在队列归零并停止本地 Bot/旧写入口后重新导出。

2026-06-07 11:15 CST 维护窗口前最终只读快照：

- 本地正式库 `orders` 中 `status IN ('PENDING', 'CREATED')` 当前共有 3298 条。
- 分布为 `PENDING/RMB=3212`、`PENDING/TON=86`，未发现 `CREATED` 行。
- 明细文件：`backups/cloud-prod-dryrun/pending-orders-20260607-111510/unreturned_orders.csv`。
- 摘要文件：`backups/cloud-prod-dryrun/pending-orders-20260607-111510/summary.json`。
- 相比 10:05 CST 准备快照增加 3 条；这再次说明维护窗口内必须在队列归零并停止本地 Bot/旧写入口后重新导出最终快照。

2026-06-07 10:19 CST RMB Cloudflare Tunnel 准备结果：

- systemd 服务 `cloudflared-rmb-pay` 当前 `active/running`，实际启动命令为 `cloudflared tunnel --protocol http2 run rmb-pay`。
- tunnel 配置文件为 `/home/hfy/.cloudflared/config.yml`，`rmb.aivison.it.com` 当前回源为 `http://127.0.0.1:8021`。
- 本地 Payment API `http://127.0.0.1:8021/pay/result` 与云 Payment API `http://100.107.220.127:8021/pay/result` 均返回 200。
- 已新增 `scripts/switch_rmb_tunnel_to_cloud_prod.sh`，默认 dry-run；维护窗口真实执行时必须显式传 `--execute`，目标回源为 `http://100.107.220.127:8021`。
- 已新增 `scripts/rollback_rmb_tunnel_to_local_prod.sh`，默认 dry-run；回滚目标回源为 `http://127.0.0.1:8021`。
- 两个脚本均通过 `bash -n` 与 dry-run；本轮未修改 `/home/hfy/.cloudflared/config.yml`，未 restart `cloudflared-rmb-pay`。

2026-06-07 11:25 CST 维护窗口前最终入口复核：

- 边缘 live Nginx 仍指向本地正式 Web API `http://100.99.254.53:8000`，`assets.aivison.it.com` 仍指向本地 MinIO `http://100.99.254.53:9000`，线上配置 `nginx -t` 通过；本轮未 reload Nginx。
- `cloudflared-rmb-pay` 仍为 `active/running`，`rmb.aivison.it.com` 当前回源仍是 `http://127.0.0.1:8021`，公网 `https://rmb.aivison.it.com/pay/result` 返回 200。
- `scripts/switch_rmb_tunnel_to_cloud_prod.sh --dry-run` 与 `scripts/rollback_rmb_tunnel_to_local_prod.sh --dry-run` 均通过；本轮未执行 `--execute`，未修改 tunnel 配置，未重启 cloudflared。

## 6. 边缘入口门禁

- [x] 当前边缘 VPS 生产 Nginx 配置已备份。
- [x] Web 主模板 `all_bot_nginx_cloud_prod.conf` 已复制到边缘 VPS 临时路径并通过独立 wrapper `nginx -t`。
- [x] `web.aivison.it.com /api/` 模板 upstream 为 `http://100.107.220.127:8000`。
- [x] `assets.aivison.it.com` 旧 MinIO 代理已保留，继续支撑 legacy MinIO fallback。
- [x] `rmb.aivison.it.com` 独立模板 upstream 为 `http://100.107.220.127:8021`。
- [x] `rmb.aivison.it.com` 当前域名仍通过 Cloudflare Tunnel 服务；本轮已选择维护窗口内改 Tunnel 回源到云 Payment API。
- [x] 本轮未 reload Nginx，正式维护窗口前不得 reload 到正式用户流量。

2026-06-07 10:08 CST 边缘预校验结果：

- 已备份边缘现有 `/etc/nginx/sites-available/all_bot` 到 `/root/allbot-nginx-backups/all_bot.before-cloud-prod.20260607-100556.conf`。
- 边缘现有线上配置 `nginx -t` 通过。
- 首次包含 `rmb.aivison.it.com` HTTPS server 的模板测试失败，原因是边缘 VPS 不存在 `/etc/letsencrypt/live/rmb.aivison.it.com/fullchain.pem`。
- 已修正主模板：保留 `assets.aivison.it.com` 到本地 MinIO 的 legacy 代理，只把 `web.aivison.it.com /api/` 指向云 Web API。
- 修正后的主模板已复制到 `/tmp/all_bot_nginx_cloud_prod.20260607-100816.conf`，独立 wrapper `/tmp/allbot-nginx-cloud-prod-test.20260607-100816.conf` 执行 `nginx -t -c` 通过。
- RMB 独立模板已复制到 `/tmp/all_bot_nginx_cloud_prod_rmb.20260607-100816.conf`，等待证书/Cloudflare 路由决策后再启用。

## 7. 维护窗口前最终停靠点

达到本节之前仍不切流、不停正式服务：

- [x] 云正式控制面可预启动并健康。
- [x] 云正式 worker 可 preflight，通过 7 worker 配置渲染和 R2/Central 探测，并已启动 7 个 worker heartbeat。
- [x] 数据库 dry-run restore + Alembic 到 head 通过。
- [x] R2 样本镜像、历史 URL backfill dry-run、可见集合原文件小批 apply、缩略图小批 apply、`bot-template` 迁移和 legacy MinIO fallback 验收完成。
- [x] 支付控制策略已确认：维护窗口先开启 Web 维护标记，等待当前队列自然归零，不立即停止本地 Bot 或旧 worker；最终 dump 前再停止本地 Bot/旧写入口并导出 `PENDING`/`CREATED` 待处理订单最终快照。
- [x] 边缘 Web 主模板已校验但未 reload。
- [x] RMB 支付入口切换方式已确认：维护窗口内使用 Cloudflare Tunnel 脚本切云，可用回滚脚本切回本地。
- [ ] 维护窗口内 RMB tunnel 切云尚未执行。
- [x] 本地生产 DB/Redis/MinIO 仍保留为事实源和回滚源。

2026-06-07 11:25 CST 最后门禁结论：

- 已完成本轮只读/dry-run 最终门禁复核；本地正式服务保持运行，云 Bot 未启动，边缘 Nginx 未 reload，RMB tunnel 未执行切云。
- 环境、配置、云控制面健康、云库 Alembic、R2/legacy URL 读路径、worker heartbeat、任务类型覆盖、RMB tunnel dry-run 和待处理订单准备快照均已补齐证据。
- 仍不建议直接进入维护窗口，原因是 `bot-template` 模板桶最终样本复核未通过，`quick_face/` 大量源对象未在 R2 `user-data-prod` 命中。下一步应补迁移 `bot-template` 或明确接受对应模板能力风险后，再把本节剩余门禁改为完成。
- 正式切换未执行：未停本地正式入口、未最终 dump/restore、未切 `web.aivison.it.com`、未切 `rmb.aivison.it.com`、未启动 `cloud-tg-bot-prod`。

2026-06-07 11:55 CST `bot-template` 阻断解除后结论：

- `bot-template` 已完成补迁移并全量 HEAD 验证通过，维护窗口前静态准备门禁已补齐。
- 正式切换仍未执行：未停本地正式入口、未最终 dump/restore、未切 `web.aivison.it.com`、未切 `rmb.aivison.it.com`、未启动 `cloud-tg-bot-prod`。
- 进入维护窗口前仍需用户明确确认窗口开始；窗口内先开启维护状态并实时确认运行态归零，不立即停止本地 Bot 或旧 worker；归零后再停止本地 Bot/旧写入口、重新导出 `PENDING`/`CREATED` 待处理订单最终快照、做最终 DB dump/restore 与增量对象同步。

2026-06-07 16:59 CST 支付控制口径确认：

- 用户已确认本轮不新增 `PAYMENT_CREATION_DISABLED`；订单频率低，进入 Web 维护状态后用户不方便继续支付，维护等待期间少量 Bot 端新订单风险可接受。
- 正式切换仍未执行；维护窗口先只开启 Web 维护状态，等待当前任务队列自然跑完，不立即停止本地 Bot 或旧 worker；最终 dump 前必须停止本地 Bot/旧写入口后重新导出 `PENDING`/`CREATED` 待处理订单最终快照，并确认快照不再增长。
