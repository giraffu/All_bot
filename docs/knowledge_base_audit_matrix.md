# AllBot Knowledge Base Audit Matrix

> 2026-07-20：Dashboard Backend 逐服务投影补齐精确必填 `AGENT_SECRET_TOKEN`。RunPod down/delete 会先通过该 token 调用 Central agent control；旧契约只允许 `RUNPOD_*` 等键，虽然测试夹具提供 token，却没有断言 Dashboard 投影，旧整份 env 注入又掩盖了缺口。现在目标宿主缺键会在投影阶段 fail closed，投影只增加这一精确凭据，不恢复整份 prod env、镜像 `.env` 或无关秘密。事实源为 `deploy/service-env-contract.yml`、runtime env 契约回归、Dashboard RunPod command/operator 链路与 ops Skill/云正式文档。
> 2026-07-20：逐服务配置局部暂存从 Dashboard 特例扩展到全部具有容器 env 契约的独立模块。`config-plan/config-apply --module <name>` 以目标服务和现有 active service 的并集构建快照；目标服务允许更新 revision，所有非目标 active 投影必须继续存在并保持相同 revision/原始字节。目标缺键、非目标变化、闭包逃逸、篡改或清单外未知键均在写入前 fail closed；成功只替换/追加权限 `600` 的目标投影并合并 active state，不维护、不备份、不调用 Compose/重启。内部 contract revision 只用于 environment revision，不再伪装成未知宿主键扩大影响集；影响闭包由真实 changed keys 与逐服务 revision drift 共同计算。相同环境 revision 的多次追加及目标更新保留完整联合回滚状态和不可变 activation history；完整 plan 继续报告未投影服务。Public Web 无容器 env 投影，继续注入 runtime config。事实源为 runtime env helper、`release.py`、专项 TDD、ops/concurrent/QQCC Skills 与不可变发布/云正式文档。
> 2026-07-20：Dashboard exact-digest 发布成功后的重复 `deploy-module` 暴露 no-change 顺序错误：当前 SHA 已与目标一致、容器 digest/健康/config revision 均可只读证明，但旧流程先执行 mutation rollback preflight，因本机没有“previous”同 SHA bundle cache 而误报 blocker。no-change 真实运行态核验现提前到 rollback preflight 前，完全一致直接返回 `status=no-change`；任一 digest、RepoDigest、健康或逐服务 revision 不一致仍继续完整 preflight，不能绕过回滚门禁。
> 2026-07-19：独立模块的运行配置检查收口到机器定义的逐服务闭包；Dashboard `deploy-module` 只投影 Dashboard 前后端必填键，仍验证生产全局语义、env revision 与全部已激活投影完整性，因此非目标 main-bot 尚缺 canonical key 不再阻断 owner-only rolling，目标缺键/漂移/篡改仍 fail closed。正式机无 active projection 且只授权 Dashboard rolling 时，`config-plan/config-apply --module dashboard` 仅以 `600` 暂存 Dashboard 两份投影和指针，不进维护、不备份、不调用 Compose/重启；既有且不投影到 Dashboard 的 12 个 legacy 宿主键只按代码内精确清单兼容，清单外未知键、已有 active 或闭包逃逸拒绝，完整 inspect 继续把其它缺失投影报告为 drift。`deploy/env.defaults` 与 legacy test compose 仅以当前精确 SHA256 加入独立发布 snapshot，任一内容变化恢复共享契约 blocker。事实源为 `release.py`、runtime env helper、release policy、专项 TDD、不可变发布文档与 ops Skill。
> 2026-07-19：局部 Dashboard 首次投影后，公共 Compose 曾因解析非目标 `central-api.env` 而在 pull 前失败并完整恢复旧栈。云 Compose 的逐服务 `env_file` 改用 `required: false`，仅允许项目解析缺失的非目标投影；目标 closure 仍由发布器在 pull/up 前严格校验，非目标不得 `up` 且启动时间强制不变。公共 Compose 新内容 checksum 已更新到独立契约 snapshot，后续任何漂移恢复 shared-compose blocker。事实源为 cloud base Compose、release policy、immutable Compose TDD、不可变发布文档与 ops Skill。
> 2026-07-19：Dashboard-only 正式发布进一步发现两项通用发布器错误并 fail closed：Compose 会展开逐服务投影中密码 hash 的字面 `$`，发布后 smoke 又把 test Central 别名硬编码为 `central-api`，误拒绝正式环境的 `central-api-prod`。所有投影 `env_file` 现同时使用 `format: raw` 保留原始配置字节；API smoke 改为从容器真实 `config.API_BASE` 请求 `/health`，不记录配置值或绑定环境别名。Compose 精确 checksum、专项 TDD、不可变发布文档与 ops Skill 同步更新。
> 2026-07-20：真实连通性 smoke 证明当前正式 Compose 的 Central DNS 只有 `central-api`，而历史 Dashboard 服务投影仍为不可解析的 `central-api-prod`；这也是早期 `fix-test-api-base` 成果在严格配置投影重构后被覆盖的回归。为遵守本次只更新 Dashboard 的授权，prod overlay 只对 Dashboard Backend 覆盖 `API_BASE=http://central-api:8003`，不修改宿主 prod env、不重启其它服务；发布器继续以目标容器真实配置请求健康端点。完整宿主 API_BASE 收敛留到独立配置窗口。
>
> 2026-07-19：环境中立镜像扫描按 artifact 自身构建 SHA 去重：main workflow 的 `--only-source-sha` 必须精确等于 release index SHA，本批新构建镜像继续执行 Config.Env、文件系统与 test/prod 解析检查；复用控制面和仅作 Dashboard pin 索引的历史 GPU 镜像保留其原构建 CI 扫描证据，不在每个控制面 bundle 重拉超大镜像或错误执行控制面身份哨兵。事实源为环境中立校验器、模块化 workflow、专项 TDD、不可变发布文档与 ops Skill。
>
> 2026-07-19：main-first 控制面 bundle 的 GPU pin 清单改为从目标 main 全祖先中最近的完整 main-channel manifest 原样继承未变化 profile；保留每项精确 digest、artifact source/OCI revision 和模型证据，只用于 Dashboard 后续 RunPod operation 的完整 pin 注入，不触发 GPU build/test/deploy。真实 GPU 输入变化仍必须当前 main SHA attestation，历史基线不能满足；缺完整祖先、mutable ref、digest mismatch 或 catalog 集合不等均阻断 main bundle。事实源为模块化 workflow、`ci_release_v2.py`、专项 TDD、不可变发布文档与 ops Skill。
>
> 2026-07-19：main-first 增量 planner 补齐运行态 digest 漂移：artifact 可由较早 main 构建并在新 bundle 复用，若当前 test/prod state 缺失该 artifact 或 digest 与目标 bundle 不同，仍必须选入部署；只有实际 digest 已一致才能返回空集/no-change。测试计划静态排除该环境不存在的 Dashboard/Payment/Paid Group artifact，可选 private worker 继续按宿主配置显式禁用。事实源为 `release.py`、发布器回归、不可变发布文档与 ops Skill。
> 2026-07-19：首次控制面 `config-apply` 的数据库备份门禁在写 projection 前安全失败；根因是 Web API 容器的 `/bin/sh` 不支持 Bash 风格 `${VAR/pattern/replacement}`。配置备份与 migration 备份现以 POSIX `case` + `${VAR#prefix}` 严格接受 `postgresql+asyncpg:` / `postgresql:`，未知 scheme 继续 fail closed；不允许手工跳过 `pg_dump`。事实源为 `release.py`、发布器回归、不可变发布文档与 ops Skill。
> 2026-07-19：控制面配置契约与 GPU/Worker 链路进一步解耦。`ALLBOT_WORKER_*` / `CLOUD_TEST_WORKER_*` / `CLOUD_TEST_SHARED_AIO_*` 仍原样保存于受限宿主 env，但不再进入 control-plane environment revision、逐服务投影、漂移或影响集；单独 Worker 变更不触发控制面 `config-apply`。Dashboard 消费的 `RUNPOD_*` / `LAN_AIO_*` 仍被跟踪，其它未知键继续全量 fail closed。事实源为逐服务配置契约、运行时投影工具、专项回归、不可变发布/云测试控制面文档与 ops Skill。
> 2026-07-19：云测试当前 env 仍以 `BOT_TOKEN_TEST`/`QQCC_BOT_TOKEN_TEST` 等旧别名驱动旧容器，严格逐服务投影因缺 canonical `BOT_TOKEN` 和 `TELEGRAM_FILE_BASE_URL` 在写入前 fail closed。测试迁移器新增 `--control-plane-only`：只补已知 canonical key 和旧自建 Telegram `:8081`/`:8082` 配对，canonical 已有值优先，未知 endpoint 拒绝猜测，且完整保留 Worker 选择与槽位。事实源为测试迁移器、专项回归、不可变发布文档与 ops Skill。
> 2026-07-19：main bundle 环境中立门禁的 test/prod 运行身份哨兵收口到可运行 service artifact。`python-runtime-base` 与 `python-worker-base` 本来就不包含 `src` 应用源码，不再被误当服务启动；两者仍必须通过 Image Config.Env 与文件系统的环境材料扫描。事实源为环境中立校验器、专项回归、不可变发布文档与 ops Skill。
> 2026-07-19：main bundle 环境中立扫描从新 Dashboard Backend 镜像检出 `/app/paid_group_guard_bot/.env.example`；根因是根级 `.env.*` ignore 没有覆盖目录级 `COPY` 的嵌套示例。构建上下文和门禁现同时要求 `**/.env`、`**/.env.*`，保留仓库内本地开发示例但禁止其进入任何镜像层；失败 SHA 未发布 bundle。事实源为 `.dockerignore`、环境中立校验器、专项测试、精确镜像只读导出、不可变发布文档与 ops Skill。
> 2026-07-19：严格配置外置后，main bundle 的 Dashboard/QQCC Backend 真实 import smoke 改为显式注入合成 CI 逐服务投影；不再只传单个 MinIO 地址，也不向镜像或源码恢复鉴权默认值。缺少 Dashboard/QQCC secret、管理员标识或共享运行配置仍 fail closed。事实源为模块化 workflow、专项静态门禁、精确失败 digest 的容器重放、不可变发布文档与 ops Skill。
> 2026-07-19：main bundle 的 catalog 影响判定改为对可信基线做逐 artifact 语义比较。控制面、Public Web、文档和发布工具变化不再自动调度 `gpu-execution`；只有 GPU artifact/profile、`remote_workers/**` 或真实基础依赖变化进入 GPU 专用 manifest/canary/operator 链路。无法读取旧 catalog 时继续 fail-safe 全量重建，真实 GPU 变化的同 SHA attestation 门禁不放宽。事实源为 `release_artifacts_v2.py`、`ci_release_v2.py`、模块化 workflow、专项测试、ADR 0008、不可变发布文档与 ops Skill。
> 2026-07-19：并发发布改为 main-first release batch。A-H 统一从 `origin/main` 开发，功能 AI 推送后以 `slot/branch/head/base_sha` 不可变 handoff 并立即释放槽位；集成 AI 用 `batch-plan` 冻结多个 handoff，只创建一个 `release-batch -> main` PR。PR 阶段不发布容器，main push 的可信 CI 成功后 `modular-release-v2.yml` 只构建一次 main-channel bundle；用户需要时再部署云测试，standard artifact 用 main-channel exact-digest history 验收。旧 test-train/candidate/promotion 只保留历史兼容。事实源为 `manage_ai_workspaces.py`、两份 workflow、`release.py`、ADR 0008、并发/不可变发布文档与 Skills。
> 2026-07-19：控制面运行配置收敛为目标主机 `/etc/allbot/<env>.env` 与权限 `600` 的逐服务投影；本机 env、镜像层和 Public Web tar 不再承载环境秘密。配置漂移先走 `config-plan`/`config-apply`，秘密轮换完成状态只能由 `credential-isolation-complete` 校验限时 HMAC/健康/撤销证据后原子写入。当前提交只实现契约和受控接口，不代表正式配置切换或秘密轮换已执行。
> 历史（已由上条与 ADR 0008 取代）：promotion approval 曾通过受保护 GHCR 写入通道发布 test-train 批准记录；相关脚本只保留既有 artifact 兼容，不再作为新批次入口。
> 历史（已由上条与 ADR 0008 取代）：发布曾采用最终 test-candidate 单次构建和 tree-identical main promotion；新流程改为单批次 main 合并后构建、按需部署测试。
> 2026-07-19：RunPod profile pin 门禁上线后，正式 preflight 暴露旧 Dashboard 基线 `release.env` 缺失；旧 bundle 的 GPU manifest 合法记录为 incomplete，普通 main Dashboard 渲染必须继续拒绝。`recover --repair-rollback-materials` 现增加唯一向后兼容例外：仅为精确已部署、CI/main/运行 digest 均核验通过的旧基线物化 checkout/release.env 时允许缺 pin JSON，不拉镜像、不重启服务、不改 deployment state；所有 plan/preflight/deploy 仍要求完整 digest-pinned profile 集合。
> 2026-07-19：正式 Dashboard RunPod profile pin 改为从同一受保护 main release index 解析完整 `image@sha256` 集合，并通过 track-scoped control-plane `release.env` 的非敏感 JSON 注入 Dashboard Backend。手动新增与 autoscaler/recovery operation 会覆盖容器旧 `.env` 的全部 profile 镜像变量；缺 profile、mutable tag 或共用 image env 冲突 digest 时在 RunPod mutation 前 fail closed。Dashboard-only 更新不触碰已经运行的 Pod，新 pin 只作用于后续操作；同时保留 baked `remote_workers` import smoke 修复，最终 GPU 镜像必须由新 main 同 SHA 重建。
> 2026-07-18：共享测试站不运行 Dashboard，但 Dashboard-only test-candidate 的完整 bundle 可能成为 control-plane 记录基线；旧 `recover --modules dashboard --repair-rollback-materials` 因找不到测试 Dashboard 容器而无法补齐该基线，继而阻断后续全量 preflight。修复后该精确场景从同一完整 bundle 展开实际启用服务，要求 `current.json` artifact digest 与 bundle 一致，并继续核对每个运行容器 image digest，随后只物化完整 checkout/track-scoped release.env 和执行 `compose config -q`；仍不 pull/up/stop/restart、不写维护或 deployment state，任何不一致继续 fail closed。
> 2026-07-18：GPU release manifest 增加受保护 CI 发布入口 `.github/workflows/publish-gpu-release-manifest.yml`。本地 token 无 `write:packages` 时，不再依赖人工扩权或临时 workflow；Actions 只接受 main 可达完整 SHA、base64 manifest 与文件 SHA256，解码后由 `gpu_profile_release_v2.py --publish-existing-manifest` 校验 schema、完整 GPU catalog、每项 source/OCI revision 同 SHA、SHA tag 和远端不可覆盖，再以仓库 `packages:write` 发布。事实源为 workflow、helper TDD 与 Git 不可变发布文档。
> 2026-07-18：图片换脸 V2 发布契约补齐：`deploy/release-artifacts-v2.json`、全部 i2i_pro LAN slot 与 test env 迁移器统一声明 `face_swap_v2`，旧 `face_swap` 只留在 V1 worker。artifact catalog 内容变化现在强制重建全部自有产物；main 若包含 GPU rebuild，必须先发布同 SHA、完整且不可覆盖的 `allbot-gpu-release-manifests:<sha>`，否则在 main bundle tag 创建前 fail closed。测试站真实两图反馈环随后发现 test-1 仍指向已切为 `image_to_video` 的 `gpu-252` GPU0/8192；迁移器和云测试文档现已对齐在线 i2i_pro GPU1/8191，并只归一该已知旧组合。正式 LAN/RunPod/控制面运行态仍需按 operator 与发布门禁验收。
> 2026-07-18：LTX 最终 main 镜像本地 build/smoke 通过但推送旧 `allbot-comfy-runpod-ltx-video` 包被 GHCR `write_package` 拒绝；发布 workflow、RunPod profile 前缀和云测试/正式/GPU 控制器文档统一切到仓库 Actions 可创建和写入的 `allbot-comfy-runpod-ltx-video-v2`。旧无 `-v2` 包仅保留为 digest-pinned 历史回滚来源，不允许登记新的 code/workflow revision。
> 2026-07-18：测试/生产独立模块仍运行已记录 digest、但对应远端 immutable checkout、track-scoped `release.env` 或本地旧 bundle 被误删时，普通 preflight 必须继续以 transaction rollback blocker fail closed。新增 `recover --repair-rollback-materials` 受控恢复入口：只接受 schema-v2 test/prod control-plane、精确已部署模块 baseline、main/CI/环境/rolling 边界全部通过；生产执行还必须由用户再次 `--confirm-prod`。它核对运行中目标容器 digest 后，仅创建 Git worktree、原子写非敏感 release env 并执行 `compose config -q`，禁止 pull/up/stop/restart、维护标记和 current/history 改写。事实源为 `scripts/release.py`、发布器 TDD 与不可变发布文档。
> 2026-07-17：图片换脸执行类型拆分为 V1 `face_swap` 与 V2 `face_swap_v2`。V1 保持 `face_swap.json`、1 灵石和快速/随机/旧 Gallery/历史重生成路由，正式启用容量预期仅 `worker_remote_02`；V2 使用 `face_swap_v2.json`、独立调用 2 灵石并归属 i2i_pro profile，自由P图 v3 与 QQCC 原脸恢复改走 V2 但保留上层总价/History，SCAIL-2 首帧预处理也使用 V2 且不另扣费；幻想换脸仍是 `i2i_pro` 6 灵石。代码、Worker 候选、canary、autoscaler 与知识库已同步，但尚未部署；正式实时能力必须在发布窗口以 Central 心跳重新核验，不能由 Git 候选配置推断。
> 历史（已由 2026-07-19 main-first 批次流程取代）：2026-07-17 首次为旧 train/test/release 流程引入成员冻结。当前冻结单元是 `slot + task branch + handoff head/base SHA`，handoff 后立即释放槽位；集成 AI 通过 `batch-plan` 冻结一个 release batch，只创建一个 main PR，批次后新交接进入下一批。
> 2026-07-17：`gpu-252` GPU1 RMA replacement 卡已作为第二个正式 `i2i_pro` worker 上线，固定新 UUID `GPU-8153a439-e3f6-8922-039d-dc13e97da6d7` 与 8191。canonical digest `sha256:a56620158da13c561e077511ebd310eb93de8821218da92c908df63f040b6495`、六文件模型缓存、disabled heartbeat、节点/模型枚举、容器健康与 `enable-aio` 门禁均通过；Central control 为 enabled，真实负载下两张卡同时满载且未见新 Xid。8192 原 worker 未重启并继续正常接单。
> 2026-07-16：可信 bundle 的 Dashboard Backend 实际由多阶段 `Dockerfile.control-plane` 构建；只修独立 `Dockerfile.dashboard-backend` 不会改变 release image。两条构建路径现同时内置 LAN AIO rollout 的 `gpu_release_rollout.py`、`release_manifest_v2.py`、`release_strategy.py`，专项回归同时约束两份 Dockerfile，真实 ASGI import smoke 继续作为最终闭包门禁。
> 2026-07-16：test-train 跨轨部署失败并成功逆序恢复后，包装器不再吞掉底层失败原因；最终错误在通用恢复结论后附带 `release.py` 最后一行摘要，使集成 AI 可在唯一 `test_train_release.py` 入口内定位 Worker/Pages/控制面门禁，无需绕过包装器重放 mutation。TDD 覆盖后轨失败、前轨回滚和原始错误保留；事实源为 `scripts/test_train_release.py`、专项测试、并发 Skill/SOP。
> 2026-07-16：PornMaster FP8/BF16 RunPod 共用 runtime 改由 workflow-owned `allbot-comfy-runpod-pornmaster-flux2-edit-baked` 公共 GHCR 包发布，避免 legacy 包缺少当前仓库 Actions write access；Dashboard catalog 固定到完成 BF16 single/multiple workflow、baked entrypoint、revision 与匿名拉取 smoke 的精确 tag。模型 manifest、任务类型、GPU/`--lowvram` 与 UNet 节点隔离口径不变。
> 2026-07-16：QQCC `AI视频` 正式晋级按控制面独立发布，复用既有健康 LTX GPU runtime，不重建正式 GPU 容器、不创建 RunPod canary；默认策略仍把 `dashboard/backend/schemas.py` 归类为 Dashboard/QQCC Config 两个消费者的 rolling 更新，本次用户明确要求非目标模块不动时改用 fail-closed 的 `deploy/release-policy-qqcc-control-plane.yml`，只允许 Central API 与 QQCC Bot/配置/私有 worker 五模块闭包，公共 Web、主 Bot、Dashboard、支付、群管及全部 GPU/Worker 路径均拒绝。唯一测试站存在未整体晋级 main 的 accepted test-train 时，测试端使用带 `environment=test` 硬门禁的 reconcile policy，只忽略已审计的非选择漂移并以真实当前 SHA 做 rollback；正式仍用严格 QQCC policy，二者选择完全相同的五模块 digest。空 LTX 负面提示词继续省略并保留 workflow 默认；非空值须待 Worker mapping 独立发布验收后再宣称生效。同轮修正生产回滚预检对 v2 bundle cache 的识别：`release-v2/release-index.json` 与 `public-web-dist.tgz` 和旧 v1 路径均为合法不可变回滚材料，禁止为通过门禁伪造旧 `release.json`。
> 2026-07-16：生产控制面事务在 cloud/private worker 健康后于 Pages 阶段 fail closed，并完整恢复旧栈与旧 canonical deployment；根因是精确 SHA 的干净 release checkout 没有 `frontend/node_modules`，而 Pages 发布器使用 `npx --no-install wrangler`，故上传命令在创建 deployment 前退出。发布器现交叉校验 `package.json` 与 lockfile 的精确 Wrangler 版本并显式获取该锁定版本，回归测试覆盖无预装依赖的 checkout；该修复不改变 Bot 路由、业务代码、Compose 或 GPU Worker。
> 2026-07-16：test-train 两轨部署现场发现 `test-execution` 回滚 preflight 错查无 track 的 Worker `release-env`，并在没有 cloud service 时错误要求云端 track 合约。修复后 Worker 按 `release-env/<track>/<previous_sha>/release.env` 取证，无 cloud service 的轨道跳过 cloud preflight；两条 TDD 回归锁定该契约，失败候选已自动恢复且须以 forward-fix 新 SHA 重跑。
> 2026-07-16：自动恢复把 control-plane 恢复到 track 隔离上线前的历史 SHA 后，新候选 preflight 错把缺少 track-scoped `release.env` 判为不可回滚。修复后云端控制面优先读取 `/var/lib/allbot/releases/<track>/<previous_sha>/release.env`，缺失时兼容同一 SHA 的 legacy 合约；preflight、失败恢复和恢复验证共享选择逻辑，正向发布仍只写 track-scoped 路径，两条 TDD 回归及真实只读 preflight 锁定该契约。
> 2026-07-16：schema v2 `test-execution` 首次没有 track-scoped 状态时必须标记 `initial-release`，以现有 legacy Agent/Relay 做端口所有权预检和 allowlist 快照；首次失败恢复及后续 immutable 回滚统一读取 `release-env/test-execution/<sha>`。该修复解除“首次发布却要求 immutable Relay 已存在”的闭环，并以 TDD 覆盖 planner 与 track-scoped legacy recovery；事实源为 `scripts/release.py`、`tests/ops/test_release_cli.py`、不可变发布/云测试文档和 ops/concurrent Skills。
> 2026-07-16：schema v2 同 SHA 的 control-plane/test-execution transaction journal 与 staged state 改为 `transactions/<track>/<sha>`，防止 Worker 事务覆盖已提交控制面 journal；test-execution 的首次切换不再错误包含 Postgres/Redis 控制面闭包。`recover --track` 兼容读取升级前无 track 的同轨失败 journal并写回新路径，禁止手工删 journal/maintenance；TDD 覆盖轨道路径、Worker-only cloud 选择和 legacy journal 恢复读取。
> 2026-07-16：schema v2 云端非敏感 Compose 合约与 legacy 云快照进一步按 track 隔离到 `/var/lib/allbot/releases/<track>/<sha>/`。修复同 SHA test-execution 覆盖 control-plane `release.env`、使控制面回滚缺少镜像变量的问题；预检、部署、回滚、恢复验证和 transaction snapshot 统一解析 track-scoped 路径，TDD 覆盖发布写入与 immutable 回滚读取。
> 2026-07-16：补充 fail-closed 的云测试数据服务修复模式。仅当 test/control-plane 状态存在而 immutable PostgreSQL/Redis 缺失，且已从停止的 legacy Redis 取证 worker pending/running 均为 0，才允许精确选择 postgres+redis 并同时确认 legacy cutover 与空队列；普通发布不跳过 drain，test-execution 不得调用该模式。事实源为 `scripts/release.py`、发布器 TDD、云测试/不可变发布文档与 ops Skill。
> 2026-07-15（已由 2026-07-16 风险分级发布决策取代）：test-train 曾默认按 control-plane→test-execution 串行部署；当前默认不再部署 test-execution，只有专项诊断显式使用 `--with-test-execution`。状态与 acceptance evidence 只记录实际部署的 track，未部署 Worker 不得写成通过；事实源为 `scripts/test_train_release.py`、`tests/ops/test_test_train_release.py`、并发工作区 Skill、不可变发布与云测试文档。
> 2026-07-15：主 Bot/Web 的公开逻辑类型 `free_edit_v2_5` 扩为 1/2 图动态链路：单图 3 灵石 alias 到 `pornmaster_flux2_edit_bf16`，双图 7 灵石 alias 到内部 `pornmaster_flux2_multi_edit_bf16`，均单阶段直出且不调用换脸；自由P图 v3 保持单图 BF16→原脸恢复与 5 灵石两阶段语义。v2.5/v3 普通结果均可投稿；v2.5 apply-context 仅返回原输入张数而不复用原图，模板锁 prompt、隐藏 LoRA并要求重新上传 1/2 张，分别扣 3/7 灵石，v3 模板仍为 1 张/5 灵石；派生结果禁止再投稿。双图内部类型复用既有 multiple-images workflow、BF16 模型和共享 GPU profile，autoscaler 聚合两个 BF16 队列，canary 覆盖单/双图；QQCC 生成与 6 灵石口径不变，仅在市集正确标名并交给 Web 应用。
> 2026-07-16：并发开发容量从 A-D 四槽位扩展为 A-H 八槽位；`manage_ai_workspaces.py` 的 init/status/assign/claim/park/refresh 与 `test_train_release.py` 的 slot 审计统一接受 A-H，自动接单仍按字母顺序选择首个安全槽位，共享测试站仍保持单写入者。事实源为两个管理脚本、专项测试、并发工作区 Skill/SOP 与 ADR 0005。
> 2026-07-15：明确 A-H 并发槽位采用“能力开放、授权分离”：同一受信任 OS 用户下，功能 AI 可读取真实 env、配置、SSH/API 凭据、日志、数据库连接和远端状态，用于只读核对、本地测试与计划；秘密原文不得进入对话、日志、diff、commit 或 PR，凭据可见不授权共享 test/prod、Cloudflare、RunPod/GPU、migration 或发布状态 mutation。`.venv`、`node_modules` 与可写缓存仍以槽位独立为目标，但审计发现运行中任务沿用历史共享依赖时只记录风险，不自动中断或清理。事实源为 `AGENTS.md`、并发工作区 Skill、自动接单/测试列车文档和 ADR 0005。
> 历史：2026-07-15 新增 A-D 四槽位并发开发与受保护 `codex/test-train`（后扩 A-H）。旧 test-candidate/promotion 日常流程已由 ADR 0008 取代；A-H 工作区、唯一共享测试站单写者和 forward-fix 边界继续有效。
> 2026-07-16：正式 Dashboard LAN AIO 基础控制收口到不可变 SSH runner 契约：prod overlay 固定 SSH、env schema 强制 runner host/key directory、Compose 只读挂载精确 key，本地主以独立 systemd OpenSSH 在 Tailscale 地址 2222 端口提供 runner，不干扰 Tailscale SSH 22。cloud preflight 校验可读性、`600` 权限及真实 runner helper/env 连通性，后端在 prod 禁止回退 local；专项 TDD 同时锁定命令构造、Compose、env、systemd unit 与 preflight，避免后续发布再次丢失 runner。事实源为 `runpod_admin_commands.py`、v2/legacy prod Compose、`deploy/env.schema.yml`、systemd unit、`scripts/release.py`、专项测试、GPU/云正式/不可变发布文档与 ops Skill。
>
> 历史（已由 ADR 0008 取代）：2026-07-17 的 candidate accept 曾经决定槽位 park 时机。当前槽位在不可变 handoff 成功后立即释放，不等待测试环境；正式发布的维护模式、精确 digest 证据和非目标服务保护契约不变。
> 2026-07-15：收口本轮生产修复进入不可变基线：Dashboard 对 Redis 中 detached `add` operation 只在对应 RunPod worker heartbeat 新鲜、`idle|running` 且 enabled 时自动写回 succeeded 并释放 active-add 锁；gpu-226 GPU0 当前态更新为 i2i_pro，BF16/image_to_video 等保留同卡 ready 候选；prod overlay 强制注入 `RUNPOD_IMAGE_NAME_PORNMASTER_FLUX2_EDIT`，PornMaster 镜像 smoke 强制验证 BF16 复用的 single-edit workflow、mapping 和解析表，避免容器重建或新 Pod 再现现场补变量/缺 workflow 问题。本轮仅固化代码、测试与知识库，不重建正式服务或创建 RunPod。
> 2026-07-15：不可变发布升级为 schema v2 与三发布链。控制面/测试执行面/GPU 执行面使用环境无关 manifest，控制面与 Worker 拆为模块级薄镜像，Public Web 保持单 tar；正式逐模块晋级只接受云测试 verified 的同 digest。RunPod/LAN profile 改为 baked agent/workflow，禁止启动 clone，模型由 key/size/SHA256 固定并要求逐 profile canary。v2 bundle 发布与 GPU canary 解耦：缺证据的变化 profile 从可用 artifacts 移除并显式标记 incomplete，不创建 RunPod且不阻断其它两条链；GPU 选择/部署/晋级仍 fail closed。CI 沿 main first-parent 选择最近成功 bundle 做增量基线。首次 GPU 聚合按 6 个 runtime 镜像映射 8 个发布 profile，补齐 img2img 同 SHA 构建入口，并以真实 Central task type、版本化 model manifest key 和 GPU 列表校正 v2 catalog。正式 legacy Relay/暂停容器经只读审计保留 dormant，本轮不下线、不重建正式 Dashboard、不操作现有 RunPod。
> 2026-07-15：所有当前正式 LAN AIO Worker 已统一启用深度 1 原子预接，预取类型自动跟随各自 `SUPPORTED_TASK_TYPES`，预接等待期间以 `set_current=false` 续 task heartbeat；六个 active slot 均通过单卡 drain/wait-idle/restart、Docker health、Central enabled 与实际 env 核验。RunPod 统一 create request 也已固化同一深度 1 预接环境契约，覆盖后续 autoscaler/手动新建的 cloud-test/cloud-prod Pod，但不原地更新现有 Pod。同步收口 gpu-002 GPU1 slot catalog 漂移为实时 `image_to_video` 当前态，并给 fleet 远端镜像 pull 增加 300 秒超时与遗留同镜像 pull 清理，保留 runner 镜像导入兜底。
>
> 2026-07-17：gpu-002 GPU1 已从 `image_to_video` 受控切换为 `i2i_pro`。候选声明、精确镜像、六文件模型缓存、自然 drain、disabled 启动、Docker/ComfyUI/heartbeat、旧进程清理与 enable 门禁均通过；fleet state/catalog、LAN 资源文档和容量画像同步把 image_to_video/PornMaster 标为同卡回切候选。
> 2026-07-15：正式发布链路新增 `--dashboard-fast-track`。用户明确授权时，管理后台可跳过云测试部署/verified 晋级，但仍要求 main 可达完整 SHA、成功 CI、digest-pinned Dashboard 镜像、生产确认、全量只读 preflight、事务回滚与非目标容器启动时间不变；路径 allowlist 只接受 Dashboard runtime、BF16 Dashboard catalog 和配套 release/docs/tests 元数据，拒绝 migration、共享 runtime、其它 GPU ops、Compose、未知路径与 modules/services/from-SHA 覆盖，并强制 rolling、不写生成维护标志。2026-07-16 补齐 schema v2：按目标 main merge 的 first-parent delta 分类、只选择对应 Dashboard artifact，实际 production current state 继续作为回滚基线。同轮 `pornmaster_flux2_edit_bf16` 已纳入 Dashboard autoscaler，旧 v2 监控行隐藏。
> 2026-07-14：测试验收仍默认要求同一 SHA/digest 完成全部代表任务、回滚演练与 24 小时观察；新增显式、可审计的短观察例外。只有用户明确确认测试服务无问题并授权提前晋级时，`verify-test --confirm-short-observation` 才接受 evidence 中的 `short_observation_override=true`、非空原因与批准者。例外只跳过固定等待时长，时间顺序、全部 smoke、Web/Worker 运行态和 SHA/digest 一致性继续 fail closed；verified current/history 持久化实际观察秒数与例外审计。
> 2026-07-14：云测试 BF16 生成完成后出现 upload sidecar 502，运行态取证确认 agent 与不可变 release relay 的 `/app/spool` 分别绑定仓库 `logs/worker-spool-cloud-test` 和 `/var/lib/allbot/test-worker/spool`，relay 因不可见 1.7 MB 成品而三次失败。cloud-test worker compose 现统一默认宿主 spool 为 `/var/lib/allbot/test-worker/spool`，可用单一 `CLOUD_TEST_WORKER_SPOOL_HOST_DIR` 做整体覆盖；relay 逐次记录底层异常，compose/日志回归测试锁定该契约。仅更新云测试接单层，不修改正式 GPU runtime 或正式 worker。
> 2026-07-14：PR #21 已把全量只读 `preflight`、双维护路径、Pages production/canonical/runtime SHA、无秘密事务 journal 与逆向 `recover` 合入 main；`1e0fb87...` release workflow 成功。首次云测试 deploy 的 cloud pull 中断后，恢复验证误查未触碰且原本 stopped 的测试 Worker，现已用 TDD 修正为只验证 attempted stages，并将现场事务收口为 `rolled_back/recovery_verified`、旧栈健康且 test 维护解除。最新边界明确：测试事务仍为 cloud → 测试 Worker → Pages，正式事务仅 cloud → Pages；生产 GPU Worker 由各 GPU host operator 独立发布，prod 不检查 heartbeat/relay、不操作 Worker，显式 `--services worker` fail closed。本轮未修改生产容器、正式机或正式 Pages。
> 2026-07-14：Web 发布收敛为单一环境无关 tar + 版本化公开 runtime config + Wrangler Pages；测试目标 `allbot-web-cf-test/test`、正式目标 `allbot-web-prod/main`，状态将记录 Pages 元数据与 config revision，旧测试 VPS 不再接收 SSH/scp 文件。只读门禁同时发现 `api-cf-test` 错误回源正式 `100.107.220.127:8000`，经测试 mutation 授权已改为云测试 `100.82.124.91:8001` 并完成配置回读与公网 health 200；独立 `600` Pages token 已通过 API 200 验证，测试项目 production/preview Git 自动部署已关闭，正式项目未修改。
> 2026-07-14：不可变测试切换后的两次 Bot 任务均因 `Errno -3` 派发失败并完成 Saga 退款。根因是 legacy test env 的 `API_BASE_TEST=central-api-test` 在 `BOT_TYPE=TEST` 下优先于 overlay 的 `API_BASE=central-api`，而原健康探针误测了未解析原始 env。修正候选同时钉死 test `API_BASE`/`API_BASE_TEST`，补全 prod 所有 Python 消费者的 `API_BASE`，并在 release 写状态前进入容器 import `config` 校验解析值；须待新 main SHA/digest 仅部署测试并完成真实任务回归，生产 mutation 仍未授权。
> 2026-07-14：`c6d473...` 测试发布暴露 SSH stdin 截断：远端 `docker compose exec -T` 队列探针读取了后续 bash 脚本，云镜像未 pull/recreate，但 Worker 与状态清单继续更新。测试控制面已用同 SHA/digest 受控恢复并核对容器 digest、OCI revision、解析配置和非目标数据库启动时间；发布器修正为所有远端 Compose exec/run 使用 `</dev/null`、要求绑定 SHA 的完成标记并在写状态前逐容器核对 digest/revision。须等修正后的新 main bundle 重跑测试，生产 mutation 仍未授权。
> 2026-07-14：首次切换执行前的运行态核对确认测试 env 未配置 QQCC Bot token、私有 QQCC rollout gate 也未开启；依赖 planner 的全栈集合不能等同于强制启动可选 Bot。发布器现用已校验 env 仅过滤 `qqcc-bot`、`qqcc-private-bot-worker`、`paid-group-guard-bot` 三个明确禁用 runtime，plan 显示 `disabled_cloud_services`；API、Postgres、Redis、主 Bot 等核心依赖仍不能通过配置缩小。
> 2026-07-14：`587d665...` 首次测试切换在 Dashboard Frontend 健康门禁 fail closed，原因是镜像未创建 `/etc/nginx/templates`；自动回滚已移除新容器、清理维护标志并恢复 legacy 控制面，Web/Worker/成功状态未切换。修正同时把 frontend image smoke 升级为真实执行 dashboard/QQCC 两种入口模式，须等新主线 SHA/digest 才能重试；生产 mutation 仍未授权。
> 2026-07-14：`074109e...` 重试在 Dashboard/QQCC Backend 健康门禁 fail closed，镜像缺少根 `config.py`、`paid_group_guard_bot` 和 `ops` 导入闭包；自动回滚再次恢复 legacy 控制面，队列为空，Web/Worker/成功状态仍未切换。修正将 Dashboard image smoke 升级为真实 import Dashboard 和 QQCC 两个 ASGI app，须等新主线 SHA/digest 才能重试；生产 mutation 仍未授权。
> 2026-07-14：`92fcdd3...` 已完成测试云控制面与本地 Worker 的同 SHA/digest 首次切换，原数据卷复用、并发锁签名、task core、locale、Worker heartbeat 验证通过。测试 Web 边缘机已离线 18 小时，因此用 `--skip-web` 完成其余切换，Web 仍未验收并阻止正式晋级。发布器补充 Web SSH 有界超时，并把 skip 状态明确记为 `health.web=skipped`，不再误记 checksum passed；生产 mutation 仍未授权。
> 2026-07-14：首次仅测试执行使用已全绿并独立核验的 `a2a44...` bundle，在新 Compose 项目绑定旧 Dashboard 端口时 fail closed；未写成功状态、未切 Web/Worker，部分新容器已清理且旧控制面保持单实例。复盘补齐首次 legacy 控制面交接：测试 Postgres/Redis 纳入初始依赖集合，显式复用 `deploy_cloud-*-data` 数据卷，保留 `postgres-test`/`redis-test` DNS 与 `CLOUD_TEST_POSTGRES_*` 映射；全部 digest pull/校验及 legacy Central drain 后才停旧容器，新项目失败则移除目标容器并重启本轮实际运行的 legacy 容器。旧 `a2a44...` 只作演练证据，须等待修正合入后的新 SHA/bundle；生产 mutation 仍未授权。
> 2026-07-13：首次仅测试环境切换预检发现并收口 Worker/配置发布闭包：公共 worker compose 覆盖 01–08 与 pool/prefetch/pipeline/SCAIL-2 参数，未选 dormant 槽位使用 disabled 默认且 allowlist 槽位由 schema 强校验；发布器在跨云控制面/本地 Worker 维护发布中保持 generation maintenance，首次切换先停 allowlist 对应 legacy 同 Agent 容器，digest Worker health 后才解锁；测试 Web key 移出源码 checkout；test-only env 迁移以云测试当前 env 为控制面事实源、本机旧 env 只补 Worker 参数并脱敏输出。聚焦回归 29 passed，合并候选 schema 与 cloud/worker Compose `config -q` 通过；生产 mutation 仍未授权。
> 2026-07-13（早期 stabilization 基线）：统一 release workflow、digest manifest、公共 cloud/worker compose、env schema、影响 planner、test acceptance/生产同 digest 晋级、legacy rsync fail-closed 与 ADR 0003 已落仓；Python 2563、Web 294、Dashboard 93、Ops 379 项测试及两个前端生产构建通过，Alembic 单 head，三套 immutable Compose dummy `config -q` 通过。后续 Git/CI 与仅测试切换进度以上方更新记录为准。
> 2026-07-14：Web 自由P图已从 v2 升级为 `edit_v3`：单图、5 灵石、BF16 编辑后自动以原图恢复人脸；Web finalizer 使用带版本 continuation 和确定性第二阶段 ID 保证重启恢复与幂等。Gallery 新增 `free_edit_v3_group` 并把历史 single/multi v2 统一展示、应用为 v3，旧 group 仅保留查询别名。QQCC v3 仍保持单 BF16、6 灵石口径。
>
> 2026-07-12：QQCC 自由P图 v3 已补齐 `free_edit_v3` → 单图 `pornmaster_flux2_edit_bf16`、6 灵石、配置前后端与 Central 路由。随后仅 force-recreate `central-api-prod` 使挂载代码重新注册路由；正式 OpenAPI 已出现该 POST，BF16 worker 保持在线，QQCC v3 可端到端提交。
> 2026-07-13：为 `pornmaster_flux2_edit_bf16` 增加独立 RunPod RTX 4090 profile。云端模型使用源下载链接直转 R2 和 R2 server-side copy，三文件 manifest 校验通过；Pod `0owo38uayvyjcu` 只声明 BF16 任务，RTX 4090 24GB + `--lowvram` 单图 canary 端到端 23.88 秒完成且无 OOM，现保持 idle/enabled 供人工验收。正式 Pod 未开 SSH，本轮未采集精确峰值显存曲线。2026-07-15 按用户要求将该 profile 纳入 Dashboard autoscaler，默认按 30 秒/单、30 分钟清空阈值复用 add/down/restart/enable、锁定跳过、最短生命周期、冷却和失败清理。
> 2026-07-12：QQCC 用户私有 Bot 平台已在未提交工作区补齐 schema、加密凭据、申请/管理 API、webhook stream、独立 worker、Owner/Admin UI 与 test/prod compose 定义；同日经用户明确确认完成云正式 migration `3e9c7a1b5d24`、严格 env validator、`PRIVATE_QQCC_BOT_ENABLED=true`、private worker/官方 QQCC/Web/API/配置后台发布，以及 `private-bot.aivison.it.com` Tunnel + DNS 上线。首个 owner 真实 token 申请仍待用户手动验证，真实 500 租户外部负载验收仍未完成。

本矩阵记录 2026-06-27 起对 AllBot 实时知识库的逐项核对结果，并承接至 2026-07-11 的 QQCC、Cloudflare、Bot/FSM、registry、任务队列展示与本地分析平台等局部同步。2026-07-11 再次执行全仓静态扫描并重建可复现基线。事实源优先级为当前代码和未提交工作区、compose、脚本、Alembic、`pytest --collect-only`、Ruff、Radon、Symilar、Vulture、文档结构检查与明确标注的只读运行态/API 探测；未标注的远端 SSH、线上 curl 或 Docker 运行态不作为本轮事实源。

## 1. 本轮校准基线

| 项目 | 当前事实 |
| :--- | :--- |
| Git 分支 / 提交 | `codex/identity-concurrency-limits` / `5ac63b54`；扫描包含 2026-07-11 当时的未提交工作区改动，不把分支名视为部署环境事实 |
| 代码规模 | 1513 个 tracked 文件；769 个 Python 文件；TypeScript/TSX/Vue/JavaScript 合计 348 个；Python/TS/TSX/Vue/JS/Shell 合计约 291,440 行（包含测试、生成/供应商静态资源与双 worker bundle） |
| Alembic | 当前代码单 head：`3e9c7a1b5d24`；2026-07-12 已在云正式执行到该 head，云测试运行态仍需单独复核 |
| 测试门禁 | `python -m pytest -q`：2563 passed；`tests/ops`：379 passed；Web：294 passed；Dashboard：93 passed；两个前端生产构建通过 |
| Ruff | `ruff check . --statistics` 零告警 |
| 复杂度 | Radon 扫描 5335 个 Python block，平均圈复杂度 3.49；A/B/C/D/E/F 分别为 4391/683/220/32/7/2，41 个 block 复杂度不低于 21 |
| 重复与死代码 | Symilar 对非测试 Python、8 行窗口估计重复率 4.45%，主要由 `workers/` 与 `remote_workers/` 双 bundle 镜像造成；Vulture 90% 置信度的 2 个“不可达”候选均为含显式 `break` 的分页 `while True` 误报，本轮未确认高置信死代码 |
| 文档结构 | `python scripts/doc_quality_checker.py` 通过 |
| Core Isolation | `src/core` 未发现 Telegram `Update` 或 FastAPI `Request/APIRouter` 等平台对象 import，入口对象隔离成立；但仍直接依赖 `config`、SQLAlchemy、HTTPX、PIL、subprocess 与部分默认 provider，基础设施隔离只达到“部分完成”，不能再表述为 core 只消费 capability/provider 的已实现事实 |
| 远端运行态 | 本轮未执行远端 SSH、线上 curl、Docker/Redis/PostgreSQL 或 Cloudflare 运行态探测；容量、worker 健康、域名和服务状态均需人工运行态复核 |
| 云测试入口 | `scripts/release.py plan\|deploy --env test --sha <full-sha>`；代码/env rsync、现场 build 与源码 bind mount 均禁止 |
| 云正式入口 | `release.py deploy --env prod --strategy auto|standard|direct|emergency` 按模块风险和 artifact digest 判门禁；管理面/执行面默认 direct，公共 Web 可显式 direct，核心仅 emergency 可旁路，migration/共享契约/未知路径永久 standard；`--dashboard-fast-track` 仅为兼容别名 |
| 本地云正式 shadow 同步 | `scripts/sync_cloud_prod_to_local_shadow.py` 默认 dry-run，`--execute` 才把云正式 PostgreSQL 恢复为本地 `bot_db_prod_shadow`；数据库获取主路径为 `CLOUD_PROD_DB_DUMP_MODE=remote_r2`，由 `allbot-do-sgp1-control` 在云机 dump 后临时上传 R2 `user-data-prod/__shadow-transfer/<timestamp>`，本地经 HTTPS/rclone 下载校验后 restore，避免依赖家宽/VPN 出口 IP 作为托管 DB trusted source；恢复 `_next` 后、切换 shadow 前默认用 `LOCAL_ANALYTICS_PRESERVE_ON_SHADOW_SYNC=true` 保留旧 shadow 中用户画像快照、Prompt Mart、提示词瘦身、词元映射规则、指定词元规则、词元删除规则、词元统计/词元提示词映射和 embedding/state 基础表，prompt 表使用显式白名单避免旧派生表被恢复；`R2_BUCKET_SYNC_ENABLED=true` 时才把 R2 `user-data-prod` 增量同步到 MinIO `user-data-prod-shadow` 并用 quarantine 保留云端覆盖/删除的旧对象；`COMPLETE_MEDIA_SYNC_ENABLED=true` 时每日从本地 R2 shadow 非破坏式 copy 到 `user-data-complete-shadow`；legacy `bot-data`/`comfyui-temp` 只在手动 `--include-legacy-media-import` 首次/补漏时导入；脚本持有 `.shadow-sync.lock` 防并发；本地分析刷新入口为 `scripts/run_local_analytics_shadow_pipeline.py` 与 `allbot-local-analytics-refresh.timer`，默认每日 Asia/Shanghai 05:45；刷新链会先 upsert `analytics_user_profile_daily_snapshots`，即使 `.refresh_prompt_vectors.lock` 显示上一轮向量刷新仍在运行也保留当天画像快照，随后才可能跳过 Prompt Mart/embedding 链；无向量锁时按 affected `prompt_hash` 增量刷新 Mart、瘦身、词元和缺失 embedding，不再生成语义场景、相似边、近重复族或图谱；05:00 切库断线后向量阶段重连并断点续跑 |
| 旧本地脚本 | `safe_deploy.sh` 只用于云正式整体故障时的本地正式灾备；`safe_deploy_test.sh` 只作历史取证 |
| 归档材料 | `docs/archive/` 与 `logs/` 只作历史证据或排障报告，不作为当前 SOP |

## 2. 总览与索引文档

| 文档 | 事实源 | 本轮状态 | 处理结果 |
| :--- | :--- | :--- | :--- |
| `README.md` | `docs/` 清单、`.github/workflows/docs_ci.yml`、部署脚本 | 已修正 | 按 `docs/*.md` 当前清单补齐活跃索引，恢复 GPU Pool、QQCC、付费群审核、本地数据分析平台与 Cloudflare 公网入口等入口；同步云测试快速单模块更新为日常默认；2026-07-12 新增 QQCC 用户私有 Bot 平台文档索引 |
| `AGENTS.md` | `.codex/skills/*/SKILL.md`、运维脚本 | 已修正 | 测试优先部署改为云测试快速单模块更新优先，维护式脚本退为整栈/迁移/排空/明确维护窗口入口；新增 `allbot-cloudflare-ops` 路由；2026-07-12 将 QQCC 私有 Bot 路由到 `allbot-qqcc-lazy-bot` 与专项文档 |
| `docs/knowledge_base_audit_matrix.md` | 本轮代码/静态工具/文档校验 | 已修正 | 重建 2026-07-11 基线，明确工作区范围、2240 测试、Ruff/Radon/Symilar/Vulture 结果、Core 平台隔离与基础设施隔离的差异，并标记未探测远端运行态 |
| `docs/system_architecture_report.md` | compose、服务入口、core import graph、静态质量基线 | 已修正 | 保留多入口与部署拓扑，纠正“core 只消费 capability/provider”为目标边界而非已完成事实；同步 2026-07-11 质量与架构债务摘要 |
| `docs/skills/README.md` | `.codex/skills` 清单与 Skill 体积审计 | 已修正 | 保留历史瘦身记录，新增 2026-07-11 体积复核；将 QQCC、Gallery、TG FSM 标为下一批短入口拆分候选 |
| `docs/domain/CONTEXT.md` | 领域文档、运维脚本、本地分析平台代码与文档、Cloudflare 只读 API 探测 | 已修正 | 补充实时知识库、归档材料、运行态快照、维护式更新、云测试快速更新、本地数据分析平台、shadow 数据库、Prompt Mart、提示词瘦身、提示词向量化、提示词词义分析、Cloudflare 公网入口与 Cloudflare 自动化令牌等术语 |
| `docs/adr/0000-template.md` | ADR 模板 | 已核对 | 模板有效；QQCC 私有 Bot polling/webhook 多租户取舍属于长期、非显然决策，已新增 ADR 0002 |
| `docs/adr/0001-postgresql-only-runtime.md` | `src/database/core.py`、`src/database/models.py`、`migrations/versions/`、云正式/云测试 PostgreSQL 运行口径 | 新增 | 明确 AllBot 后端运行时数据库为 PostgreSQL-only；`::json`、partial index、`setval(pg_get_serial_sequence(...))` 与 PostgreSQL migration 语义不再作为跨数据库兼容缺陷处理 |
| `docs/adr/0002-qqcc-private-bots-use-webhooks.md` | private Bot schema/lifecycle、Web API ingress、Redis stream worker、compose/Nginx | 新增 | 记录官方 QQCC 保持 polling、私有 Bot 统一 webhook + stream worker、exact client type、加密凭据与 owner/admin Host 隔离的决策和代价 |

## 3. 任务、Worker、Comfy 与 RunPod

| 文档 | 事实源 | 本轮状态 | 处理结果 |
| :--- | :--- | :--- | :--- |
| `docs/子模块_任务调度_task_scheduler.md` | `src/core/task_core*.py`、`src/task_core_process_defaults.py`、部署脚本、`src/domain_config/task_type_registry.py`、`backend/app/main_simple_task_routes.py`、`src/workflow_mapping_validation.py` | 已修正 | 部署章节同步云测试快速单模块更新入口；2026-07-04 补充取消退款账本幂等：`registry_task_id` 派生 `task_refund:refund_user_cancel:<registry_task_id>`，重复取消/monitor 收口只允许第一次加灵石；2026-07-06 同步 task type registry 已驱动 Gallery/apply、Central simple task 映射与 workflow filename facts，dispatcher 策略仍由 core 显式装配；2026-07-17 新增 worker pool registry，task facade 把 `task_type` 传入 billing seam，在扣费前按目标执行池 projected pending 做低阶外门准入 |
| `docs/子模块_生成任务全链路_task_full_chain.md` | `src/web_api/services/*task*`、`backend/app`、`workers/comfy_agent`、`src/domain_config/task_type_registry.py`、`src/workflow_mapping_validation.py` | 已修正 | 主链路仍符合现状；补充自由P图 v2 到 `pornmaster_flux2_single_edit` / `pornmaster_flux2_multi_edit` 的费用、无 LoRA 和 exact worker task type 路由；修正 SCAIL-2 动作迁移合并长时长：用户侧仍为 `scail2_action_transfer`，10/15/20s 隐式路由到 `scail2_action_transfer_long` 执行；同步长动作迁移 Context Windows `freenoise=true` 速度优先口径，并注明动作循环伪影风险可能回归；补充 Web 用户侧 pending 关闭即撤销、active registry `credits_deducted` 退款判断、confirmed cancel 立即 finalizer 退款/释放锁/清理 active registry、running cancel request 不提前清理的口径；补充 `i2i_draw` 局部重绘仅在 Web 提交入口禁用、core/dispatcher/worker 能力保留的口径；2026-07-04 同步 Redis 连接瞬断鲁棒性：`api_client` 按 submit/status/media 隔离 breaker，Central Redis transient 503 进入 busy/补偿路径；2026-07-04 同步取消退款 `credit_idempotency_key` 语义，Web/Bot/恢复重复看到同一 `cancelled` 不重复退款；2026-07-06 同步新增任务 checklist：simple route 先补 registry `central_type`，workflow 映射先补 registry `workflow_filename`；2026-07-16 同步用户展示 presenter：task type 全量 i18n、未知值不回显，新 History prompt 纯正文 + `extra_outputs._generation_context`，旧系统前缀读取兼容且不迁移数据 |
| `docs/子模块_计费与支付_billing_payment.md` / `docs/子模块_社区与存储_gallery_storage.md` / `docs/子模块_附加模型配置指南_comfy_models.md` | `src/services/user_visible_generation_presenter.py`、账本/History/Gallery/apply 服务、共享 locales | 已修正 | 2026-07-16 同步灵石账本 `display_key` 契约、Gallery 清洗后遮罩与公共 `prompt_model`、新任务结构化 generation context、旧 prompt 前缀兼容及未知模型脱敏边界；不涉及 schema migration、计费/路由/worker 协议变更 |
| `docs/子模块_生成任务全链路_task_full_chain.md` | `src/core/task_core.py`、`src/core/task_core_submission.py`、`src/core/task_core_runtime.py`、`src/services/task_service_flow.py`、`src/services/tg_task_runtime.py`、`src/services/qqcc_draw_chain_service.py`、`src/services/quick_video_submission_service.py` | 已修正 | 2026-07-11 同步 Bot task flow 的 `base_priority`、`allow_cancel` 与 `user_cancel_allowed` 语义：默认保持普通任务行为；active registry 的 `user_cancel_allowed=false` 是用户取消权威边界，不调用 Central cancel、不退款；QQCC 链式 continuation 以 `base_priority=100` 排队且不展示取消按钮，不改数据库结构或 worker workflow |
| `docs/子模块_生成任务全链路_task_full_chain.md` / `docs/子模块_QQCC懒人Bot_qqcc_lazy_bot.md` / `.codex/skills/allbot-qqcc-lazy-bot/SKILL.md` | `src/services/task_service_flow.py`、`src/services/tg_task_runtime.py`、图片/旧视频/Wan22/LTX actor 入口、`task_recovery_contract.py`、`private_qqcc_continuation_service.py`、quick image/video submission service | 已修正 | 2026-07-16 新增 presentation-only `show_queue_status`（默认 `true`）：QQCC AI绘图、AI动图、AI视频只有首个真实子任务展示排队并允许取消，第 2 个及以后 pending/running 均持续展示“生成中”；官方恢复 contract 与私有 durable stage plan 保留该策略。成功/失败/退款终态、Central 队列、`base_priority`、计费、任务顺序与 Worker 调度不变 |
| `docs/子模块_生成任务全链路_task_full_chain.md` | private worker、`src/services/recovery_service.py`、task registry、QQCC runtime context | 已修正 | 2026-07-12 补充 `bot:qqcc-private:<id>` exact 恢复、访客按自己账户扣费、同 Bot 顺序/跨 Bot 并发及暂停/禁用不取消既有任务的租户语义 |
| `docs/子模块_生成任务全链路_task_full_chain.md` / `docs/子模块_QQCC懒人Bot_qqcc_lazy_bot.md` / `docs/子模块_QQCC用户私有Bot平台_qqcc_private_bot_platform.md` | `src/services/task_recovery_contract.py`、`task_recovery_runtime.py`、`private_qqcc_continuation_service.py`、quick image/video submission service、private worker 与 FSM | 已修正 | 2026-07-12 私有 Bot `_bot_task_recovery` 展示契约与 exact tenant 恢复保留；新增 Redis durable continuation checkpoint，原始输入先持久化，多步绘图/原图换脸/尾帧视频以确定性 stage/registry ID 跨重启续跑。中间阶段先 CAS advance 再 cleanup，最终结果先进入 `delivery_pending`、发送后再 CAS delivered；checkpoint outage 保留 paid runtime，TaskRegistry 空时仍扫描，租约丢失取消旧 executor，暂停/禁用不截断已接纳链，永久解绑停止；缺契约或无效 continuation ref 的隐藏任务仍不得发送半成品 |
| `docs/子模块_生成任务全链路_task_full_chain.md` / `docs/子模块_中控API与节点通信_central_api.md` | `src/services/task_queue_position_display.py`、`src/services/tg_task_progress_presentation.py`、`src/services/image_service.py`、`src/api_client.py`、`src/web_api/services/task_runtime_api_service.py`、`src/web_api/services/task_stream*.py`、`backend/app/main_response_helpers.py` | 已修正 | 2026-07-11 同步用户侧排队展示口径：Central 原始 `queue_pos` 仍为全局 0-based rank，`include_type_position=true` 额外返回同任务类型 `queue_type_pos`；Bot 进度、Web 粗状态与 Web SSE fallback 都请求同类位置并优先展示 `queue_type_pos`，缺失时回退全局 `queue_pos`；Web 对前端字段名继续保持 `queue_pos` 兼容，不改调度、Redis、扣费、取消或 worker 分发语义 |
| `docs/子模块_生成任务全链路_task_full_chain.md` | `src/api_client.py`、`src/utils.py`、`src/web_api/services/task_runtime_api_service.py`、Web 状态回归测试 | 已修正 | 2026-07-18 纠正 Central 404 触发 breaker 的误判：HTTP 4xx 均不计入，真实打开条件为传输错误、超时或 5xx；Web 对已归属 active task 在 Central 短时不可用时返回 registry 保守粗状态而非 HTTP 500，并在请求/重试日志保留异常类型 |
| `docs/子模块_中控API与节点通信_central_api.md` | `backend/app/main.py`、`backend/app/queue_manager.py`、`backend/app/main_simple_task_routes.py`、`src/domain_config/task_type_registry.py`、worker relay | 已修正 | Central / worker protocol 口径有效；同步 `/system/status.queue_by_type_details` 作为 Central pending 轻量等待统计，按 `priority <= 0` / `priority > 0` 分免费与付费最长等待，区别于 Dashboard 低信任聚合口径；SCAIL-2 测试与正式 LAN worker 四任务声明、正式 RunPod scail2 两任务口径仍有效；2026-07-04 同步统一 Redis factory、Central transient 503、入队事务 retry 与 `zpopmin` 不盲 retry 口径；2026-07-06 同步 Central simple route 的 task key -> `TaskType` 值从 registry `central_type` 派生，队列 task type 与 worker `SUPPORTED_TASK_TYPES` 分发语义不变；2026-07-17 新增 `queue_pressure_by_worker_profile`，同池 pending 求和且只统计健康 enabled Worker，明确其为约 10 秒缓存的快照式准入信号而非原子预约 |
| `docs/子模块_任务黄金路径回归清单_task_golden_path.md` | `tests/backend`、`tests/core`、`tests/web_api`、worker tests | 已修正 | 回归分组仍可用；补充 `tests/core/test_task_runtime_cleanup.py` 覆盖用户取消 pending 后退款/锁释放/active registry 清理，以及 running cancel request 不提前清理 |
| `docs/子模块_GPU算力资源池控制器_gpu_pool_controller.md` | `ops/gpu_pool_controller`、LAN AIO helper/XDG ledger、Dashboard RunPod 服务、RunPod scripts、GPU profile Dockerfile | 已修正 | LAN AIO current/cache/验证时间迁到 XDG ledger/history；Git catalog v2 只保存候选、硬件/端口、digest/manifest 与稳定阻断。普通切换由本地 operator 事务化执行且不改仓库，Dashboard 只保留基础控制并由本地主 helper 二次确认 current；2026-07-19 补充 baked `remote_workers` 必须在镜像构建阶段以自身 `PYTHONPATH` import workflow patcher，避免 compatibility module 漂移延迟到生产启动才暴露 |
| 图片换脸 V1/V2 执行类型拆分 | `src/domain_config/task_type_registry.py`、Central routes/API client、自由P图 v3/QQCC continuation、worker mappings、i2i_pro release catalog/LAN/test migration/canary/autoscaler、相关 Skills 与专项文档 | 已修正，待运行态发布 | 2026-07-18 发布 artifact、全部 i2i_pro LAN slot 与测试迁移器统一为 `face_swap_v2`，main GPU rebuild 改为同 SHA OCI manifest fail-closed；V1 `face_swap` 仍固定旧 workflow、1 灵石和 `worker_remote_02`。本轮未修改生产运行态，测试/正式 Worker 能力仍须按发布门禁核验 |
| `${XDG_STATE_HOME:-~/.local/state}/allbot/lan-aio/current.yml`、`history/*.json` | `scripts/lan_aio_fleet_prod_ops.py status/state-init/state-reconcile`、远端 Docker/container/cache marker 检查 | 已更新，需部署后运行态复核 | LAN AIO 易变 current/cache/验证时间迁出 Git；helper 以原子 current + operation history 维护，live/ledger/catalog drift 与未完成 operation 均 fail closed。本轮只实现和本地测试，未查询或修改生产 live |
| `ops/gpu_pool_controller/config/lan_aio_fleet_state.legacy.yml` | 迁移前 Git state 快照 | 已冻结 | 仅作首次 `state-init` 的兼容种子，普通 takeover/recover/restart/warm-cache 后禁止更新；初始化后仍须 live status 或显式 reconcile |
| QQCC AI动图 5 LoRA / 49 对目录（2026-07-18） | `src/wan22_explicit_lora_catalog.py`、`src/qqcc_video_lora_catalog.py`、QQCC config/quick submission/demo、Central/API/dispatcher、Wan22 patcher、LAN/R2 manifest 组装、split manifest、GPU release catalog、RunPod request/worker、QQCC Config Vue | 契约已修正，待 GPU 正式验收 | 官方与私有 QQCC 的 `image_to_video`/`wan22_video_v2` 支持最多 5 个有序 `{name,strength}`，主 Bot 不增加多选入口；后台候选来自本地 registry 已下载并核对的 49 High + 49 Low，逐项带真实路径和保守推荐强度，旧七键自动迁移。manifest 组装把 `wan22_explicit_lora_library/2026-07-18` 合入 Wan22 AIO union，测试锁定全部 98 个显式 LoRA 同时进入两个 split；AIO、旧图生视频与 v2 使用各自独立的 `2026-07-18-lora5` key，旧 6 月 key 保持不可变。R2 的 98 个对象、三个 composed manifest 与 LAN cache 已完成逐对象 size/SHA HEAD 校验；运行态首次直发因 split-video tag-only allowlist 阻断，旧 RunPod 已恢复，现已把请求与 worker 收口为仅接受兼容旧 RIFE tag或 canonical 仓库完整 SHA256 digest，任意 tag/仓库继续 fail closed。本轮不提交人工 1/5-LoRA canary，运行态验收以用户批准的 direct+attested 与首个正常旧任务为准。 |
| `docs/子模块_附加模型配置指南_comfy_models.md` | `workers/comfy_agent/workflows`、`remote_workers`、workflow patcher、PornMaster Flux2 edit workflow/API 映射、`src/lora_catalog.py`、`src/services/advanced_video_submission_service.py`、`src/services/advanced_video_settings_view_service.py`、`src/services/ltx_video_extension_service.py`、`src/services/wan22_video_v2_extension_service.py`、QQCC config service/FSM | 已修正 | workflow 事实源和 SCAIL-2/LTX 口径有效；新增 PornMaster Flux2 single/multiple edit API workflow、cached 模型 bundle、LAN cache、云端 RunPod transfer 转存、manifest HEAD 校验发布、Civitai token 安全入口、fp8 默认与 bf16 canary 口径；修正 SCAIL-2 Context Windows API workflow 为动作迁移 10/15/20s hidden execution，不作为独立用户入口；同步 QQCC 场景 engine/LoRA 配置边界：旧 `image_to_video`/`free_edit` 可选 catalog 附加模型，`wan22_video_v2`/`free_edit_v2` 自动清空 LoRA；新增 QQCC `end_frame_draw_scene_id` 复用 AI绘图场景的完整 `postprocess_draw_scene_id` 链生成最终尾帧后首尾帧提交旧图生视频或 v2，不新增 workflow/profile；2026-07-17 修正 QQCC `draw_scenes[].original_face_swap_enabled`：每步绘图后提交 `face_swap_v2`，以用户最初上传原图做人脸来源、该步输出图做 body，每步 +2 灵石，AI动图尾帧继承；快速换脸仍走 V1；2026-07-06 补充主 Bot 高级视频提交计划事实源：`advanced_video_submission_service.py` 只收口 Bot payload 归一、首尾帧与 LTX LoRA/链路字段，不改变 workflow、RunPod profile 或模型目录；2026-07-06 补充 LTX Bot 扩展 seed 与拼接链路恢复事实源：`ltx_video_extension_service.py` 只收口 histories/last_frame/context 准备，不改变 workflow/profile/model dir；2026-07-06 补充主 Bot 高级视频 settings view service 只生成 Telegram view-model/keyboards 与费用展示，不改变 workflow/profile/model dir；2026-07-06 补充 Wan22 AIO Bot 链路扩展/重生成/拼接准备事实源：`wan22_video_v2_extension_service.py` 覆盖旧图生视频与 v2，只收口 histories/last_frame/input/context 准备，不改变 Wan22 workflow/profile/model dir；2026-07-10 补充 QQCC 场景 `negative_prompt` worker 映射：Qwen `img2img/img2img_lora` 写 `4.prompt`，PornMaster Flux2 single/multi 写 `254.text`/`49.text`，Wan22 视频写 `2371.value`，不新增 workflow/profile；2026-07-16 修正 LTX 首尾帧默认与 10Eros override 的时空 VAE 末帧边界：`workers/remote_workers` 两侧 `last_frame_fix=true`，并新增镜像一致性回归测试；继续作为 `allbot-comfy-models` 的节点级细节事实源 |
| `docs/子模块_附加模型配置指南_comfy_models.md` | `src/services/qqcc_config_service.py`、`src/services/qqcc_draw_chain_service.py`、`src/services/quick_image_submission_service.py`、`dashboard/frontend/src/components/QqccBotSettings.vue`、`src/lora_catalog.py` | 已修正 | 2026-07-11 同步 QQCC `filter_scenes` 模型配置边界：AI滤镜复用 AI绘图 `free_edit_v2`/`free_edit`、负面提示词、LoRA 与原图换脸规则；AI滤镜自身无后处理链，可被 `draw_scenes[].postprocess_filter_scene_id` 作为终止后处理模板引用；不新增 workflow、RunPod profile、模型 bundle 或数据库表 |
| `docs/compat_seam_exit_table.md` | compat 文件现状、`rg` 引用 | 已修正 | 作为 compat 清理挂账表保留；2026-07-18 登记 QQCC Wan22 多模型协议对单模型字段及旧七模型名的兼容回退、依赖方和退出条件。 |
| `docs/双入口重复能力_inventory.md` | `backend/app`、`src/web_api` | 已核对 | 双入口分层描述有效 |
| `docs/入口职责矩阵_entry_responsibility_matrix.md` | Web/Central/Dashboard/Payment/Bot entrypoints | 已核对 | 入口职责有效 |
| `docs/测试与入口命名约定.md` | 测试目录与入口命名 | 已修正 | 命名约定有效；2026-07-06 统一以 `src/bot_main.py` + `BOT_TYPE` 作为 Telegram Bot 测试/生产入口 |
| `docs/子模块_热点文件门禁与回归触发规则_hotspot_guardrails.md` | hotspot docs、tests、compat 状态 | 已核对 | 长文档保留为门禁清单 |
| `docs/子模块_GitHub分支保护与热点回归门禁_branch_protection.md` | `.github/workflows`、hotspot guardrails | 已核对 | 分支保护和回归门禁说明有效 |

## 4. Bot 与交互

| 文档 | 事实源 | 本轮状态 | 处理结果 |
| :--- | :--- | :--- | :--- |
| `docs/子模块_交互状态机_fsm_handlers.md` / `.codex/skills/allbot-tg-fsm/SKILL.md` | `src/services/main_bot_menu_config_service.py`、`src/services/main_bot_menu_runtime.py`、`src/i18n/keyboards.py`、Dashboard main Bot menu API/Vue、focused tests | 已修正 | 2026-07-17 新增主 Bot Reply Keyboard 运行时配置：`main_bot_menu_config:v1` 保存主菜单排序、每行 1–4 个及主/二级显隐，Dashboard 提供认证管理页；所有新发菜单按次读取并在失败时回退默认，二级返回固定可见，既有能力闸门继续优先。显隐只改变菜单展示，不移除 route/FSM/旧文本入口；无数据库 migration，未执行远端部署或运行态探测。 |
| `docs/子模块_交互状态机_fsm_handlers.md` | `src/handlers`、FSM tests、`src/handlers/menu_route_registry.py`、`src/services/telegram_runtime_bootstrap.py`、`src/services/fsm_temp_file_service.py`、`src/services/quick_image_submission_service.py`、`src/services/quick_video_submission_service.py`、`src/services/advanced_video_submission_service.py`、`src/services/advanced_video_settings_view_service.py`、`src/services/ltx_video_extension_service.py`、`src/services/wan22_video_v2_extension_service.py`、`src/bot_main.py`、`qqcc_bot/main.py` | 已修正 | 主 Bot FSM 边界有效；2026-07-05 同步主 Bot 菜单去重：`懒人bot` 跳转 QQCC、`图片换脸` 只保留快速/随机换脸，QQCC 动态场景 callback 保持；2026-07-05 补充菜单反向路由 registry 分区与主 Bot/QQCC 共享 Telegram runtime bootstrap，注册边界不变；2026-07-06 同步主 Bot 旧懒人入口清退：旧 `menu.video_edit` / `menu.video_edit_*`、`AI绘图` / `AI动图` / `快速换脸`、快速脱衣/自慰和主 Bot `qvid_*` 残留入口回复 QQCC 懒人 Bot inline 跳转或入口未配置提示，不提交任务；2026-07-06 补充 Quick Video 提交计划 service seam，FSM 只保留 Telegram 状态/额度/回复/清理；2026-07-06 补充 Quick Image 提交计划 service，随机换脸模板过滤、QQCC AI绘图后处理链成本与 payload 从 FSM 下沉，并清理旧 `WAIT_UNDRESS_METHOD`；2026-07-06 补充高级视频提交 service seam，旧图生视频/Wan22 v2/LTX 的分辨率/时长归一、首尾帧 payload、LTX LoRA 多选和扩展链上下文从 FSM 下沉；2026-07-06 补充 LTX 扩展/拼接回调 service seam，历史校验、context 合并、尾帧下载、扩展 FSM seed 和完整链 histories 加载从 handler 下沉；2026-07-06 补充高级视频 settings view service，旧图生视频/Wan22 v2/LTX 面板按钮、费用展示和 LTX 扩展提示从 FSM 下沉；2026-07-06 补充 Wan22 AIO 链路扩展/重生成/拼接准备 service seam，覆盖旧图生视频与 v2，历史校验、context 合并、上一段尾帧/当前段输入图下载、FSM seed 和完整链 histories 加载从 handler/callback 下沉；2026-07-06 本轮补充 `random_faceswap_again` 复用 quick image submission service、Quick Video 设置 callback 归一进入 quick video submission service、高级视频 settings callback data 解析进入 advanced video settings view service、LTX 提交显式传递 `resolution`/`duration`/`ltx_mode` 并移除顶层 `user_data` 参数桥；2026-07-06 本轮同步主 Bot/QQCC Bot 关闭 PTB FSM 并发、`cleanup_fsm_user_data(...)` 全局临时文件清理、旧 `face_video_fsm.py` 删除且视频换脸入口由 SCAIL-2 接管、LTX Bot 层移除 `v2v_audio` callback/`WAIT_VIDEO`/视频上传状态、callback manifest 启动自检 |
| `docs/子模块_交互状态机_fsm_handlers.md` | `src/handlers/fsm/quick_image_fsm.py`、`src/handlers/fsm/quick_draw_callback_data.py`、`src/handlers/menu_route_registry.py`、`src/handlers/message_handler.py`、`qqcc_bot/prompt_handlers.py` | 已修正 | 2026-07-11 同步 QQCC AI滤镜 FSM 路由：主 Bot 旧 `AI滤镜` 文本只跳转 QQCC；QQCC 保留 `qqcc.menu.ai_filter` 与 `qfilter_scene:*`，复用 quick image FSM 单图上传；Quick Image submission service 统一处理 `scene_kind=draw\|filter`、滤镜终止后处理链和重生成 metadata |
| `docs/子模块_交互状态机_fsm_handlers.md` | `qqcc_bot/private_bot_fsm.py`、private worker Application factory | 已修正 | 2026-07-12 补充官方 QQCC token 接收消息先删除、无审核自动接入、一人一绑定、私有实例不注册申请入口，以及同 Bot 顺序 update 的 FSM 边界 |
| `docs/子模块_交互状态机_fsm_handlers.md` | `src/services/telegram_update_processor.py`、`src/bot_main.py`、`src/handlers/callback_handler.py`、Update Processor focused tests | 已修正 | 2026-07-12 修复主 Bot 自 2026-06-05 入口重构后回落到 PTB `SimpleUpdateProcessor(1)` 的全局队头阻塞：主 Bot 改为同用户严格串行、不同用户默认 32 路有界并发，poll interval 降为 0，记录 queue wait/handler duration，并在全局 callback 用户同步前安全应答；QQCC 官方 Bot 串行边界不变，未执行远端部署或运行态探测 |
| `docs/子模块_QQCC懒人Bot_qqcc_lazy_bot.md` | QQCC Bot/Config runtime、release policy、`scripts/release.py`、cloud compose QQCC profile | 已修正 | 既有 QQCC 菜单、场景、Gallery、Config Web 与 single-polling 边界有效；2026-07-16 将 test/prod 发布统一到 immutable `release.py` 模块入口，legacy 专用脚本、rsync、现场 build 和手工 compose 全部退出当前 SOP，正式维护模式遵循逐次选择且默认开启 |
| `docs/子模块_QQCC懒人Bot_qqcc_lazy_bot.md` | `src/services/qqcc_config_service.py`、`qqcc_bot/keyboards.py`、`qqcc_bot/prompt_handlers.py`、`src/handlers/fsm/quick_image_fsm.py`、`src/services/qqcc_draw_chain_service.py`、`src/services/quick_image_submission_service.py`、`dashboard/frontend/src/components/QqccBotSettings.vue` | 已修正 | 2026-07-11 同步 QQCC AI滤镜：新增 `main_buttons.ai_filter` 与默认空 `filter_scenes`，无场景时主菜单不展示；新增 `qqcc.menu.ai_filter`、`system.ai_filter_hint` 和 `qfilter_scene:*`；AI绘图新增 `postprocess_filter_scene_id`，与 `postprocess_draw_scene_id` 互斥且作为终止后处理；直接滤镜复用 quick image 链路，直接入口受 `ai_filter` 开关控制，被 AI绘图引用不受该开关影响 |
| `docs/子模块_QQCC懒人Bot_qqcc_lazy_bot.md` | `src/services/quick_image_submission_service.py`、`src/services/quick_video_submission_service.py`、`src/services/qqcc_regenerate_metadata.py`、`src/services/qqcc_regeneration_service.py`、`src/services/tg_task_result_presentation.py`、`src/services/task_service_completion.py`、`qqcc_bot/regeneration_callback.py`、`qqcc_bot/callback_handler.py` | 已修正 | 2026-07-11 同步 QQCC 生成结果发布与重生成边界：快速换脸、AI绘图和 AI动图最终可见结果均 `allow_contribute=false`，新结果不展示投稿/公开按钮，旧 `submit_gallery_*` / `public_share*` 由 QQCC callback 入口拒绝；最终结果完成文案使用 QQCC 实际功能名/场景名，结果 metadata 通过 `_qqcc_regenerate` 写入 History 并展示 `重新生成` 按钮；QQCC 重生成从本人历史下载原始输入、按当前配置重建 quick image/video 提交计划并重新做额度检查；`修仙市集` 浏览、点赞和一键应用保留 |
| `docs/子模块_QQCC懒人Bot_qqcc_lazy_bot.md` | `src/services/qqcc_draw_chain_service.py`、`src/services/quick_image_submission_service.py`、`src/services/quick_video_submission_service.py`、`src/services/task_service_flow.py`、`src/core/task_core_runtime.py` | 已修正 | 2026-07-11 同步 QQCC 链式任务取消与排队策略：链路第一个真实子任务普通排队并 pending 可取消；后续绘图/内部换脸/尾帧步骤和最终视频作为 continuation，`base_priority=100`、不展示取消按钮、`user_cancel_allowed=false`，用户取消返回 `not_cancellable` |
| `docs/子模块_QQCC懒人Bot_qqcc_lazy_bot.md` / `.codex/skills/allbot-qqcc-lazy-bot/SKILL.md` | `src/services/qqcc_demo_generation_service.py`、`src/services/qqcc_config_service.py`、`src/services/qqcc_demo_media_service.py`、`dashboard/backend/routers/qqcc.py`、`dashboard/backend/routers/private_bots.py`、`dashboard/frontend/src/components/QqccBotSettings.vue` | 已修正 | 2026-07-13 新增场景示范生成：上传 input 后按场景草稿参数直接向 Central 提交，前端提交后轮询终态，图片/视频结果写入唯一 generated output 草稿 key 且不覆盖当前生效媒体；必须手动保存配置才引用新 key。该预览链不扣费、不占用户并发、不写 History，私有 Bot 严格使用租户 demo namespace；操作列扩宽，输入/输出图例放大并左对齐。同日修复 Config Backend 与旧 QQCC Bot key allowlist 版本裂缝：Bot file_id 缓存回写不得删除 generated output；R2 现存产物可按 scene ID 恢复，无需重新生成。正式单服务更新后 Bot 加载 34 个 input+output 场景且 34 个对象均存在 |
| `docs/子模块_QQCC用户私有Bot平台_qqcc_private_bot_platform.md` | private Bot schema/migration、credentials/lifecycle、owner auth/API、webhook queue/worker、membership checker、metrics、Vue/Nginx、cloud compose、submission ledger/recovery | 新增/已上线 | 同步一人一 Bot、无审核自动开通、AES-GCM/HMAC、独立配置、owner/admin 状态优先级、同 Bot ID 在 active task 存在时可救援轮换、一次性 ticket、owner 独立 limiter/CSP/双层 Host 隔离、官方 QQCC membership checker、stream worker 三层有界背压与 startup PEL catch-up、admin metrics、gate 条件 validator；补充 owner fence、debit 同事务 marker、统一退款/并发 key、TaskRegistry 字段自动兼容旧 unkeyed 任务且无需人工 drain、复合索引与默认 90 天/最少 30 天的有界安全终态 retention；worker 按 idle TTL 回收无 queue/后台任务的 Application，并由 pending sweeper 周期执行 active task/continuation/ledger retention。2026-07-12 云正式 gate/migration/profile/webhook/owner Host 已启用，并修复 Python 3.10 `asyncio.TimeoutError` 周期循环兼容；真实 Redis/PostgreSQL/PTB/Telegram HTTP 的 500 租户与 webhook burst 压测仍未执行，内存 fake 不算容量验收 |
| `docs/子模块_QQCC懒人Bot_qqcc_lazy_bot.md` / `docs/子模块_QQCC用户私有Bot平台_qqcc_private_bot_platform.md` / `.codex/skills/allbot-qqcc-lazy-bot/SKILL.md` | `src/services/qqcc_config_service.py`、`qqcc_bot/keyboards.py`、`qqcc_bot/private_bot_fsm.py`、`dashboard/frontend/src/components/QqccBotSettings.vue` | 已修正 | 2026-07-15 新增 `main_buttons.private_bot` 官方 QQCC 独立菜单开关：默认开启且不跟随生成全局开关；关闭只隐藏/拒绝新申请入口，不停止 private worker 或既有私有 Bot。旧 reply keyboard 与等待 token 的会话均 fail closed，token 消息仍先尽力删除；私有 Application 继续强制隐藏入口，部署总 gate 继续叠加生效 |
| `docs/子模块_QQCC懒人Bot_qqcc_lazy_bot.md` / `.codex/skills/allbot-qqcc-lazy-bot/SKILL.md` | `src/services/qqcc_config_service.py`、`qqcc_bot/keyboards.py`、`dashboard/backend/schemas.py`、`dashboard/frontend/src/components/QqccBotSettings.vue`、QQCC/Config/Private focused tests | 已修正 | 2026-07-17 新增 `main_menu_layout`：`buttons_per_row=null` 精确保持旧固定分行，`1..4` 在先过滤开关、gate、场景和官方/私有上下文后按 `button_order` 统一分行；未知/重复/旧 key 被丢弃、缺失 key 自动补齐，隐藏按钮保留排序位置。官方 checkpoint 与私有 Bot JSON 独立持久化，配置 Web 只在选择统一列数后解锁上移/下移；无数据库 migration、无路由或二级 inline 菜单语义变化 |
| `docs/子模块_付费群审核Bot_paid_group_guard_bot.md` | `paid_group_guard_bot`、Dashboard paid group router/service、release policy、`scripts/release.py`、cloud compose | 已修正 | 独立 Bot 与 Dashboard 配置管理边界有效；2026-07-16 正式发布改用 immutable release 模块入口，并继承逐次维护选择、默认开启与 planner fail-closed 规则 |
| `docs/子模块_Telegram本地API与文件代理_tg_local_api.md` | Telegram API env、`src/services/telegram_runtime_bootstrap.py`、`src/bot_main.py`、`qqcc_bot/main.py` | 已修正 | 文件代理边界有效；2026-07-05 同步主 Bot 与 QQCC Bot 共用 runtime helper，统一 Local Bot API URL、HTTPXRequest、File/Poll patch 与语言注入 |

## 5. Web、商业化、社区与后台

| 文档 | 事实源 | 本轮状态 | 处理结果 |
| :--- | :--- | :--- | :--- |
| `docs/子模块_用户认证与权限_user_auth_permission.md` | `src/core/auth_core*`、`src/web_api/core/security.py` | 已核对 | JWT、password_version、权限复核口径有效 |
| `docs/子模块_计费与支付_billing_payment.md` | `src/core/billing_core*`、`src/payment_api_server.py`、`src/services/permission_growth_channel_service.py`、`src/quota.py`、`src/web_api/services/user_credit_ledger_service.py`、affiliate migrations | 已修正 | 支付履约与 affiliate 账本口径有效；标准邀请仅允许本次真实新建用户，历史用户不得按 `invited_by` 空值补绑；Web 个人中心账本保持只读本人非 0 `user_logs` 与白名单展示边界 |
| `docs/子模块_社区与存储_gallery_storage.md` | `src/core/gallery*`、`src/web_api/services/*gallery*`、`dashboard/backend/services/gallery_admin_service.py`、`dashboard/frontend/src/components/GalleryReportsTable.vue`、`src/services/gallery_browse_service.py`、`src/services/gallery_apply_context_presenter.py`、`qqcc_bot/gallery_market.py`、`qqcc_bot/gallery_market_apply.py`、R2 scripts | 已修正 | R2/legacy MinIO 退出口径有效；补充 Web 好友搜索、QQCC 修仙市集轻量 Bot 浏览与 Telegram file_id 缓存口径；2026-07-04 同步 `gallery_reports` 举报治理：详情页举报、重复举报 `409`、Dashboard 举报筛选/标记处理/联动下架、`GalleryPost.is_active` 与 `History.is_public` 同步；保留 LTX/SCAIL-2/Wan22/i2i_draw apply-context 禁用与回填口径；2026-07-06 本轮同步 apply-context presenter shared seam、QQCC market 分层和原生 Bot apply 临时文件清理/不预增 apply count 策略；2026-07-18 同步举报媒体弹窗预览、举报页复用用户级“封禁并下架”、作者 pending 举报同事务处理与 `resolved_reports` 输出 |
| `docs/子模块_后台监控与清理_dashboard_monitoring.md` | `dashboard/backend`、`dashboard/frontend`、RunPod admin/autoscaler services | 已更新 | Dashboard 监控和清理边界有效；同步 `/api/system/status` 低信任免费层 pending 用户数/任务数、非低信任最长等待、高质量邀请者豁免、RunPod profile `non_low_trust_clear_pending_count*` 前缀统计、`pornmaster_flux2_edit` 正式 RunPod profile 展示、手动新增入口与 Dashboard autoscaler 自动 add/down 纳管口径；2026-07-14 补充 pending 采样按 `WORKER_REDIS_URL`、`DASHBOARD_PENDING_QUEUE_FALLBACK_REDIS_URL`、`REDIS_URL` 只读兜底，避免 Central 队列误落通用 Redis 时管理后台排队数显示为 0；2026-07-15 将 `pornmaster_flux2_edit_bf16` v3 纳入自动 add/down/restart/enable，默认 30 秒/单、30 分钟清空阈值；2026-07-17 “可接单服务器”优先复用 Central 执行池压力快照的健康 enabled RunPod/本地计数，旧响应按相同规则回退 |
| `docs/子模块_本地数据分析平台_local_analytics_platform.md` | `local_analytics_platform`、`local_analytics_platform/app/auth.py`、`local_analytics_platform/app/user_profile_analytics.py`、`local_analytics_platform/app/user_profile_snapshots.py`、`local_analytics_platform/app/prompt_vectors.py`、`local_analytics_platform/app/prompt_token_rules.py`、`local_analytics_platform/app/prompt_template_candidates.py`、`local_analytics_platform/app/refresh_prompt_template_candidates.py`、`scripts/run_local_analytics_shadow_pipeline.py`、`scripts/cleanup_local_analytics_prompt_derivatives.py`、`bot_db_prod_shadow`、本地 compose、Cloudflare token 文件元数据、`analytics.aivison.it.com` Access/Tunnel 验证 | 已更新 | 记录独立本地分析平台入口、只读 shadow 数据边界、可选应用层登录和 Cloudflare Access 公网保护边界、核心四 Tab ECharts 可视化与新增对比 API、用户画像固定看板（含从未活跃/沉睡用户 KPI 与人群规模趋势）/人群透视/用户宽表/单用户详情抽屉、`GET /api/user-analytics` 的 `visualizations`、`analytics_user_profile_daily_snapshots` 快照趋势、`GET /api/user-analytics/groups`、`GET /api/user-analytics/users`、`GET /api/user-analytics/users/{user_id}`、用户画像开始/结束日期范围、用户宽表按日期范围收敛画像信号用户池、人群透视继承下钻用户列表的日期/搜索和分层范围且不再独立预筛、旧用户增长/旧分布/排行榜前端退出、低信任免费层含高质量邀请者豁免、豁免低信任用户、低信任用户邀请价值/真实充值率概览/按邀请人平均受邀充值率/邀请转化/受邀充值/affiliate 返佣账本聚合、每日 shadow 后只保留 Prompt Mart、提示词瘦身、词元映射规则、指定词元规则、词元删除规则、词元抽取缓存、词元统计/词元提示词映射、embedding/state 与用户画像快照白名单表、05:45 自动链路先刷新用户画像快照再进入 Prompt/向量链、Mart 增量刷新、瘦身、`token-v4-available-task-scope-cjk-lexeme` 中文词汇/短语词元提取、带分类/子分类元数据的指定词元表和词元同义映射表、分类 v2 合并旧表情/场景/成人主题/身体/动作分类、`技术参数`/任务流程词软删除、`原图背景` 归 `背景保持`、`木瓜奶` 归胸部短语、人数/性别/族裔词元补强、`观测高频词` 不再自动生成有效词元、`POST /api/prompt-token-rules/overwrite-generated` 一键生成并覆盖规则、长词元拆解覆盖报告、标准中文代表词映射、raw tokens 抽取缓存、`analytics_prompt_token_scope_summary` scope 候选数摘要、tokens-only 指定词元匹配器与阶段耗时/逐索引耗时返回、tokens-only 最小索引白名单与重复/未使用大索引退役、`GET /api/prompt-tokens?include_filters=false` 翻页快路径、规则表和删除表异步自动加载、三张规则表 25 行分页渲染，指定词元表按分类一级页签与子分类二级页签筛选且表格去掉分类/子分类列，映射表继续按分类页签切换、详情 total 复用 stats `prompt_count`、默认 `min_prompt_count=5` 低频展示阈值、当前可用任务分类/无可用任务兜底与附加模型二级 scope 统计、模板候选 v1 只读物化表 `analytics_prompt_template_candidates`/`analytics_prompt_template_candidate_prompts`/`analytics_prompt_template_state`、`python -m app.refresh_prompt_template_candidates` 刷新入口、`GET /api/prompt-template-candidates` / `GET /api/prompt-template-candidates/{template_key}/prompts` / `POST /api/prompt-template-candidates/refresh`、任务类别和附加模型 scope 下的分类 v2 词元槽位组合候选、向量断点续跑链路、提示词瘦身、提示词向量化基础状态、独立提示词词元 Tab 与 `GET /api/prompt-tokens` / `GET /api/prompt-token-prompts` 分类下钻、近似代表/近似图/语义场景/语义图谱 Tab/API/刷新入口下线、旧派生表 dry-run 默认清理脚本、媒体引用核验与不挂载现有 Dashboard 的运行口径；补充慢 Tab 主接口限流并发查询、提示词页移除完整 `/api/generation` 预取、`LOCAL_ANALYTICS_DB_POOL_MAX_SIZE` 默认 5 的性能保护口径、本地分析公网入口已通过独立 tunnel、Cloudflare Access 邮箱 allowlist 和应用登录双层保护上线；Cloudflare 账号、token、DNS、Access 与 Tunnel SOP 已拆到 `docs/子模块_Cloudflare公网入口与账号管理_cloudflare_ops.md` |
| `docs/子模块_本地数据分析平台_local_analytics_platform.md` | `local_analytics_platform/app/routes_prompts.py`、`local_analytics_platform/app/prompt_token_rules.py`、`bot_db_prod_shadow` 规则表/API 复核 | 已更新 | 2026-07-08 补充词元分类 v2 残留清理：规则表 API 只返回 enabled 指定词元和映射，旧 `观测高频词/技术参数/生成编辑` disabled 历史行不再生成分类页签；旧三类残留已按语义迁移到新分类或写入软删除，明确英文入口如 `bed/chest/doggy/cowgirl/female/bob/ahegao` 归一到中文代表词，`床` 等明确单字中文允许作为 curated seed |
| `docs/子模块_本地数据分析平台_local_analytics_platform.md` | `local_analytics_platform/app/prompt_token_rules.py`、`bot_db_prod_shadow.analytics_prompt_token_custom_terms`、`bot_db_prod_shadow.analytics_prompt_token_alias_rules`、`bot_db_prod_shadow.analytics_prompt_token_deleted_rules`、`/tmp/prompt_token_tag_value_cleanup_20260708_165427/`、模板候选刷新输出 | 已更新 | 2026-07-09 补充词元“标签价值”清理口径：自动种子过滤 `背景/人物/头发/镜头/角度/比例/特征/skin/hair/angle` 等过宽标签，`水印/文字/裁剪/放大/修改/速度/少指/多指/单反/焦距/闪光灯` 等流程/技术/错误质量词进入软删除；长句式词元软删原句并补基础入口，如 `腿套/叉腰/密室/储藏室/洗手间/猫耳朵/抓拍/瓜子脸`；`戏服/凤冠/无套灌精` 改入正确分类。本轮治理软删除 128 个词、禁用 68 组低价值代表映射，刷新模板候选后物化 16,359 个模板候选和 147,081 条候选提示词明细 |
| `docs/子模块_本地数据分析平台_local_analytics_platform.md` | `local_analytics_platform/app/prompt_token_rules.py`、`bot_db_prod_shadow.analytics_prompt_token_custom_terms`、`bot_db_prod_shadow.analytics_prompt_token_alias_rules`、`bot_db_prod_shadow.analytics_prompt_token_deleted_rules`、`/tmp/prompt_token_mapping_sync_20260708_175159/`、tokens-only/模板候选刷新输出 | 已更新 | 2026-07-09 补充指定词元删除/移动后的映射同步口径：`女性/女人/男性/男人/面部/脸部/表情/身体/衣服/高清/画质/清晰/细节/光影` 及中英文别名等低标签价值代表词禁用并写入删除表，39 个保留代表词的映射分类同步指定词元分类；本轮禁用 17 组宽泛代表映射、46 个指定词元行 disabled、103 个词/别名写入删除表，tokens-only 后物化 182,963 个词元和 549,703 条明细，模板候选刷新后物化 17,730 个模板和 167,309 条明细 |
| `docs/子模块_本地数据分析平台_local_analytics_platform.md` | `local_analytics_platform/app/prompt_token_rules.py`、`bot_db_prod_shadow.analytics_prompt_token_custom_terms`、`bot_db_prod_shadow.analytics_prompt_token_alias_rules`、`/tmp/prompt_token_demographic_apply_20260709_033841/`、tokens-only/模板候选刷新输出 | 已更新 | 2026-07-09 补充人群/繁简/多语言遗漏词元治理：新增年龄、年龄性别、数字年龄、族裔国别、族裔年龄性别发现标签；`陰茎/陰莖/陰道/台灣/臺灣/亞洲/東亞/韓國` 等归一到标准中文代表词；`年轻女生/年輕女生/年轻女性`、`亚洲女生/亚洲女性`、`台湾男生/台湾男性` 等称谓同义口径合并，`亚洲小男孩/黑人男孩` 等组合人群保留独立标签 |
| `docs/子模块_本地数据分析平台_local_analytics_platform.md` | `local_analytics_platform/app/prompt_vectors.py`、`local_analytics_platform/app/prompt_token_rules.py`、`bot_db_prod_shadow.analytics_prompt_token_custom_terms`、`bot_db_prod_shadow.analytics_prompt_token_alias_rules`、`bot_db_prod_shadow.analytics_prompt_token_deleted_rules`、`/tmp/prompt_token_attribute_cleanup_20260709_043756/`、tokens-only/模板候选刷新输出 | 已更新 | 2026-07-09 补充词元属性拆分和碎片清理：普通核心部位标签保留，胸部尺寸、阴毛密度、阴茎尺寸/颜色拆成独立代表词；`门/双腿/雙腿/便器/字腿/型腿/腿成/度弯腰` 等低价值碎片软删除，`m 字腿/腿成m形/字開腳` 派生为 `m字开腿`，`雙腿大幅打開/雙腿張開` 归 `双腿分开` |
| `docs/子模块_本地数据分析平台_local_analytics_platform.md` | `local_analytics_platform/app/prompt_vectors.py`、`local_analytics_platform/app/prompt_token_rules.py`、`bot_db_prod_shadow.analytics_prompt_token_alias_rules`、`bot_db_prod_shadow.analytics_prompt_token_deleted_rules`、`/tmp/prompt_token_m_leg_audit_20260709_132834/`、tokens-only/模板候选刷新输出 | 已更新 | 2026-07-09 二次审计 `M/m + 腿` 相关词元：抽样确认 `腿呈/雙腿呈/张开双腿成/字型腿` 等来自 `M字开腿` 或低价值缺前缀碎片，升级 `token-v6-available-task-scope-cjk-lexeme-derived`，补充 `m型开腿/m形开腿/腿呈m形/雙腿呈m字型` 派生抽取和软删除兜底；v6 重建后 `m字开腿` 归并 48,716 条提示词，精确碎片前端可见数为 0，模板候选刷新到 19,740 个 |
| `docs/子模块_本地数据分析平台提示词词义分析_prompt_semantics.md` / `.codex/skills/allbot-local-analytics-prompt-semantics/SKILL.md` | 本轮提示词词元治理现场、`local_analytics_platform/app/prompt_vectors.py`、`local_analytics_platform/app/prompt_token_rules.py`、`analytics_prompt_token_*` 规则/物化表、tokens-only 输出、模板候选口径 | 新增 | 新增本地分析提示词词义分析专项文档与 Skill：沉淀指定词元、同义映射、删除表、raw token cache、低频阈值、tokens-only 物化、年龄人群发现标签、基础部位与属性拆分、长词元拆解、模板候选槽位和写库前后校验原则；同步 `AGENTS.md` 与 `docs/skills/README.md` 路由入口 |
| `docs/子模块_本地数据分析平台_local_analytics_platform.md` / `docs/子模块_本地数据分析平台提示词词义分析_prompt_semantics.md` / `.codex/skills/allbot-local-analytics-prompt-semantics/SKILL.md` | `local_analytics_platform/app/routes_prompts.py`、`local_analytics_platform/app/prompt_decomposition.py`、`local_analytics_platform/static/index.html`、`local_analytics_platform/static/js/bootstrap.js`、`analytics_prompt_decomposition_saved_templates`、Playwright 本地截图验收 | 已更新 | 2026-07-09 补充“自由P图拆解”Tab：固定 `edit` scope、只展示 `prompt_count >= 20` 的已分类标签，按 `保持口径/场景/物品/表情/成人主题/动作姿势/画面风格构图/身体细节/外观特征/服饰配件/人物主体` 组织一级筛选；新增优秀模板沉淀表和 `GET/POST/DELETE /api/prompt-decomposition*`，并修正保存接口把 ISO `last_seen` 归一为 `datetime` 后再写库 |
| `docs/子模块_本地数据分析平台_local_analytics_platform.md` / `.codex/skills/allbot-local-analytics-prompt-semantics/SKILL.md` | `local_analytics_platform/app/prompt_template_candidates.py`、`local_analytics_platform/app/routes_prompts.py`、`local_analytics_platform/static/index.html`、`local_analytics_platform/static/js/bootstrap.js`、`tests/local_analytics/test_prompt_template_candidates.py`、`tests/local_analytics/test_visualization_static_assets.py` | 已更新 | 2026-07-10 补充模板候选相似度筛选与审核暂存：候选表物化 `similarity_bucket/similarity_score/similarity_metrics`，新增 `analytics_prompt_template_candidate_review_marks` 保存候选 prompt 暂存快照；列表支持四档相似度与已处理/未处理筛选，详情接口返回 `review_checked/review_marked_at`，前端抽屉可逐条勾选或取消暂存。暂存仅用于本地审核状态，不写入优秀模板沉淀表、不改原始 prompt、词元规则或生产生成逻辑 |
| `docs/子模块_本地数据分析平台_local_analytics_platform.md` / `.codex/skills/allbot-local-analytics-prompt-semantics/SKILL.md` | `local_analytics_platform/app/routes_prompts.py`、`local_analytics_platform/static/index.html`、`local_analytics_platform/static/js/bootstrap.js`、`local_analytics_platform/static/js/state.js`、`tests/local_analytics/test_prompt_template_candidates.py`、`tests/local_analytics/test_visualization_static_assets.py` | 已更新 | 2026-07-10 补充模板候选“暂存汇总”入口：新增 `GET /api/prompt-template-candidates/review-marks`，按任务、附加模型、相似度和关键词汇总已暂存候选 prompt；前端模板候选筛选栏新增“暂存汇总”按钮和右侧抽屉，支持逐条复制、复制全部和 CSV 导出。该入口只读取审核暂存表，不写入优秀模板沉淀表或生产提示词链路 |
| `docs/子模块_本地数据分析平台_local_analytics_platform.md` / `.codex/skills/allbot-local-analytics-prompt-semantics/SKILL.md` | `local_analytics_platform/app/prompt_template_candidates.py`、`local_analytics_platform/app/routes_prompts.py`、`local_analytics_platform/static/index.html`、`local_analytics_platform/static/js/bootstrap.js`、`local_analytics_platform/static/js/state.js`、`tests/local_analytics/test_prompt_template_candidates.py`、`tests/local_analytics/test_visualization_static_assets.py` | 已更新 | 2026-07-10 暂存汇总升级为分页审核流：暂存表增加 `review_processed/review_processed_at` 保存单条暂存 prompt 的已处理状态，`GET /api/prompt-template-candidates/review-marks` 新增 `processed_status` 筛选并默认小页加载，新增 `POST /api/prompt-template-candidates/review-marks/processed` 切换行级处理状态；前端汇总抽屉增加处理状态筛选、分页控件和行级“已处理”勾选，复制/导出限定当前页 |
| `docs/子模块_本地数据分析平台_local_analytics_platform.md` / `.codex/skills/allbot-local-analytics-prompt-semantics/SKILL.md` | `local_analytics_platform/app/prompt_template_candidates.py`、`local_analytics_platform/app/routes_prompts.py`、`local_analytics_platform/static/index.html`、`local_analytics_platform/static/js/bootstrap.js`、`local_analytics_platform/static/js/state.js`、`local_analytics_platform/static/styles.css`、`tests/local_analytics/test_prompt_template_candidates.py`、`tests/local_analytics/test_visualization_static_assets.py` | 已更新 | 2026-07-10 模板候选新增模板级低质量审核状态：新增 `analytics_prompt_template_candidate_template_review_marks`，`GET /api/prompt-template-candidates` 返回 `low_quality/low_quality_marked_at` 并把 `processed` 调整为“有暂存 prompt 或低质量”，新增 `review_status=low_quality` 和 `POST /api/prompt-template-candidates/template-review-marks`；前端操作列新增紧凑低质量 checkbox 与低质量 badge。该状态仅写本地分析审核表，不修改原始 prompt、词元规则、模板 key 或生产生成逻辑 |
| `docs/business/00_INDEX_业务板块分类与规范总览.md` | business docs | 已核对 | 业务导航有效 |
| `docs/business/00_DICT_全局业务数据字典.md` | models、domain config、business docs | 已修正 | 补充 `GalleryReport` 字典项，记录举报原因枚举、处理状态、快照字段与 `reporter_user_id + post_id` 唯一约束 |
| `docs/business/01_BIZ_AI创作与生成板块.md` | task type registry、FSM、Web task routes | 已核对 | 生成业务描述有效 |
| `docs/business/02_BIZ_商业化与会员资产板块.md` | billing/auth/affiliate code、`src/web_api/services/user_credit_ledger_service.py` | 已修正 | 商业化描述有效；补充 Web 用户侧灵石账本只读本人流水查询 |
| `docs/business/03_BIZ_社区广场与社交互动板块.md` | Gallery models/services | 已修正 | 同步 Gallery 提示词解锁不再按低信任免费层拦截；2026-07-04 补充修仙市集详情举报、原因枚举、重复举报冲突、Dashboard 举报管理与联动软下架同步语义 |
| `docs/business/04_BIZ_用户修为与身份权限体系.md` | User model、auth core、permission service、billing core | 已修正 | 保留低信任免费层实时标记、高质量邀请者豁免、非低信任用户 `+40` 队列加成，并同步 Gallery 提示词解锁限制退出；2026-07-17 移除总队列 `>300` 限制，改为外门凡人/练气按目标 Worker 池 `(pending + 1) > 50 × max(accepting_workers, 1)` 拒绝，筑基/内门以上豁免，指标异常 fail-open |
| `docs/business/image_to_image_flow.md` | image FSM/task routes | 已核对 | 图生图流程有效 |
| `docs/business/image_to_video_flow.md` | video FSM/task routes、Wan22/LTX/SCAIL-2 docs | 已核对 | 图生视频流程有效 |

## 6. 运维与环境

| 文档 | 事实源 | 本轮状态 | 处理结果 |
| :--- | :--- | :--- | :--- |
| `docs/SAFE_DEPLOY_GUIDE.md` | deploy scripts、cloud compose | 已修正 | 云测试日常入口改为目标 service 快速重建，维护式更新脚本只在整栈/迁移/排空/明确维护窗口时使用，`safe_deploy_cloud_test.sh` 标为子步骤 |
| `docs/子模块_Git不可变发布_git_immutable_release.md` | `scripts/release.py`、release policy、事务状态与 release bundle 契约 | 已修正 | 2026-07-16 增加正式维护模式逐次选择：默认开启，明确请求且 planner 允许时关闭；强制 maintenance 不可覆盖，发布器不能证明请求/实际模式一致时不执行 mutation。2026-07-18 增加缺失 rollback checkout/release.env 的受控物化 recover，保持运行服务、维护状态和 current/history 不变 |
| `docs/子模块_Git不可变发布_git_immutable_release.md` / `docs/子模块_云正式控制面部署_cloud_prod_control_plane.md` / `docs/domain/CONTEXT.md` | `scripts/release.py`、`deploy/release-policy.yml`、schema v2 track current/history 与 rollback contract | 已修正 | 2026-07-17 增加 Dashboard、官方 QQCC Bot、QQCC Config 三个严格独立模块组：各 artifact 使用自己的 `source_sha`，旧局部 current 缺失项从成功 history 只读恢复并允许组内混合基线，目标 SHA 的其它产物不自动扩入；migration、未知共享 Compose/env 和未审计跨模块契约仍 fail closed，精确内容 SHA256 固定的 owner-only/向后兼容 snapshot 才可例外，非目标 artifact 混合版本继续保留 |
| `docs/子模块_并发AI开发与测试列车_concurrent_ai_workspaces.md` / `docs/并发AI自动接单使用指南_auto_workspace_claim.md` | `scripts/manage_ai_workspaces.py`、main workflows、并发 Skill | 已修正 | 2026-07-19 切换为 main-first release batch：A-H 从 main 开发，推送后 handoff 冻结远端 head 并立即释放；`batch-plan` 固定多个交接成员，只建立一个 main PR。PR 不发布容器，main CI 成功后构建一次 main bundle，用户要求时再部署唯一测试站；旧 train/candidate 仅历史兼容 |
| `docs/子模块_运维指南与容器管理_ops_deployment.md` | deploy scripts、compose、ops Skill | 已修正 | 补充正式 QQCC Bot 单服务更新入口；同步云测试快速单模块重建为日常默认，维护式脚本仅用于整栈/迁移/排空/明确维护窗口；2026-07-16 明确正式维护模式逐次选择、默认开启，只有用户当次明确要求且 planner 允许时关闭，模式无法被发布器证明则 fail closed |
| `docs/子模块_云测试控制面部署_cloud_test_control_plane.md` | immutable compose、`scripts/release.py`、main-channel release index、cloud test workers、R2 CORS 实测、QQCC Config 测试 Tunnel/Access 实测 | 已修正 | QQCC Config 前后端保持 standard 测试发布，测试 overlay 固定 8045/8088；Dashboard 仍不进入测试站。2026-07-19 起改为从已构建 main-channel bundle 按需部署，不再经 test-train；`qqcc-admin-test.aivison.it.com` 的 Access/应用层登录与 digest/revision 验收契约不变 |
| `docs/子模块_云正式控制面部署_cloud_prod_control_plane.md` | `deploy/docker-compose-cloud-prod.yml`、`deploy/release-policy.yml`、Dashboard 两条 Dockerfile 构建路径、`scripts/release.py`、legacy release shells、shadow/analytics scripts、systemd timer、Dashboard autoscaler service、LAN AIO prod helpers、Cloudflare Pages build logs | 已修正 | 保留现有云正式、shadow、autoscaler、LAN/RunPod 与 Pages 口径；2026-07-16 补充逐次维护模式选择、Dashboard 手动/autoscaler RunPod operation 的 catalog pin，以及两条 Dashboard Backend 镜像构建路径都必须携带 LAN AIO rollout 脚本导入闭包的契约；禁止陈旧 `/app/.env` ref 覆盖并要求目标镜像先完成入口/revision smoke |
| `docs/子模块_云正式控制面部署_cloud_prod_control_plane.md` / `docs/子模块_运维指南与容器管理_ops_deployment.md` | `deploy/docker/Dockerfile.control-plane`、`deploy/docker-compose-cloud-base.yml`、`.github/workflows/modular-release-v2.yml` | 已修正 | 2026-07-18 固化 Web 视频缩略图的 ffmpeg 镜像依赖与最终 digest smoke；控制面全部服务统一 `json-file` 50m×5 轮转，并将已审阅内容 SHA256 同步到独立模块契约快照。`tests/ops/test_release_cli.py` 逐项核对仓库 snapshot 与当前文件内容，后续漂移继续 fail closed。构建缓存清理仍为需明确授权的正式主机 mutation，禁止连带清 image/volume/container |
| `docs/子模块_本地正式灾备切换_local_prod_fallback.md` | `safe_deploy.sh`、cloud prod scripts、shadow sync script | 已修正 | 灾备时优先核对/使用 `bot_db_prod_shadow`、`user-data-prod-shadow` 与 `user-data-complete-shadow`，本地写入前停止 shadow timer |
| `docs/子模块_Cloudflare公网入口与账号管理_cloudflare_ops.md` | Cloudflare DNS/Access/Tunnel 只读 API 探测、R2 CORS 实测、`/home/hfy/.cloudflare/allbot-cloudflare-admin.token` 文件元数据、本地分析 compose、用户级 `cloudflared-local-analytics.service`、网络/本地分析文档 | 新增/已更新 | 新增 Cloudflare 专项 SOP：记录账号/zone/team domain 非敏感事实、自动化 token 主路径与兼容链接、当前 `analytics.aivison.it.com` 与 `qqcc-admin.aivison.it.com` Access 保护入口、DNS/Tunnel/Access 验证命令、受保护公网入口新增流程、token 轮换流程、回滚与文档维护红线；2026-07-12 增加公开 `private-bot.aivison.it.com` owner Host，复用 admin Tunnel 但不创建 Access app，原 admin Access 保持不变；2026-07-14 同步 `r2-test` 公网读路径、测试桶四 Origin CORS 与凭据按目标 API 实测能力口径；2026-07-16 新增 `qqcc-admin-test.aivison.it.com` 测试 Tunnel/DNS/Access，未登录 302 且仅管理员邮箱 allow，未启用 private Owner Host；明确不记录 token 明文 |
| `docs/子模块_网络暴露与代理穿透_network_proxy.md` | Cloudflare/Tunnel scripts、network docs、本地分析 compose、Cloudflare DNS/Access/Tunnel API 探测、用户级 `cloudflared-local-analytics.service` | 已更新 | 网络入口和回滚边界有效；保留 `analytics.aivison.it.com`、`qqcc-admin-test.aivison.it.com`、`qqcc-admin.aivison.it.com` 与 Cloudflare token 路径摘要，Cloudflare 账号、Token、DNS、Access、Tunnel 细节拆分到 `docs/子模块_Cloudflare公网入口与账号管理_cloudflare_ops.md`；继续强调禁止裸露 8095、Dashboard/QQCC Config 管理端口或 shadow 数据库端口 |
| QQCC private Bot 部署/公网契约 | cloud test/prod compose、safe/update 脚本、`scripts/validate_private_qqcc_bot_env.py`、QQCC private Bot 文档、Cloudflare/网络文档 | 已更新/已上线 | 新增 `qqcc-private-bots` profile、独立 32-byte Base64URL keyring/fingerprint/owner JWT、forbidden IDs、private Telegram HTTPS/webhook/双 Host env contract；safe deploy 以 `--allow-disabled` 允许 gate 缺失/`false` 的 inactive profile 不填写 activation secrets，gate=`true` 时严格要求全部 secret、R2/HTTPS/Host 和环境对应官方 QQCC token。owner/admin/unknown Host 分别为 owner-only/admin/404，backend 再做 Host 校验；owner `connect-src 'self'`、允许 Telegram WebView且无 XFO，ticket exchange 使用独立 `50r/s`/`burst=500` limiter，admin/unknown 仍禁止嵌入。2026-07-12 云正式 gate=`true`，migration/profile/webhook 已启用，`private-bot.aivison.it.com` 已创建 proxied DNS/Tunnel ingress且不套 Access，原 admin Access 保持 302 |
| `docs/子模块_边缘节点运维指南_edge_node_ops.md` | edge docs、cloud prod preflight、Cloudflare Pages build logs | 已修正 | 边缘节点说明有效；补充 Cloudflare Pages 使用 Node 24 / npm 10.9.2 构建时的 `npm ci` 同版本验证、lockfile 刷新和 `Missing: @emnapi/runtime@1.11.1 from lock file` 排障口径 |
| `docs/子模块_云控制面SSH密钥管理_cloud_ssh_access.md` | SSH docs、cloud compose | 已核对 | 不含私钥，作为登录边界文档保留 |
| `docs/子模块_局域网GPU节点SSH管理_lan_gpu_ssh_access.md` | LAN SSH docs、GPU resource docs | 已核对 | 不含私钥，作为节点访问文档保留；2026-07-04 同步 gpu252：8192 固定健康 UUID 接 i2i_pro，8191 固定返修 UUID 只接 PornMaster Flux2 edit，SCAIL-2/Wan22 仍不得直接接正式队列 |
| `docs/子模块_局域网GPU节点资源与运维_lan_gpu_resource_ops.md` | LAN AIO scripts、Git catalog/legacy seed、XDG ledger/history、worker compose、live status | 已修正，需运行态复核 | 日常 current 与 cache 不再写长文档或 Git；2026-07-17 gpu-002 GPU1 切到 `i2i_pro`、image_to_video/PornMaster 保留为同卡回切候选的交接事实已进入冻结 legacy seed，仅供首次 `state-init`。后续容量与 current 必须先读 XDG ledger 再跑 helper status；drift、catalog revision 变化和未完成 operation 均停止 mutation，稳定 OOM/维护阻断仍通过 Git PR 管理 |
| `docs/子模块_系统资源与容量画像_resource_inventory.md` | compose、resource docs、deployment scripts、shadow sync script | 已修正 | 云正式/云测试入口口径更新；补充本地 shadow DB、R2 shadow、完整合并桶与 MinIO bucket 资源事实，并记录 shadow DB 获取路径已切为云机 dump + R2/HTTPS 中转；2026-07-04 同步 gpu252：8192 i2i_pro 固定健康 UUID，8191 返修 UUID 改为 PornMaster Flux2 edit 低负载接单，SCAIL-2/Wan22 因真实 workload Xid 119/154 保持 maintenance-disabled；运行态快照仍需人工探测 |
| `docs/子模块_容灾与持久化_database_recovery.md` | migrations、runtime checkpoint code | 已核对 | 恢复主链有效 |
| `docs/子模块_代码静态分析与质量评估规范_code_quality.md` | `pytest --collect-only`、Ruff、Radon、Symilar、Vulture、Alembic、Core import 扫描、doc checker | 已修正 | 保留历史快照，新增 2026-07-11 全仓静态复核与热点队列；报告事实源为 `logs/code_analysis_report_20260711_2140.md` |
| `docs/子模块_前端浏览器预览截图_frontend_browser_preview.md` | Playwright preview skill | 已核对 | 前端截图验收口径有效 |

## 7. 归档材料

| 文档 | 事实源 | 本轮状态 | 处理结果 |
| :--- | :--- | :--- | :--- |
| `docs/archive/2026-06-cloud-migration/README.md` | 归档索引 | 归档确认 | 已明确“不作为当前 SOP” |
| `docs/archive/2026-06-cloud-migration/正式云环境切换前准备清单.md` | 历史迁云记录 | 归档确认 | 不重写历史证据 |
| `docs/archive/2026-06-cloud-migration/子模块_Cloudflare_Pages与API_Tunnel测试入口迁移_runbook.md` | 历史 runbook | 归档确认 | 不重写历史证据 |
| `docs/archive/2026-06-cloud-migration/变更说明_web历史原视频R2优先链路.md` | 历史变更说明 | 归档确认 | 不重写历史证据 |
| `docs/archive/2026-06-cloud-migration/问题分析_web老任务恢复导致无效状态轮询.md` | 历史问题分析 | 归档确认 | 不重写历史证据 |
| `docs/archive/2026-06-runtime-canaries/README.md` | 归档索引 | 归档确认 | 已明确“不作为当前 SOP 或容量事实源” |
| `docs/archive/2026-06-runtime-canaries/LAN_AIO_PROD_CANARY_20260616.md` | 一次性 canary | 归档确认 | 不重写历史证据 |
| `docs/archive/2026-06-runtime-canaries/lan_model_cache_upload_2026-06-15.md` | 一次性模型上传记录 | 归档确认 | 不重写历史证据 |

## 8. Skills 与 Skill 附属文件

| 文件 | 事实源 | 本轮状态 | 处理结果 |
| :--- | :--- | :--- | :--- |
| `.codex/skills/allbot-concurrent-workspaces/SKILL.md` | `AGENTS.md`、自动接单/批次集成文档、ADR 0008、`scripts/manage_ai_workspaces.py`、两份 release workflow | 已修正 | A-H 保持高读取能力与秘密脱敏边界；2026-07-19 改为 origin/main 基线、推送后不可变 handoff 立即释放、多个 handoff 只形成一个 main PR。PR 不发布容器，main 合并后才构建一次 bundle；冻结后的新开发自动进入后续批次。 |
| `AGENTS.md` / `docs/adr/0006-risk-based-artifact-release-gates.md` / `docs/adr/0008-main-first-release-batches.md` / `docs/子模块_Git不可变发布_git_immutable_release.md` | `scripts/release_strategy.py`、`scripts/release.py`、v2 release manifest/workflow、用户风险授权 | 已修正 | 按 artifact 的 standard/direct/emergency 保留；2026-07-19 构建事实源收敛到单批次 main push，standard 从云测试 retained history 按 exact digest 取证，direct/waived/attested 不伪装 tested；main/CI/digest/配置/健康/事务/回滚/生产确认永久保留。 |
| `.codex/skills/allbot-kb-auto-updater/SKILL.md` | 本矩阵、KB 维护流程 | 已修正 | 补充核对矩阵输出要求 |
| `.codex/skills/allbot-ops-deployment/SKILL.md` | deploy scripts、compose、shadow sync script、Dashboard autoscaler service、LAN AIO worker 基础控制、Cloudflare ops Skill、Skill 体积审计 | 已修正 | 保留路由型运维入口与既有高压红线；2026-07-16 新增逐次正式维护模式契约，并补充 Dashboard RunPod operation 必须 pin 已验收 img2img/PornMaster baked 镜像、镜像发布/smoke 先于 Dashboard 部署的门禁 |
| `.codex/skills/allbot-ops-deployment/SKILL.md` / `.codex/skills/allbot-qqcc-lazy-bot/SKILL.md` | `scripts/release.py`、`deploy/release-policy.yml`、`tests/ops/test_release_cli.py` | 已修正 | 2026-07-17 同步独立模块发布入口：`dashboard`、`qqcc-bot`、`qqcc-config` 一次只选一个完整组，逐 artifact baseline 检查并可从 retained history 恢复旧局部状态；半组、混组、未知共享契约和 snapshot 内容漂移拒绝，非目标服务不得重建 |
| `.codex/skills/allbot-cloudflare-ops/SKILL.md` | `docs/子模块_Cloudflare公网入口与账号管理_cloudflare_ops.md`、Cloudflare DNS/Access/Tunnel 只读 API 探测、网络/本地分析文档 | 新增 | 新增 Cloudflare 运维短入口；用于 Cloudflare Account API Token、DNS、Tunnel、Access、Pages/R2、公网管理域名、本地分析与 QQCC 管理入口配置。正文只保留路由、红线、标准流程和最小验证，详细账号事实与 SOP 放在 Cloudflare 专项文档 |
| `.codex/skills/allbot-local-analytics-prompt-semantics/SKILL.md` | `docs/子模块_本地数据分析平台提示词词义分析_prompt_semantics.md`、本地分析提示词词元治理现场、`analytics_prompt_token_*` 表、tokens-only 输出 | 新增 | 新增本地分析提示词词义治理短入口；用于指定词元、映射表、删除表、高频未覆盖词元、年龄人群发现标签、词元分类和模板候选槽位治理。正文保持路由型入口，详细原则沉淀到提示词词义分析专项文档 |
| `.codex/skills/allbot-lan-aio-operator/SKILL.md` | `allbot-ops-deployment`、Git catalog、XDG ledger/history、`scripts/lan_aio_fleet_prod_ops.py` | 已更新 | Skill 改为 live/ledger/catalog 三方门禁；普通切换由 helper 原子写本地 current/history，失败/回滚也审计，只有稳定 catalog 变化才走 PR |
| `.codex/skills/allbot-lan-aio-operator/SKILL.md` / `docs/子模块_GPU算力资源池控制器_gpu_pool_controller.md` | `scripts/gpu_release_rollout.py`、RunPod/LAN operator、GPU v2 manifest | 已修正 | 2026-07-16 GPU 证据拆成强制 attestation 与可选 business canary；direct 单槽 rollout 从 release index 取 exact digest，disabled 验证失败恢复旧镜像并停止；正式 remote_workers 只使用镜像烘焙内容，不再同步/挂载仓库源码 |
| `.codex/skills/allbot-task-engine/SKILL.md` | task core、queue manager、runtime cleanup、`src/services/advanced_video_submission_service.py`、`src/services/ltx_video_extension_service.py`、`src/services/wan22_video_v2_extension_service.py`、Skill 体积审计 | 已修正 | 已从约 23KB 瘦身为约 8.4KB 的任务生命周期路由入口，保留 core/Web/Central/Worker 边界、双 ID 红线、新任务类型清单和验证要求；修正 SCAIL-2 合并长时长后的 public/history/execution 路由口径；补充 `i2i_draw` Web 入口级禁用不等于全局删除能力；2026-07-04 补充 Central Redis transient 503、入队 retry、`zpopmin` 不盲 retry 与 submit/status breaker 隔离排障入口；2026-07-06 补充 Bot 高级视频入口 payload 计划事实源：修改旧图生视频/Wan22 v2/LTX 提交字段优先改 `advanced_video_submission_service.py` 并覆盖 service focused tests；2026-07-06 补充 LTX Bot 显式参数提交事实：分辨率、时长与模式由 plan 传入 `process_ltx_video_task(...)`，不得通过 `context.user_data` 顶层键桥接；2026-07-06 补充 LTX Bot 扩展 seed 与拼接 histories 恢复事实源：优先改 `ltx_video_extension_service.py`；2026-07-06 补充 Wan22 AIO Bot 链路扩展/重生成 seed 与拼接 histories 恢复事实源：覆盖旧图生视频与 v2，优先改 `wan22_video_v2_extension_service.py`；长链路细节改由任务调度/生成全链路文档按需加载 |
| `.codex/skills/allbot-task-engine/SKILL.md` | `src/core/task_core.py`、`src/core/task_core_submission.py`、`src/core/task_core_runtime.py`、`src/services/task_service_flow.py`、`src/services/tg_task_runtime.py` | 已修正 | 2026-07-11 同步 Bot 任务控制 seam：`process_and_submit_task(...)` 可透传 `base_priority` 与 `user_cancel_allowed`；Telegram 展示层用 `allow_cancel` 控制取消按钮；core runtime 必须按 active registry 拒绝不可取消任务 |
| `.codex/skills/allbot-comfy-models/SKILL.md` | workflow patcher、remote_workers、`src/services/advanced_video_submission_service.py`、`src/services/advanced_video_settings_view_service.py`、`src/services/ltx_video_extension_service.py`、`src/services/wan22_video_v2_extension_service.py`、Skill 体积审计、QQCC config/FSM | 已修正 | 已从约 36KB 瘦身为约 7.4KB 的模型/workflow 路由入口，保留 workflow 事实源、Central/Worker 边界、Wan22/LTX/SCAIL-2 关键提醒和部署验收；修正 `scail2_action_transfer_long` 为动作迁移 10/15/20s hidden execution；补充 QQCC `AI动图` 尾帧来源只复用 AI绘图后处理链和现有首尾帧输入，不新增 workflow/profile；2026-07-06 补充主 Bot 高级视频 submission service 只做 Bot payload 事实源，不改变 workflow/profile/model dir；2026-07-06 补充 LTX Bot 扩展/拼接准备 service 不改变 workflow/profile/model dir；2026-07-06 补充高级视频 settings view service 只生成 Telegram view-model/keyboards 与费用展示，不改变 workflow/profile/model dir；2026-07-06 补充 Wan22 AIO Bot 链路扩展/重生成/拼接准备 service 覆盖旧图生视频与 v2，不改变 workflow/profile/model dir；2026-07-10 补充 QQCC `negative_prompt` 节点映射：Qwen `4.prompt`、PornMaster Flux2 `254.text`/`49.text`、Wan22 `2371.value`；节点级细节改由 Comfy 子模块文档与 runtime reference 按需加载 |
| `.codex/skills/allbot-comfy-models/SKILL.md` | `docs/子模块_附加模型配置指南_comfy_models.md`、QQCC config/FSM、`src/services/quick_image_submission_service.py` | 已修正 | 2026-07-11 同步 QQCC AI滤镜模型红线：`filter_scenes` 复用 AI绘图 engine/LoRA/negative_prompt/original_face_swap 规则，不新增 workflow/profile/model bundle；`draw_scenes[].postprocess_filter_scene_id` 只作为终止模板引用 |
| `.codex/skills/allbot-tg-fsm/SKILL.md` | `src/handlers`、Bot entrypoint、`src/handlers/menu_route_registry.py`、`src/services/telegram_runtime_bootstrap.py`、`src/services/quick_image_submission_service.py`、`src/services/quick_video_submission_service.py`、`src/services/advanced_video_submission_service.py`、`src/services/advanced_video_settings_view_service.py`、`src/services/ltx_video_extension_service.py`、`src/services/wan22_video_v2_extension_service.py` | 已修正 | FSM 边界有效；2026-07-05 同步菜单 registry 事实源、FSM 临时文件服务优先、主 Bot/QQCC 共享 Telegram runtime bootstrap 且 QQCC 注册面不扩大；2026-07-06 补充主 Bot 已迁移旧懒人入口只能跳转 QQCC 懒人 Bot 或提示入口未配置，不得提交任务；2026-07-06 补充 Quick Video 提交 service seam；2026-07-06 补充 Quick Image 提交 service seam 并清理旧脱衣方式选择态；2026-07-06 补充高级视频提交 service seam，主 Bot `image_to_video` / `wan22_video_v2` / `ltx_video` FSM 只保留交互、额度、回复和清理；2026-07-06 补充 LTX 扩展/拼接 service seam，handler 只保留 Telegram 交互与结果发送；2026-07-06 补充高级视频 settings view service seam，设置面板按钮/费用/提示从 handler 下沉；2026-07-06 补充 Wan22 AIO 链路扩展/重生成/拼接 service seam，覆盖旧图生视频与 v2，handler/callback 只保留 Telegram 交互、任务启动与结果发送；2026-07-06 本轮补充 quick image/video 与高级视频 settings callback 归一、LTX 显式参数提交并禁止 `context.user_data` 顶层桥接的最新 service seam |
| `.codex/skills/allbot-tg-fsm/SKILL.md` | `src/handlers/fsm/quick_image_fsm.py`、`src/handlers/fsm/quick_draw_callback_data.py`、`src/handlers/menu_route_registry.py`、`src/handlers/message_handler.py`、`qqcc_bot/prompt_handlers.py` | 已修正 | 2026-07-11 同步 QQCC AI滤镜 FSM 技能口径：主 Bot 旧 `qqcc.menu.ai_filter` / `AI滤镜` 只跳转 QQCC；QQCC `qfilter_scene:*` 复用 quick image FSM 与 Quick Image submission service，场景 kind 写入重生成 metadata |
| `.codex/skills/allbot-qqcc-lazy-bot/SKILL.md` | QQCC runtime、release policy、`scripts/release.py`、cloud compose、配置/提交 service 与 quick image/video FSM | 已修正 | 既有 QQCC 配置、场景、任务归属与 single-polling 红线有效；2026-07-16 正式发布入口统一为 immutable `release.py`，按模块请求 Bot/Config 闭包，禁止 legacy shell、rsync、现场 build/compose，并继承正式维护模式逐次选择、默认开启的运维契约 |
| `.codex/skills/allbot-qqcc-lazy-bot/SKILL.md` | `src/services/qqcc_config_service.py`、`qqcc_bot/keyboards.py`、`qqcc_bot/prompt_handlers.py`、`src/handlers/fsm/quick_image_fsm.py`、`src/services/qqcc_draw_chain_service.py`、`src/services/quick_image_submission_service.py`、`dashboard/frontend/src/components/QqccBotSettings.vue` | 已修正 | 2026-07-11 同步 QQCC AI滤镜技能口径：主菜单新增可隐藏 `AI滤镜`，配置新增默认空 `filter_scenes`，AI绘图可通过 `postprocess_filter_scene_id` 引用滤镜终止模板；`postprocess_draw_scene_id` 优先互斥；直接 `qfilter_scene:*` 与 AI绘图滤镜后处理均复用 quick image 提交链，不新增数据库或执行面 |
| `.codex/skills/allbot-qqcc-lazy-bot/SKILL.md` | `src/services/quick_image_submission_service.py`、`src/services/quick_video_submission_service.py`、`src/services/qqcc_regenerate_metadata.py`、`src/services/qqcc_regeneration_service.py`、`src/services/tg_task_result_presentation.py`、`src/services/task_service_completion.py`、`qqcc_bot/regeneration_callback.py`、`qqcc_bot/callback_handler.py` | 已修正 | 2026-07-11 同步 QQCC 投稿能力退出与重生成入口：QQCC 自生成结果不可投稿/不可公开，旧发布 callback 防绕过；最终结果完成文案使用 QQCC 实际功能/场景名，结果按钮展示 `重新生成`，回调从本人 History 重建 quick image/video 提交计划并重新做额度检查；市集原生应用继续带 `source_post_id` 与 `allow_contribute=false` |
| `.codex/skills/allbot-qqcc-lazy-bot/SKILL.md` | `src/services/qqcc_draw_chain_service.py`、`src/services/quick_video_submission_service.py`、`src/core/task_core_runtime.py`、`tests/services/test_quick_image_submission_service.py`、`tests/services/test_quick_video_submission_service.py` | 已修正 | 2026-07-11 同步 QQCC 链式 continuation 策略：第一个真实子任务普通可取消，后续子任务 `base_priority=100`、`allow_cancel=false`、`user_cancel_allowed=false`；快速换脸、无尾帧 AI动图和单步 AI绘图保持普通可取消 |
| `.codex/skills/allbot-qqcc-lazy-bot` / `allbot-tg-fsm` / `allbot-task-engine` / `allbot-ops-deployment` / `allbot-cloudflare-ops` | QQCC private Bot code、compose、validator、membership/metrics、Nginx 与专项文档 | 已修正 | 2026-07-12 同步 private 申请 token 删除、无审核/一人一 Bot、同 ID active-task 救援轮换、webhook worker exact client type、官方 QQCC token 仅用于共享频道会员 checker且无 polling、三层有界背压/startup PEL catch-up、admin metrics、加密 secret、owner 独立 limiter/CSP/双层 Host 隔离和 gate 条件 validator；同日更新为云正式 gate/migration/worker/webhook/owner Host 已启用，并补 Python 3.10 `asyncio.TimeoutError` 周期循环红线 |
| `.codex/skills/allbot-qqcc-lazy-bot` / `allbot-task-engine` | private task recovery contract、quick image/video submission service、private continuation/worker recovery | 已修正 | 2026-07-12 同步私有最终单任务展示契约和 durable 多阶段续跑：原始输入/stage plan/checkpoint 先持久化，阶段结果 CAS 后 cleanup，最终进入 `delivery_pending` 后投递，TaskRegistry 为空仍扫描，租约与 external cancel 不能覆盖已 advance/delivery 状态；缺恢复契约或 continuation ref 继续 fail closed。生产已启用但首个真实 owner token/任务恢复仍待手动验证，不能把“代码支持续跑”误写成已完成真实 Telegram 端到端验收 |
| `.codex/skills/allbot-billing-auth/SKILL.md` | auth/billing/affiliate code、`src/services/permission_growth_channel_service.py`、`src/quota.py` | 已核对 | 计费鉴权边界有效；标准邀请新增 `is_new` / `new_user_was_created` 双层准入，阻断历史用户补绑和后续奖励资格 |
| `.codex/skills/allbot-gallery-storage/SKILL.md` | Gallery/R2 code、apply-context、Web canonical mapping、QQCC 市集代码、R2 CORS 实测 | 已修正 | 2026-07-14 同步 `free_edit_v3_group`、旧 v2 group 查询别名、历史 v2 投稿转 v3 单图应用、prompt 锁定与模板结果禁止再投稿；同日将不可变 Pages 测试自定义域/默认域纳入 `user-data-test` 必需 CORS，并记录预检与真实签名上传验收口径；LTX、SCAIL-2、`i2i_draw` 与 apply-context 其余口径继续有效。 |
| `.codex/skills/allbot-diagnosing-bugs/SKILL.md` | bug 诊断流程 | 已核对 | 诊断闭环有效 |
| `.codex/skills/allbot-tdd/SKILL.md` | tests、dependencies seam | 已核对 | TDD seam 口径有效 |
| `.codex/skills/allbot-codebase-design/SKILL.md` | 架构词汇、`src/core` import graph | 已修正 | 明确 core capability/provider 是目标边界；当前只确认平台对象隔离，基础设施直连仍须作为架构债务报告，避免把规范误写成现状 |
| `.codex/skills/allbot-code-analyzer/SKILL.md` | 静态分析规范 | 已核对 | 只读/报告/清理规范有效 |
| `.codex/skills/backend-code-review/SKILL.md` | backend review references | 已核对 | 审查规范有效 |
| `.codex/skills/vue-best-practices/SKILL.md` | Vue 3 frontend | 已核对 | 前端规范有效 |
| `.codex/skills/frontend-browser-preview/SKILL.md` | Playwright preview workflow | 已核对 | 截图验收规范有效 |
| `.codex/skills/ops-log-monitor/SKILL.md` | log/report workflow | 已核对 | 日志监控边界有效 |
| `.codex/skills/*/references/*.md` | 对应 Skill | 已核对 | reference 文件保持作为按需深读材料 |
| `.codex/skills/*/agents/openai.yaml` | 子代理配置 | 已核对 | 配置文件不作为长期业务知识正文 |
| `docs/子模块_QQCC懒人Bot_qqcc_lazy_bot.md` / `docs/子模块_附加模型配置指南_comfy_models.md` / `.codex/skills/allbot-qqcc-lazy-bot` / `allbot-comfy-models` / `allbot-tg-fsm` / `allbot-task-engine` | `ai_video_scenes` 配置与 Config Web、quick video FSM/service、actor LTX entrypoint、private continuation、LTX request/API/worker mappings | 已修正 | 2026-07-16 新增默认空的 QQCC `AI视频`：入口紧随 AI动图，固定 LTX `1280x704`、5/10/15/20s、最多 3 个 `{path,strength}` LoRA、可选尾帧绘图链、官方/私有演示媒体和最新配置重新生成；私有尾帧链增加 durable `ltx_video` executor，结果隐藏 LTX 扩展/拼接。Web LTX 练功房与模板应用新增负面提示词；I2V/FLF2V/V2V Audio 非空负向映射节点 `29.text`，空白字段完全省略以保留 workflow 默认；Telegram 高级 LTX 设置不变。本轮无数据库表、task type、workflow JSON、模型资产或部署变更。 |
| `docs/子模块_QQCC懒人Bot_qqcc_lazy_bot.md` / `docs/子模块_附加模型配置指南_comfy_models.md` / `docs/子模块_Git不可变发布_git_immutable_release.md` / `.codex/skills/allbot-qqcc-lazy-bot` / `allbot-comfy-models` | `src/qqcc_ltx_lora_catalog.py`、`src/services/qqcc_config_service.py`、`src/lora_catalog.py`、`deploy/release-policy.yml`、QQCC Config Web 动态选项、quick video submission service | 已修正 | 2026-07-17 新增 QQCC Config 独占 LTX LoRA 目录：接入本机校验库 32 项，其中 26 项不在公开目录；后台选项、保存白名单和缺省强度读取专用目录，懒人 Bot 继续转换为 `{name,strength}` 提交。公开主 Bot/Web catalog 保持不变。两份 QQCC 目录/配置文件已纳入内容 SHA256 固定的同组独立发布 snapshot，只有内容完全匹配时才允许分别执行 `qqcc-config` 两服务与 `qqcc-bot` 单服务 rolling 事务，后续漂移立即 fail closed。模型已在本机 registry 备齐，但 RunPod/LAN AIO manifest 与正式 GPU runtime 未同步，需独立发布和 smoke 后才能宣称运行时生效。 |
> 2026-07-16：schema v2 发布门禁按 track 隔离：`control-plane` / `test-execution` 未选择 GPU profile 时，不完整 `gpu-execution` manifest 不再阻断其 plan、部署或晋级；选择缺失 GPU profile 仍 fail closed。事务暂存状态在提交时按 track 原子写入 `current/history`，避免回落到旧 flat 路径。事实源为 `scripts/release.py`、`tests/ops/test_release_cli.py`、不可变发布文档与 `allbot-ops-deployment` Skill。
>
> 2026-07-17（取代 2026-07-16 的四服务全移除口径）：专属测试 QQCC Bot 与 QQCC Config 上线后，QQCC Config 前后端恢复进入 test overlay、发布白名单和 standard 验收；Dashboard-only 仍为 `test-not-required`。共享构建输入产生 Dashboard + QQCC Config 混合 artifact bundle 时，test guard 过滤不可用 Dashboard 后继续部署 QQCC Config，只有过滤后无测试目标的纯 owner-only mutation 才拒绝。2026-07-19 起改为 main bundle 按需测试，默认不部署 test-execution，专项诊断再显式选择该 track。事实源为 test overlay、`scripts/release.py`/`release_strategy.py`、云测试/并发文档与对应测试。
>
> 2026-07-16：正式不可变切换在 private worker 启动门禁发现薄镜像缺少其直接导入的 `qqcc_bot.main`，事务自动回滚且未提交部分状态。`private-bot-worker` target 现同时复制 `qqcc_private_bot/` 与 `qqcc_bot/`，artifact inputs 同步覆盖两目录，并以模块闭包测试锁定；须由新的 main SHA/digest bundle 重跑同 SHA 验证后再晋级。事实源为 `deploy/docker/Dockerfile.control-plane`、`deploy/release-artifacts-v2.json`、`tests/ops/test_modular_images.py` 与 QQCC Skill。
>
> 2026-07-16：为生产启用、测试禁用的 private worker 闭包修复新增显式 `--control-plane-repair-fast-track`，不作为通用免测入口。它复用 verified main-channel 测试状态，只允许 control-plane Dockerfile、v2 catalog 与 release/docs/tests/skills 元数据；其它变化 digest 必须证明 inputs 未变且 Docker target 与已测 SHA 等价，private digest 必须以 `--network none` 真实导入两个运行包。业务代码、migration、Compose、GPU/ops、未知路径及 module/service/from-SHA override 均 fail closed，生产 main/CI/env/preflight/确认/事务回滚门禁保持。事实源为 `scripts/release.py`、`tests/ops/test_release_cli.py` 与不可变发布/部署 Skill。
