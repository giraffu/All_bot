# 测试与入口命名约定

更新时间: 2026-05-25

## 1. 测试目录定位

- `tests/`
  - 当前主测试目录。
  - 新增的单元测试、focused tests、router/service/core 回归、集成测试统一放在这里。
  - 默认以可复用 fixture、稳定 seam 和 CI 可执行为准。

- `src/tests/`
  - 历史遗留测试目录。
  - 目前主要承载早期 smoke/import/system 类型脚本，不作为新增测试的默认落点。
  - 若继续维护，优先迁移到 `tests/`，并对齐 `tests/conftest.py`、命名规范与回归入口。

## 2. 命名规则

- 新测试文件统一使用 `tests/<domain>/test_*.py`。
- `src/tests/` 下不再新增新的 `test_*.py`，除非是迁移过程中的短期兼容。
- 文档、脚本、回归门禁中提到“测试入口”时，默认指向 `tests/`。

## 3. Telegram Bot 入口说明

- `src/bot_test.py` 是 Telegram Bot shared entrypoint。
- `bot_test` 这一名称属于历史遗留，并不表示“仅测试环境使用”。
- 生产与测试容器都可以启动这个入口，真实运行模式由 `BOT_TYPE` 和对应 token 环境变量决定。
- 后续文档、部署脚本和排障说明中，统一将其描述为：
  - `Telegram Bot shared entrypoint (`src/bot_test.py`)`

## 4. 维护约束

- 新增测试时，先放 `tests/`，不要继续扩散 `src/tests/`。
- 若需要保留历史入口文件名，应在文件内补充用途注释，并在文档中给出“历史命名 + 当前职责”说明。
- 若未来重命名 `src/bot_test.py`，应同步更新容器启动命令、排障文档和 import smoke tests。
