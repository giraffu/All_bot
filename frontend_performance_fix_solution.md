# 前端性能与高并发问题排查及修复方案

## 背景现象
近期服务器遭遇严重的带宽消耗和由于高并发请求引发的 `110 Connection timed out` 及 `504` 错误。排查发现，前端代码缺陷导致真实用户在极短时间内对同一个多媒体资源（特别是生成的 MP4 视频）发起了疯狂的重复请求，同时伴随着海量的 API 重复请求。

## 根本原因分析

### 1. 视频组件（`<video>`）的渲染死循环：瀑布流组件引发的血案
*   **现象**：单个生成的 `.mp4` 文件在短时间内被请求 62 次。
*   **原因**：在 `Gallery.vue` 中使用了 `vue-waterfall-plugin-next` 瀑布流组件。由于可能未正确绑定绝对宽高，或给视频强制拼接了 `#t=0.001` 且设置了 `preload="metadata"`，当视频 Metadata 加载完毕导致高度发生微小变化时，瀑布流会触发重排。在这个过程中 Vue 可能会销毁并重新挂载 `<video>` 节点。每次重新挂载浏览器都会重新发起视频请求，从而陷入“加载 -> 高度变化 -> 瀑布流重排 -> 视频重新挂载 -> 重新加载”的无限死循环。

### 2. 资源预加载/列表渲染问题：全量并发请求吃光连接数
*   **现象**：单个 IP 消耗 170+ MB 带宽，视频反复拉取。
*   **原因**：核心广场页（`Gallery.vue`）完全没有使用懒加载。虽然项目中存在 `<LazyVideo>` 组件，但在流量最大的画廊页直接手写了 `<video preload="metadata">`。用户一进入画廊，浏览器会瞬间发起几十个并发的媒体请求，吃光浏览器对同一域名的并发连接数上限，阻塞后续 API 请求，引发 504 错误。

### 3. 过激的轮询/重试机制：SSE 断线后的疯狂原生重连
*   **现象**：伴随着频繁的接口请求风暴。
*   **原因**：`src/stores/tasks.ts` 中使用了 `EventSource` (SSE) 监听任务。但在 `source.onerror` 的错误处理回调中，`// source.close()` 被注释掉了。当服务器因视频并发下载导致拥塞，SSE 断开连接时，浏览器的原生行为是立即、无限次地尝试重新连接，加剧了服务器的崩溃。

### 4. API 请求去重与缓存：UI 卡顿引发的“狂点风暴”
*   **现象**：`/api/gallery/config` 请求 25 次，`/api/users/me` 请求 24 次。
*   **原因**：由于视频并发加载导致网络堵塞，页面显得“卡死”。用户焦躁地在菜单间来回切换。由于前端（如 `Profile.vue` 和 `Gallery.vue`）在每次挂载时都在无脑调用 API 获取数据，且缺乏 Promise 请求合并（Request Deduplication）机制，导致产生了海量重复请求。

---

## 灾难演进链路
没做懒加载的视频瞬间发起了大量并发请求 -> 阻塞了浏览器网络通道并导致服务器响应变慢 -> 触发了瀑布流的重绘死循环和 SSE 的无限重连 -> 用户觉得卡顿开始狂点菜单 -> 产生了海量重复的 API 请求彻底压垮服务器。

---

## 修复计划建议

### Step 1: 统一使用懒加载组件
*   将 `Gallery.vue`、`MyFavorites.vue` 等所有使用 `<video>` 的地方替换为现有的 `LazyVideo.vue`。
*   确保 `preload="none"`，仅在 `IntersectionObserver` 触发元素进入可视区域时才动态赋予 `src` 加载资源。

### Step 2: 修复 SSE 重连黑洞
*   在 `src/stores/tasks.ts` 中放开 `source.close()`。
*   手动实现带指数退避（Exponential Backoff）的重试机制，或者在网络断开时给予合理的提示，而不是让原生 API 无限重试。

### Step 3: 增加 API 请求并发锁（请求合并）
*   在 `auth.ts` 等 Store 和 API 层实现 in-flight promise 缓存。如果某个 API（如获取用户信息或配置）正在请求中，后续的并发调用应直接返回同一个 Promise，防止同时发出多个相同的 HTTP 请求。

### Step 4: 瀑布流稳定性优化
*   给所有媒体元素提供明确的占位宽高比（Aspect Ratio）。
*   利用后端返回的 `width` 和 `height`，在 CSS/Style 中锁定容器宽高比，避免图片/视频加载完毕后撑开容器引发重排和重新挂载。
