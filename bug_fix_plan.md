# AllBot 计费与一键应用 Bug 分析及修复方案

针对测试中发现的一键应用与计费异常问题，结合当前代码实现，详细分析与修复方案如下：

## 1. Bug 1: 高级图生视频 (LTX Video) 20s 计费回填异常
**现象**：高级图生视频 20s 原本应扣 40 灵石，但一键应用后只扣了 10 灵石。

### 根因分析
1. **时长单位传递错误 (秒 vs 帧)**：
   在 `src/core/task_dispatcher.py` 的 `LtxVideoStrategy.submit_task` 中，代码直接将用户选择的时长 `dur_val`（如 `20` 秒）作为 `length` 参数传给了 LTX Video 工作流。但在 LTX 工作流中，`length` 参数的实际语义是**视频帧数**。
   传入 `20` 导致 Worker 实际上只生成了 20 帧的视频（在 24fps 下不到 1 秒）。
2. **后置媒体信息提取与回填覆盖**：
   视频生成完成后，后置的 `ffprobe` 会探测实际生成的视频时长。由于生成的视频不足 1 秒，提取到的 `duration` 为 `1`（或者 `0`），该值被覆盖保存到了 `History` 表中。
3. **一键应用计费兜底漏洞**：
   当用户在前端点击“一键应用”时，前后端加载到的历史时长为 `1`。在后端计费时，`LTX_DURATION_MULTIPLIER.get("1s", 1.0)` 无法命中预设的 `"20s": 4.0`，触发了默认的 `1.0` 倍率兜底。最终导致 `10 * 1.0 = 10` 灵石的错误扣费。

### 修复方案
1. **修正 LTX 时长换算**：
   修改 `LtxVideoStrategy.submit_task`，将秒数转换为帧数后再提交给 Worker。LTX 通常为 24fps，计算公式应为 `length = dur_val * 24 + 1`（如 5s -> 121帧, 20s -> 481帧）。
2. **强化计费倍率容错**：
   在 `LtxVideoStrategy.get_cost` 和 `BaseVideoStrategy.get_cost` 中增加对非标准时长（如因历史脏数据导致的 "1s", "19s" 等）的容错计算逻辑。可以通过按比例折算或向上/向下归一化到最近的有效档位，避免 fallback 到 `1.0` 导致资损。
3. **补齐“请求时长档位”的稳定回填来源**：
   仅修正提交时的秒转帧，还不能修复已经落库的历史脏数据。当前 `History` 中保存的是成片的实际 `duration`，并不等价于用户原始选择的档位。建议补充持久化“用户请求时长档位”或等价的模板参数来源，避免后续一键应用继续把脏值 `1s` 当成真实配置使用。
4. **统一 LTX 一键应用恢复口径**：
   Web 与 TG 对 LTX 模板恢复的来源应保持一致，避免一端依赖历史元数据、另一端依赖 prompt 字符串解析，导致同一任务在不同入口恢复出不同的时长和分辨率。


## 2. Bug 2: 图生视频附加模型 (Video Lora) 5s 720p 计费翻倍
**现象**：图生视频附加模型 5s 720p 原本 18 灵石，一键应用后变成了 36 灵石（1024p 的价格）。

### 根因分析
1. **尺寸推断算法缺陷 (最长边判断)**：
   在处理旧任务的计费档位回填（`_resolve_history_billing_resolution`）以及前端解析一键应用状态（`resolveTierBillingResolution`）时，如果原始的 `billing_resolution` 丢失或需要重新推断，系统会使用实际输出视频的宽高来计算。
2. **竖屏视频触发升级**：
   当前算法 `_normalize_tier_from_longest_side` 使用的是视频的**最长边**（`max(width, height)`）。
   对于 720p 的竖屏视频，其实际分辨率通常为 `720x1280`，最长边为 `1280`。由于 `1280 >= 960`，算法错误地将其划分为 `1024p` 档位。
3. **计费联动翻倍**：
   前端据此将下拉框状态自动切换至 `1024p`，并向后端发送 `1024` 的请求。后端 `BaseVideoStrategy.get_cost` 按照 `1024p` 的基础价 `36` 灵石计算，从而导致扣费翻倍。

### 修复方案
1. **后端算法修正**：
   修改 `src/core/video_billing.py`，将计费档位的推断逻辑从“最长边”改为基于**最短边** (`min(width, height)`)。
   - `1024x1024` 或 `1024x1536` 的最短边为 `1024` -> 归为 `1024p`
   - `720x1280` 的最短边为 `720` -> 归为 `720p`
   - `512x768` 的最短边为 `512` -> 归为 `512p`
2. **前端逻辑同步**：
   修改 `frontend/src/utils/templateVideoSettings.ts` 中的 `resolveTierBillingResolution` 方法，同步将 `Math.max` 改为 `Math.min`，并将内部的 `normalizeTierFromLongestSide` 重命名并修改为按最短边判断。
3. **同步清理其他同类推断逻辑**：
   除了 `video_billing.py` 与前端工具函数外，还应检查 Telegram 广场一键应用、自定义视频限制判断等旁路逻辑，避免仍有其他位置继续按照最长边或与主逻辑不同的阈值进行档位归类。
4. **补充回归测试**：
   更新前端 `templateVideoSettings` 相关测试用例，并为后端 `billing_resolution` 推断增加 720x1280、512x768、1024x1536 等典型竖屏/长图场景，防止旧错误逻辑回归。


## 3. Bug 3: `face_video` (视频换脸) 时长单位混淆，导致 TG 用户“白嫖”时长
**现象**：Telegram 端发起视频换脸，无论选 5s 还是 10s，最终扣费仅按 5s 算，但实际生成了更长的视频。与 Web 端不一致。

### 根因分析
1. **FSM 传递参数单位错误**：
   Telegram FSM (`src/handlers/fsm/face_video_fsm.py`) 中，硬编码 `duration = 121` 传给 `TaskService`，其本意是传帧数（121帧 = 5秒）。
2. **后端策略二次放大**：
   后端的 `BaseVideoStrategy.submit_task` 期望的 `duration` 是秒数，且内部有判断 `dur_frames = 161 if duration >= 10 else 121`。
   因为传入的 `121` 大于 10，所以 `BaseVideoStrategy` 把所有 TG 的换脸视频都按 `>=10s` 处理，放大到了 161 帧。
3. **计费兜底导致白嫖**：
   在计费阶段 `BaseVideoStrategy.get_cost` 中，`dur_str` 变成了 `"121s"`。`DURATION_MULTIPLIER.get("121s", 1.0)` 无法命中高倍率，触发 `1.0` 兜底。
   最终结果：用户只花了 1.0 倍（5s 的价格，18灵石），却得到了 161 帧（10s）的视频。

### 修复方案
1. **统一传递秒数**：
   修改 `src/handlers/fsm/face_video_fsm.py`，将 `duration = 121` 改为 `duration = 5`（或其他用户选择的秒数），确保传给后端的始终是真实的秒数。
2. **对齐 API 期望参数**：
   检查并确保 `BaseVideoStrategy` 正确把秒数转换为帧数再调用 `image_service`。
3. **消除魔法数字，统一复用帧数映射常量**：
   当前代码中 `81/121/129/161` 等帧数存在散落硬编码。建议统一收敛到共享常量或公共换算函数，避免未来再次出现“上游传帧数、下游按秒解释”的单位混淆问题。


## 4. Bug 4: 广场一键应用时，小众视频模板被强制降级 (画质/计费)
**现象**：在 Telegram 广场一键应用如 `doggy_style`、`perfect_video_insert` 等模板时，FSM 正确解析了原模板画质（如 720p）并提示高扣费，但最终生成的是 512p 的视频，实际扣费也只有 6 灵石。

### 根因分析
1. **参数提取被硬编码限制**：
   在 `src/services/task_service.py` 的 `process_generation_task` 中，代码写死了只有 `if is_video and task_type in [MODE_CUSTOM_VIDEO, "video_lora"]:` 才会从上下文中提取高画质和时长参数。
2. **强制降级回退**：
   对于其他视频模板（如 `doggy_style` 等），直接被忽略了原模板的 `resolution` 和 `duration`，强制 fallback 到了 `512` 和 `5`。

### 修复方案
1. **解除硬编码限制**：
   在 `TaskService.process_generation_task` 中，将画质和时长参数的读取逻辑开放给所有支持自定义配置的视频类型，而不仅仅是 `custom_video` 和 `video_lora`。如果上下文里有这些参数，就优先使用。
2. **显式定义“哪些视频类型允许继承模板参数”**：
   建议不要继续使用零散的白名单硬编码，而是集中维护“支持模板画质/时长透传”的视频任务集合，便于未来新增模板时不会再次遗漏。


## 5. Bug 5: `img2img_lora` 单图情况计费与全局常量冲突
**现象**：图生图附加模型在全局常量表 `TASK_COSTS` 中标价 6 灵石，但在后端策略中单图被强制按 2 灵石扣费。

### 根因分析
1. **策略重写计费规则**：
   `DefaultImageStrategy.get_cost` 中硬编码了：`return 6 if len(inputs.get("images", [])) >= 2 else 2`。
2. **价格体系分裂**：
   如果其他地方（如 FSM 提示或广场展示）读取 `TASK_COSTS`（返回 6），而实际扣款按策略（返回 2），会导致显示与实际扣费不一致。

### 修复方案
1. **对齐计费逻辑**：
   要么修改 `TASK_COSTS` 和展示层逻辑使之匹配“双图 6 灵石，单图 2 灵石”的动态定价，要么修改 `DefaultImageStrategy`，使其严格遵循全局常量表的基础价格（单图也收 6 灵石），以维持价格体系一致性。需根据业务需求确定。
2. **统一价格来源**：
   无论最终决定采用静态定价还是动态定价，都应保证策略层、前端展示层、FSM 提示文案、广场模板展示共用同一套价格来源，避免后续再次出现“展示 6，实际扣 2”或相反情况。


## 6. 补充优化与注意事项
以下内容不一定单独构成当前已开放功能中的 bug，但建议一并纳入修复实施范围，以提高一致性和可维护性：

1. **避免 `multiplier.get(..., 1.0)` 成为静默资损通道**：
   当前多处计费逻辑在未命中合法时长档位时直接 fallback 到 `1.0` 倍率。对于非标准输入，建议改为显式归一化、拒绝非法值，或至少记录告警日志，避免脏数据悄悄按最低价扣费。
2. **统一视频计费档位的“唯一真相来源”**：
   一键应用配置恢复不应一部分依赖 `History.duration/width/height`，另一部分依赖 prompt 字符串解析，再另一部分依赖前端本地推断。建议后续逐步收敛为统一的持久化模板参数来源。
3. **加强旁路流程的代码复用**：
   TG FSM、Web apply-context、任务提交前校验目前存在多套近似但不完全一致的分辨率/时长推断逻辑。建议抽象公共归一化函数，减少多处复制后逐渐漂移的问题。
4. **补充端到端回归用例**：
   建议至少覆盖以下场景：
   - LTX 20s 模板生成后再一键应用，确认仍按 20s 档位扣费；
   - 720x1280 竖屏视频模板一键应用，确认不会升级到 1024p；
   - `face_video` Telegram 流程分别测试 5s/10s，确认扣费与产出时长一致；
   - `doggy_style` 等非 `custom_video/video_lora` 模板一键应用，确认能继承原模板档位。
5. **关注任务进度消息编辑频率**：
   本轮修复本身的性能开销很小，但当前任务进度轮询会频繁编辑 Telegram 状态消息。若后续并发继续提升，可考虑加入节流策略，减少编辑过于频繁导致的限流或无效更新。
