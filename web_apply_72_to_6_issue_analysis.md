# Web 端“一键应用”72 晶石误降为 6 晶石问题核查

## 1. 问题背景

用户反馈：

> Web 端应用自己的模型，明明是 72 晶石的，但都只能做消耗 6 晶石的画质。

本次仅做代码核查与方案梳理，暂不修改业务代码。

---

## 2. 结论摘要

当前问题不是计费公式本身错误，而是 Web 端“一键应用”在特定入口下没有把原模板的视频参数完整带回，导致前端回退到默认视频档位：

- 默认分辨率：`512`
- 默认时长：`5`
- 默认价格：`6` 晶石

同时，前端在模板模式下会锁定输出设置，用户无法手动切回原本的高画质档位，因此会表现为：

- 原模板本应是 `1024p + 8s = 72` 晶石
- 一键应用后却只能看到并提交 `512p + 5s = 6` 晶石

---

## 3. 正常计费逻辑梳理

视频计费并不是写死的 `72`，而是按“分辨率基础价 x 时长倍率”动态计算：

- `512p = 6`
- `720p = 18`
- `1024p = 36`
- `5s = x1`
- `8s = x2`
- `10s = x3`

因此：

- `1024p + 8s = 36 x 2 = 72`
- `512p + 5s = 6 x 1 = 6`

也就是说，用户看到 6 晶石，本质上说明最终提交到前端页面的参数已经退回到了最低默认档，而不是后端把 72 直接算错成了 6。

---

## 4. Web 端“一键应用”功能调用链

### 4.1 前端入口

Web 端“一键应用”主要有 3 个入口：

1. 社区广场
2. 我的投稿
3. 我的收藏

点击后，前端会：

1. 请求对应的 `apply-context` 接口
2. 将返回上下文写入 `sessionStorage.galleryApplyContext`
3. 按 `task_type` 跳转到具体的功能页
4. 在目标页读取上下文并自动回填参数

### 4.2 视频类目标页

视频一键应用最终主要落到：

- `frontend/src/views/SingleImageToVideo.vue`

这个页面的默认值是：

- `resolution = '512'`
- `duration = '5'`

只有当 `apply-context` 返回了原始模板里的 `width` 和 `duration` 时，页面才会把默认值覆盖成原模板档位。

---

## 5. 已定位的异常链路

### 5.1 异常入口

最可疑且能直接解释用户反馈的入口是：

- `frontend/src/views/MyFavorites.vue`
- 当 `filterType = 'favorite'` 时
- 点击“一键应用”走的是：
  - `GET /users/history/{task_id}/apply-context`

而不是广场使用的：

- `GET /gallery/posts/{post_id}/apply-context`

### 5.2 两条接口的关键差异

#### 广场 / 我的投稿链路

`/gallery/posts/{post_id}/apply-context` 会返回：

- `width`
- `height`
- `duration`
- `task_type`
- `prompt`
- `lora_name`

这条链路的数据来源是 `GalleryPost`，其中确实存有：

- `width`
- `height`
- `duration`

因此广场和我的投稿理论上能恢复原视频的画质和时长。

#### 我的收藏 favorite 链路

`/users/history/{task_id}/apply-context` 当前返回值里写死为：

- `width = None`
- `height = None`
- `duration = None`

也就是说，这条链路根本没有把原视频模板的关键参数带回来。

---

## 6. 根因分析

### 根因一：历史 apply-context 接口丢失视频档位信息

`/users/history/{task_id}/apply-context` 目前只能返回：

- prompt
- lora_name
- input_file
- input_file_url
- task_type

但对视频一键应用真正决定价格和质量的字段：

- `width`
- `height`
- `duration`

返回的都是空值。

这会导致目标页无法恢复原模板的高画质参数。

### 根因二：前端视频页默认值会回退到 512 + 5

`SingleImageToVideo.vue` 初始化默认值就是：

- `resolution = '512'`
- `duration = '5'`

在模板模式下，如果后端没有返回有效的 `ctx.width` 和 `ctx.duration`，页面就会保持默认值。

于是价格自动变成：

- `512p + 5s = 6` 晶石

### 根因三：模板模式把输出设置锁死，用户无法补救

一旦进入模板应用模式：

- 页面会提示“分辨率与时长已根据模板锁定”
- 不再允许用户手动选择输出设置

但实际上 favorite 历史链路并没有把模板参数带回来。

于是用户看到的就是一种错误状态：

- 页面宣称“已按模板锁定”
- 实际锁定的却是默认最低档

这正是“明明原模板是 72，却只能做 6 晶石画质”的直接原因。

---

## 7. 影响范围评估

### 高概率受影响

以下场景大概率会复现：

- Web 端
- 我的收藏
- `filterType = favorite`
- 对视频类模板执行一键应用

尤其是以下视频类型：

- `custom_video`
- `video_lora`

若原模板是高档位，例如：

- `1024p + 8s`

则一键应用后会掉回：

- `512p + 5s`

### 理论上不应受影响

以下入口理论上问题较小或不受影响：

- 社区广场的一键应用
- 我的投稿的一键应用

因为这两条链路走的是 `gallery/posts/{post_id}/apply-context`，会从 `GalleryPost` 中读取 `width/duration`。

### 仍需补充验证的边界

如果历史上存在一些老帖子在投稿入库时本来就没有存 `width/duration`，那么即便走广场链路，也可能出现同类问题。

但从当前代码核查结果看，主问题仍然是：

- favorite 历史链路没有返回视频参数

---

## 8. 推荐修复方案

以下为推荐的最小修复方案，优先级从高到低排列。

### 方案 A：优先修后端历史 apply-context，补齐 width/height/duration

目标：

- 让 `/users/history/{task_id}/apply-context` 能够返回原视频模板的真实参数

推荐做法：

1. 优先尝试通过 `History.task_id` 关联 `GalleryPost`
2. 如果存在对应 `GalleryPost`，直接回填：
   - `width`
   - `height`
   - `duration`
3. 若不存在对应 `GalleryPost`，再考虑从其他元数据来源兜底

优点：

- 改动最小
- 对现有前端侵入低
- 能直接修复 favorite 历史链路

注意：

- `History` 表本身目前没有 `width/height/duration` 字段，不能直接从 `History` 取
- 如果用户收藏的是自己历史，但从未投稿到广场，仅依赖 `GalleryPost` 可能仍然拿不到参数

因此这个方案适合先止血，但还不够彻底。

### 方案 B：补强数据模型，让历史记录自身持有媒体参数

目标：

- 让历史记录本身具备独立恢复模板所需的媒体元数据

推荐做法：

1. 在 `History` 或等效任务结果记录中持久化：
   - `width`
   - `height`
   - `duration`
2. 后续所有 apply-context 优先从历史本体取值

优点：

- 数据模型更自洽
- 不依赖“是否已投稿到广场”
- 更适合长期稳定维护

缺点：

- 需要迁移和补历史数据
- 改动范围大于方案 A

### 方案 C：前端模板锁定逻辑改为“有完整参数才锁定”

目标：

- 避免后端数据不完整时，前端把默认低档误当成模板配置锁死

推荐做法：

在视频类页面中增加条件判断：

- 只有当 `ctx.width` 和 `ctx.duration` 都有效时，才锁定输出设置
- 如果参数缺失，则给出提示，并允许用户手动选择画质和时长

优点：

- 就算后端偶尔漏字段，也不至于把用户锁在 6 晶石档
- 用户体验更稳健

缺点：

- 只能缓解，不能替代后端数据补齐

### 方案 D：增加模板来源标记和异常日志

目标：

- 提高后续排查效率

推荐做法：

在 apply-context 和前端跳转流程中增加日志或埋点，记录：

- 来源页面
- 来源接口
- task_type
- 是否返回 width/duration
- 模板模式下最终使用的 resolution/duration

这样可以快速确认：

- 是哪个入口出了问题
- 是后端没返回，还是前端没正确回填

---

## 9. 推荐实施顺序

建议按以下顺序修：

1. **先修方案 A**
   - 尽快让 `/users/history/{task_id}/apply-context` 带回可用的 `width/height/duration`
2. **同时补方案 C**
   - 前端只有拿到完整模板参数时才锁定输出设置
3. **后续评估方案 B**
   - 从根上补齐历史记录元数据，避免长期依赖 `GalleryPost`
4. **加上方案 D**
   - 为后续类似问题保留可观测性

---

## 10. 验证方案

修复后建议按以下用例验证：

### 用例 1：favorite 历史中的高档位视频模板

前置条件：

- 用户在 Web 端收藏一条原始参数为 `1024p + 8s` 的视频模板

验证点：

1. 点击“一键应用”
2. 请求 `/users/history/{task_id}/apply-context`
3. 返回值应包含：
   - `width = 1024`
   - `duration = 8`
4. 跳转到视频页后应显示：
   - 价格 `72`
5. 若模板参数完整，输出设置可锁定

### 用例 2：favorite 历史中的低档位视频模板

前置条件：

- 原模板为 `512p + 5s`

验证点：

1. 一键应用后显示价格应为 `6`
2. 且该 6 应来源于真实模板，而不是默认回退

### 用例 3：收藏记录参数缺失的兜底行为

前置条件：

- 构造一个无法获取 `width/duration` 的历史模板

验证点：

1. 前端应提示模板参数不完整
2. 不应直接把最低档锁死
3. 用户应能手动调整输出设置

### 用例 4：广场链路回归验证

验证点：

1. 从广场点击一键应用
2. 高档位模板仍应正确恢复为原画质和原时长
3. 不应受到 favorite 修复的副作用影响

---

## 11. 附加观察

本次核查还发现另一个与“一键应用”相关但不直接影响本问题的缺口：

- 后端任务提交协议支持 `source_post_id`
- 核心层也支持在生成成功后记录 `apply` 行为
- 但当前 Web 前端提交任务时只传了 `is_template`
- 没有把 `apply-context` 里的 `post_id` 继续提交为 `source_post_id`

这意味着：

- Web 端模板应用的“统计闭环”大概率没有完整打通
- “应用次数”数据可能并不准确

该问题不影响本次 72 -> 6 的定价异常，但建议后续一并检查。

---

## 12. 最终判断

本问题当前最可信的根因是：

> Web 端 favorite 历史链路的 `apply-context` 没有返回原视频模板的 `width/height/duration`，导致视频页面回退到默认 `512p + 5s = 6` 晶石；与此同时，模板模式又锁定了输出设置，使用户无法恢复原本应有的 `1024p + 8s = 72` 晶石档位。

因此，优先修复方向应是：

1. 补齐 favorite 历史 apply-context 的视频参数
2. 优化前端模板锁定条件，避免缺参数时误锁默认低档

