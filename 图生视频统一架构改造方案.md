# 图生视频统一架构改造方案

## 1. 目标

当前 `图生视频(附加模型)` 与 `自定义图生视频` 在用户感知层是两个入口，但在后端与 worker 底层已经高度收敛：

- backend 最终都映射到 `TaskType.VIDEO_EDIT`
- worker 最终都加载同一个 `perfect_video_edit.json`
- 真正差异只剩 Bot/FSM 层是否先收集 `lora_name`

这意味着当前重复主要集中在“交互编排层”，而不是“任务执行层”。

本方案的目标不是简单把两个菜单硬合并，而是把“图生视频”抽象成一个统一能力，并让“附加模型”退化为一个可选参数，从而实现：

- 用户模型更清晰：只有一个图生视频能力
- FSM 更薄：减少重复状态机与重复文案
- 服务层边界更清晰：统一视频任务提交入口
- backend/worker 不做无效重构：复用既有 `VIDEO_EDIT` 能力
- 兼容旧入口：避免一次性破坏已有按钮、命令和用户习惯

---

## 2. 现状诊断

### 2.1 当前三层结构并不对称

现有实现分成三层，但只有最上层仍然“分叉”：

1. Bot 交互层
   - `video_lora_fsm.py`
   - `custom_video_fsm.py`
2. 服务/提交流水层
   - `process_generation_task(..., task_type="video_lora", lora_name=...)`
   - `process_custom_video_task(...)`
3. backend/worker 执行层
   - 最终都落到 `VIDEO_EDIT`
   - 最终都用 `perfect_video_edit.json`

也就是说：

- 上层分成两套
- 中层分成两套半
- 下层其实已经是一套

这是一种典型的“上层概念多于底层真实能力”的架构失衡。

### 2.2 当前重复点

`video_lora_fsm.py` 与 `custom_video_fsm.py` 存在大量重复：

- 相同的维护模式判断
- 相同的 `in_conversation` 互斥保护
- 相同的图片下载与临时文件清理
- 相同的画质/时长选择
- 相同的非法组合自动修正
- 相同的额度校验
- 相同的超时/取消/异常输入处理
- 相同的“收图 -> 配设置 -> 收提示词 -> 提交后台任务”骨架

真正不同的只有：

- 是否多一步 `选择附加模型`
- 提交时是否带 `lora_name`
- 文案展示是否带“已选模型”

### 2.3 当前“无”选项不一致

`自由P图` 已经采用“附加模型是可选参数”的设计：

- 有模型：走 LoRA 逻辑
- 选“无”：退化为普通图生图

而 `图生视频(附加模型)` 目前仍采用“附加模型是必选分支”的设计，因此造成：

- `图生视频(附加模型)` 和 `自定义图生视频` 被人为拆成两个入口
- 用户难以理解两者关系
- 代码多维护一套 FSM

---

## 3. 更合理的架构抽象

### 3.1 统一业务概念

建议把产品概念统一为：

- 一级能力：`图生视频`
- 可选参数：`附加模型`
- 取值：
  - `无`
  - `巨乳膨胀`
  - `乳交`
  - `颜射`
  - `舔阴`
  - `平胸`
  - `足交`
  - `插入优化`

即：

- `自定义图生视频` 不再是一个独立能力
- 它只是 `图生视频 + 附加模型=无`

### 3.2 统一技术抽象

这里需要先区分**当前代码现实**与**目标技术形态**，避免把目标态误写成现状。

#### 当前代码现实

当前仍然存在两个 Bot 领域 mode：

- `MODE_CUSTOM_VIDEO`
- `MODE_VIDEO_LORA`

当前 Dispatcher 的真实判定也不是“只看 `lora_name`”，而是：

- `mode == "video_lora"` 且 `lora_name` 有值 -> `/perfect_video_lora`
- 其他情况 -> `/perfect_video_edit`

也就是说：

- backend/worker 底层已经收敛
- 但 Bot/FSM 与 TaskService 中层还没有收敛到一个统一 mode

#### 目标技术形态

建议演进到一个统一内部任务概念：

- Bot 领域模式：`MODE_IMAGE_TO_VIDEO`
- 可选字段：`lora_name: Optional[str]`

然后在不同层次分别承担不同职责：

1. FSM 层
   - 只负责采集参数
   - 不负责区分“这是 custom_video 还是 video_lora”
2. TaskService 层
   - 只负责把“统一视频任务”转换为标准 inputs
   - 显式接收 `resolution` / `duration` / `lora_name`
   - 不再依赖 `context.user_data["custom_video_resolution"]` 这类历史兼容字段做隐式传参
3. Dispatcher 层
   - 短期保留当前 `mode + lora_name` 的兼容判定
   - 中期再收口为纯 `lora_name` 决定接口选择
4. backend/worker 层
   - 不引入新的 task type
   - 继续复用现有 `VIDEO_EDIT`

这会形成一个更干净的责任划分：

- 交互层负责“收参数”
- 服务层负责“组装任务”
- 分发层负责“选接口”
- 执行层负责“跑工作流”

---

## 4. 推荐方案分级

### 4.1 方案 A：低风险收口版

这是最推荐先落地的方案。

#### 做法

1. 保留现有 backend/worker 完全不动
2. `VIDEO_LORA_MODELS` 增加空值项：`"": "无"`
3. 让 `video_lora_fsm.py` 支持选择“无”
4. 当用户选择“无”时，Bot/Service 直接走普通 `/perfect_video_edit` 链路，而不是把空 `lora_name` 继续提交到 `/perfect_video_lora`
5. 将 `custom_video` 入口改为兼容跳转到统一后的 `video_lora` 流程
6. 同步修改主菜单文案、提示路由、FSM 注册与兼容命令映射
7. 文案层逐步把“图生视频(附加模型)”更名为“图生视频”
8. 暂时保留旧命令与旧菜单 key，作为兼容别名

#### 架构收益

- 立即消除产品概念重复
- 基本不触碰执行链路
- 风险集中在 Bot 层，可控
- 可以快速验证用户是否理解“图生视频 + 可选附加模型”这一模型

#### 仍然保留的技术债

- `TaskService` 仍然存在两条视频入口
- `video_lora_fsm.py` 与 `custom_video_fsm.py` 可能短期并存
- mode 命名还会残留 `custom_video` / `video_lora`
- `custom_video_resolution` / `custom_video_duration` 这组上下文字段仍会暂时存在

#### 适用场景

- 希望先低风险统一交互
- 不想现在动服务层和测试面太大

### 4.2 方案 B：推荐的完整收口版

这是我认为从架构上最合理的主线方案。

#### 核心思想

保留“底层一套工作流”的现实，进一步把“中层任务入口”和“上层 FSM”也统一掉。

#### 做法

1. 新增统一 FSM，例如：
   - `src/handlers/fsm/image_to_video_fsm.py`
2. 新增统一服务入口，例如：
   - `TaskService.process_image_to_video_task(...)`
3. 新增统一的请求/状态数据结构：
   - `image_path`
   - `resolution`
   - `duration`
   - `lora_name`
4. 先保留旧入口，但让：
   - `process_custom_video_task(...)`
   - `process_generation_task(... task_type="video_lora")`
   内部统一转发到 `process_image_to_video_task(...)`
5. 为过渡期新增适配层：
   - 新入口优先使用显式参数
   - 旧链路短期允许 fallback 读取 `custom_video_resolution` / `custom_video_duration`
   - 待所有调用方切换完成后再删除旧上下文字段
6. 废弃：
   - `process_custom_video_task`
   - `video_lora` 作为独立业务模式的心智
7. 将 Dispatcher 从当前“`mode + lora_name` 双条件”逐步收口为“按 `lora_name` 选接口”
8. 将 `custom_video`、`video_lora` 变为历史兼容入口，只做跳转/别名

#### 推荐后的内部模型

可以采用如下统一对象：

```python
@dataclass
class ImageToVideoRequest:
    prompt: str
    image_path: str
    resolution: str
    duration: str
    lora_name: str | None = None
```

统一服务签名示意：

```python
async def process_image_to_video_task(
    *,
    update: Update | None,
    context: ContextTypes.DEFAULT_TYPE,
    prompt: str,
    image_path: str,
    resolution: str,
    duration: str,
    lora_name: str | None = None,
    cleanup: bool = True,
    source_post_id: int | None = None,
):
    ...
```

#### 架构收益

- Bot 层只保留一个主 FSM
- TaskService 层只保留一个视频入口
- 任务日志、计费、状态文案能统一生成
- 新增“更多可选视频模型”时不再需要复制一套 FSM
- 后续若要接 Web 工作台或 gallery apply，也更容易共用同一服务入口

#### 成本

- 需要补齐 focused tests
- 需要处理旧模式名兼容
- 需要同步菜单/i18n/路由映射
- 需要处理 `context.user_data` 旧字段的过渡兼容

### 4.3 方案 C：长期最优版

如果后面还会继续扩展：

- 图生视频
- 高级图生视频
- 模板图生视频
- gallery 一键套用视频
- Web 工作台图生视频

那么建议进一步抽象成“参数驱动式媒体生成 FSM 框架”。

#### 核心思想

把 FSM 的差异抽成“步骤配置”，而不是“复制一个新 FSM 文件”。

例如：

- 步骤 1：是否需要选附加模型
- 步骤 2：是否需要上传 1 张图
- 步骤 3：是否允许多图
- 步骤 4：是否需要视频参数
- 步骤 5：是否需要高级参数

统一成：

```python
class GenerationFlowSpec(TypedDict):
    flow_key: str
    collect_lora: bool
    collect_image_count: int
    collect_video_settings: bool
    submit_entrypoint: str
```

然后按 spec 驱动步骤渲染、状态切换与提交。

#### 适用前提

- 未来确实要持续扩更多生成模式
- 团队已经能接受一次较大重构

#### 当前不建议立即做的原因

- 对当前需求来说有过度设计风险
- 需要重构现有多套 FSM 的公共运行时

---

## 5. 我建议采用的目标方案

建议采用：

- 短期：先做方案 A
- 中期：落到方案 B
- 暂不直接上方案 C

原因：

1. 你的问题本质上已经指出了产品概念重复
2. 当前 backend/worker 底层已经收敛为同一个 `VIDEO_EDIT` 执行能力，而“无模型退化”可以通过上层分支显式走普通 `/perfect_video_edit`
3. 因此应该先顺着现有真实边界收口，而不是重新创造新的底层类型
4. 先把用户入口统一，再决定是否继续做服务层清理

一句话总结：

> 先统一“产品入口”，再统一“服务入口”，最后视扩展需求决定是否统一“FSM 框架”。

---

## 6. 推荐的分层改造蓝图

### 6.1 Bot 层

建议保留的长期形态：

- 一个主入口：`menu.video_lora` 重命名为 `menu.image_to_video`
- `menu.custom_video` 作为兼容别名，后续可下线
- 一个主 FSM：`image_to_video_fsm.py`

FSM 步骤建议：

1. 选择附加模型
   - 默认包含 `无`
2. 上传起始图
3. 选择画质/时长
4. 输入提示词
5. 提交统一服务入口

### 6.2 Service 层

建议新增统一 façade：

- `process_image_to_video_task(...)`

该入口负责：

- 解析分辨率/时长
- 计算计费参数
- 组装统一 `inputs`
- 构建状态文案
- 调用 `_run_bot_task_flow`

过渡阶段建议明确一层 adapter：

- 新调用方直接传 `resolution` / `duration` / `lora_name`
- 旧 `custom_video` / `video_lora` 调用方短期内部转发到统一入口
- 统一入口在过渡期可兼容读取历史 `custom_video_*` 上下文字段
- 待所有 FSM 切换完成后，再删除这组历史字段

不再区分：

- `process_custom_video_task`
- `process_generation_task(... task_type="video_lora")`

### 6.3 Dispatcher 层

这里需要明确“现状”和“目标”不是一回事。

当前真实判定是：

- `mode == "video_lora"` 且 `lora_name` 有值 -> `/perfect_video_lora`
- 其他情况 -> `/perfect_video_edit`

因此更稳的演进方式是：

1. 方案 A 阶段先保持现状判定不变
2. 统一 Bot/Service 入口后，再把 Dispatcher 收口为：
   - `lora_name` 有值 -> `/perfect_video_lora`
   - `lora_name` 为空 -> `/perfect_video_edit`

这样才能避免“上层已经统一成 `image_to_video`，底层却因为 mode 不匹配而错误退化”的问题。

### 6.4 backend 层

短期不建议重构。

原因：

- 当前两条路由已经足够清晰
- 它们本质只是不同请求模型
- 继续合并 backend 路由收益不大，反而会扩大测试面

如未来要进一步统一，可考虑：

- 保留 `/perfect_video_edit`
- 将 `lora_name` 改为可选字段
- `/perfect_video_lora` 仅做兼容壳

但这不是当前第一优先级。

### 6.5 worker 层

短期完全不建议动。

前提是继续遵守当前约束：

- `lora_name` 为空时，不触发 LoRA patch
- `272/273` 节点 ID 保持稳定

---

## 7. 渐进迁移顺序

### 阶段 1：产品与交互收口

目标：

- 用户只看到一个主能力

动作：

1. `VIDEO_LORA_MODELS` 增加 `无`
2. `video_lora_fsm.py` 支持“无”
3. `custom_video` 入口跳转到 `video_lora`
4. 主菜单文案调整
5. 保留旧 key 兼容

### 阶段 2：服务层收口

目标：

- 消除重复的 TaskService 视频入口

动作：

1. 新增统一 `process_image_to_video_task`
2. `video_lora_fsm` 改为只调用这个入口
3. `custom_video` 兼容壳也调用这个入口
4. 标记 `process_custom_video_task` 为兼容层

### 阶段 3：FSM 文件收口

目标：

- 删除重复 FSM 文件

动作：

1. 引入 `image_to_video_fsm.py`
2. 旧 `video_lora_fsm.py` / `custom_video_fsm.py` 改为薄壳或下线
3. 更新测试与路由注册

### 阶段 4：命名与遗留清理

目标：

- 统一术语，降低认知噪声

动作：

1. `video_lora` 改为更中性的 mode 名
2. `custom_video` 下线为兼容别名
3. 清理 i18n、日志、提示文案中的旧术语

---

## 8. 关键兼容策略

### 8.1 菜单兼容

短期不应直接删除：

- `menu.video_lora`
- `menu.custom_video`

建议做法：

- UI 展示统一成一个主入口
- 路由层仍接受旧文案和旧命令

### 8.2 命令兼容

短期保留：

- `/video_lora`
- `/custom_video`

统一跳转到新的主流程。

### 8.3 服务兼容

短期保留旧函数名，但内部统一转发：

- `process_custom_video_task(...)`
- `process_generation_task(... task_type="video_lora")`

这样可以减少一次性改动范围。

---

## 9. 需要重点关注的风险

### 9.1 风险一：只改菜单，不改服务边界

如果只做“菜单合并”，但中层依然保留两套入口，很容易形成：

- 用户以为已经统一
- 代码其实还是两套

这会导致后面继续扩功能时重复再次长出来。

### 9.2 风险二：过早改 backend

当前 backend/worker 已经是稳定边界。

如果为了“形式统一”强行改后端路由，可能会：

- 扩大测试面
- 增加部署风险
- 影响运行中的 worker 兼容

### 9.3 风险三：只改上层 mode，不改 Dispatcher 条件

如果直接把上层改成统一 `MODE_IMAGE_TO_VIDEO`，但 Dispatcher 仍保留当前：

- `mode == "video_lora"` 且 `lora_name` 有值 -> LoRA 接口

那么会出现：

- 上层看起来已经统一
- 实际带 `lora_name` 的任务仍可能错误走普通 `/perfect_video_edit`

这会形成非常隐蔽的行为回退。

### 9.4 风险四：统一了服务签名，但内部仍偷用旧上下文字段

如果只新增 `process_image_to_video_task(...)`，却不清理或显式封装：

- `custom_video_resolution`
- `custom_video_duration`

那么最终会变成：

- 对外看似一个统一服务入口
- 对内仍然依赖历史 `context.user_data` 状态

这会让后续 Web 复用、gallery apply 复用和 focused tests 都变得不稳定。

### 9.5 风险五：一次性删掉旧入口

不建议直接删除 `custom_video`。

更稳的做法是：

- 第一阶段保留别名
- 第二阶段观察使用情况
- 第三阶段再清理

---

## 10. 测试建议

建议把测试重点放在“统一逻辑是否正确退化”而不是重复覆盖整条旧链路。

优先补这些 focused tests：

1. FSM 选择“无”时，最终 `lora_name == ""`
2. FSM 选择具体模型时，最终 `lora_name == "BreastGrow"` 等真实值
3. Dispatcher 现状矩阵测试：
   - `mode="video_lora" + lora_name 非空` -> `submit_perfect_video_lora`
   - `mode="video_lora" + lora_name 为空` -> `submit_perfect_video_edit`
   - `mode="custom_video" + lora_name 非空` -> 仍应走 `submit_perfect_video_edit`
4. 当 Dispatcher 被重构为纯 `lora_name` 判定后，再补新的目标态矩阵测试
5. 旧 `/custom_video`、旧菜单入口、旧 callback 入口是否都正确跳转到统一流程
6. 统一服务入口是否优先使用显式参数，并且仅在过渡期 fallback 读取历史 `custom_video_*` 字段
7. 文案与日志在“无模型”场景下不再误写“附加模型视频”

---

## 11. 最终建议

如果以“架构更合理”为第一目标，而不是“最少改几行”为第一目标，我建议最终落地为：

- 一个统一用户能力：`图生视频`
- 一个统一 FSM：`image_to_video_fsm.py`
- 一个统一服务入口：`process_image_to_video_task(...)`
- 一个统一底层执行能力：继续复用现有 `VIDEO_EDIT`
- 一个可选参数：`lora_name`

其中：

- `自定义图生视频 = 图生视频 + 附加模型=无`
- `图生视频(附加模型) = 图生视频 + 附加模型=具体值`

这是当前代码现实、产品语义、以及后续可扩展性三者之间最平衡的方案。

---

## 12. 可执行结论

建议按以下顺序推进：

1. 先做交互统一
2. 再做服务入口统一
3. 暂不动 backend/worker
4. 最后清理旧命名和兼容壳

如果后续确认要实施，下一版可以直接按这个文档再拆一份：

- `执行清单`
- `涉及文件清单`
- `测试清单`
- `回滚方案`

用于正式开改。
