name: Build RunPod face_swap Profile Image

on:
  workflow_dispatch:
    inputs:
      image_tag:
        description: "GHCR tag. Leave empty to use <UTC date>-face-swap-<short sha>."
        required: false
        default: ""
      base_image:
        description: "Base image for the dedicated face_swap V2 runtime."
        required: false
        default: "yanwk/comfyui-boot:cu128-slim"
      comfyui_ref:
        description: "Pinned ComfyUI git ref with Flux2 edit nodes."
        required: false
        default: "16cd8d8a8f5f16ce7e5f929fdba9f783990254ea"
      push_image:
        description: "Push the image to GHCR after build and smoke test."
        required: true
        type: boolean
        default: true
      verify_public_pull:
        description: "Verify anonymous GHCR manifest access after push."
        required: true
        type: boolean
        default: true

permissions:
  contents: read
  packages: write

concurrency:
  group: runpod-face-swap-profile-image-${{ github.ref }}
  cancel-in-progress: false

env:
  IMAGE_NAME: allbot-gpu-face-swap
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

jobs:
  build:
    name: Build and publish face_swap profile
    runs-on: ubuntu-latest
    timeout-minutes: 180
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Free runner disk space
        shell: bash
        run: |
          set -euo pipefail
          df -h /
          sudo rm -rf \
            /usr/share/dotnet \
            /usr/local/lib/android \
            /opt/ghc \
            /opt/hostedtoolcache/CodeQL \
            /opt/hostedtoolcache/go \
            /opt/hostedtoolcache/Ruby
          docker system prune -af || true
          df -h /

      - name: Resolve image metadata
        id: meta
        shell: bash
        run: |
          set -euo pipefail
          owner="${GITHUB_REPOSITORY_OWNER,,}"
          tag="${{ inputs.image_tag }}"
          if [[ -z "${tag}" ]]; then
            tag="$(date -u +%Y%m%d)-face-swap-${GITHUB_SHA::7}"
          fi
          image_ref="ghcr.io/${owner}/${IMAGE_NAME}:${tag}"
          echo "image_ref=${image_ref}" >> "$GITHUB_OUTPUT"
          echo "Resolved image: ${image_ref}"

      - name: Login to GHCR
        if: ${{ inputs.push_image }}
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build, smoke test, and optionally push
        shell: bash
        env:
          ALLBOT_GIT_SHA: ${{ github.sha }}
        run: |
          set -euo pipefail
          args=(
            --profile face_swap
            --image-ref "${{ steps.meta.outputs.image_ref }}"
            --base-image "${{ inputs.base_image }}"
            --comfyui-ref "${{ inputs.comfyui_ref }}"
          )
          if [[ "${{ inputs.push_image }}" == "true" ]]; then
            args+=(--push)
          fi
          scripts/build_runpod_profile_image.sh "${args[@]}"

      - name: Verify baked revision labels
        shell: bash
        run: |
          set -euo pipefail
          labels="$(docker image inspect "${{ steps.meta.outputs.image_ref }}" --format '{{json .Config.Labels}}')"
          test "$(jq -r '."org.opencontainers.image.revision"' <<<"$labels")" = "${GITHUB_SHA}"
          test "$(jq -r '."io.allbot.runpod.agent-revision"' <<<"$labels")" = "${GITHUB_SHA}"
          test "$(jq -r '."io.allbot.runpod.workflow-revision"' <<<"$labels")" = "${GITHUB_SHA}"
          test "$(jq -r '."allbot.runpod.profile"' <<<"$labels")" = "face_swap"

      - name: Verify anonymous GHCR manifest access
        if: ${{ inputs.push_image && inputs.verify_public_pull }}
        shell: bash
        run: |
          set -euo pipefail
          tmp_docker_config="$(mktemp -d)"
          trap 'rm -rf "${tmp_docker_config}"' EXIT
          DOCKER_CONFIG="${tmp_docker_config}" docker manifest inspect "${{ steps.meta.outputs.image_ref }}" >/dev/null

      - name: Job summary
        shell: bash
        run: |
          {
            echo "## RunPod face_swap Profile Image"
            echo ""
            echo "- Image: \`${{ steps.meta.outputs.image_ref }}\`"
            echo "- Source SHA: \`${GITHUB_SHA}\`"
            echo "- Task types: \`face_swap,face_swap_v2\`"
            echo "- Workflow: \`face_swap_v2.json\` for both task types"
            echo "- Model manifest: \`face_swap_v2/2026-07-25/manifest.json\`"
          } >> "$GITHUB_STEP_SUMMARY"
