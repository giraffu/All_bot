# 文档更新差异报告

## 修改的文件

### 1. `AGENTS.md`
- **行 7**: 在项目概述中补充了“完善的内容分享与违禁词过滤机制”。
- **行 11**: 在系统架构（交互与处理器）中补充了“公开分享时的违禁词拦截 `FORBIDDEN_WORDS`”的说明。

### 2. `docs/bot_services_and_logic.md`
- **行 66-69**: 新增 `📢 任务结果公开与分享 (Public Share)` 小节，描述了点击“公开”按钮触发的 `FORBIDDEN_WORDS` 违禁词检查（儿童相关词汇拦截）与分享逻辑。
- **行 104**: 修正了动态优先级调度的示例，将“真传弟子”生成超过 100 次衰减更新为最新逻辑中的“生成超过 60 次降级为 0”。

### 3. `docs/cultivation_identity_system.md`
- **行 25-50**: 更新了修为系统（练气期、筑基期、金丹期）的排队优先级衰减阈值：
  - 练气期：<5次 +2，<10次 +1。
  - 筑基期：<5次 +5，<10次 +2。
  - 金丹期：<5次 +10，<10次 +5，<20次 +2。
- **行 74-98**: 更新了身份系统（VIP 特权）的排队优先级衰减阈值：
  - 内门弟子：20/30/40 次分别对应 +15/+8/+3。
  - 核心弟子：30/40/50 次分别对应 +25/+12/+5。
  - 真传弟子：40/50/60 次分别对应 +40/+20/+10。

### 4. `docs/CHANGELOG.md` (新增)
- 新建了变更日志文件，遵循倒序排列。
- 记录了 `1.1.0` 版本中关于违禁词拦截、动态优先级重构、队列VIP显示特权及文档更新的变更明细。

## 验证结论
1. 所有报告与最新的 `src/constants.py` 和 `src/handlers/callback_handler.py` 逻辑完全对齐。
2. 已经过 `markdownlint` 风格测试校验（修复了基本的结构格式对齐）。
3. 因无对外开放 API 变更，暂不需额外输出 `UPGRADE.md`，前端交互兼容。
