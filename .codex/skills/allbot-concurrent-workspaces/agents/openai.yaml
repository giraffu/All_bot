interface:
  display_name: "AllBot 自动并发工作区"
  short_description: "自动抢占 A-H 槽位并分离高访问能力与外部操作授权"
  default_prompt: "Use $allbot-concurrent-workspaces to automatically claim a safe AllBot task slot and implement my request there."
