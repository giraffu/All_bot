# 正式服务云发布环境迁移计划

最后修订：2026-06-06 18:58 CST
适用范围：AllBot 正式控制面从武汉本地主服务器迁移到 DigitalOcean SGP1 云控制面，本地主服务器回归测试、冷归档与 GPU worker 节点。
执行原则：本计划是迁移手册，不是生产发布授权。正式切换必须在用户明确确认低峰维护窗口后执行。

本次修订基于仓库真实代码、Compose、Nginx、部署脚本和只读运行态复核。修订重点是把 R2 历史公网 key、Central 队列门禁、9 个正式 worker 拓扑、支付履约单写、前端存储 URL 和 Telegram Local Bot API 依赖全部写成正式切换硬门禁。

## 1. 总体判断

推荐目标形态仍然是“云正式控制面 + DigitalOcean 托管 PostgreSQL + DigitalOcean 托管 Valkey + Cloudflare R2 + 本地 GPU worker”。这个方向已经通过云测试控制面的基础链路验证：云端 Central API、Web API、Dashboard Backend 与 imgproxy 可以运行在 `allbot-do-sgp1-control-01`，Web 测试入口 `web-test.aivison.it.com` 也已经通过边缘 VPS 反代到云 Web API。

但这份迁移不能按“把测试 compose 改名为 prod”来做。当前代码和运行环境里有几处正式切换门禁：生产数据库 Alembic 尚未到 head，生产任务运行态在复核时仍非空，worker 真实拓扑不是计划中单纯的 7 个本地 worker，生产连接池也远超托管 PostgreSQL 2GB 档的 47 连接额度。迁移方案必须先把这些门禁写进流程，否则云端服务即使启动成功，也可能出现支付履约分叉、任务运行态丢失、历史媒体不可读或数据库连接耗尽。

首发规格保持保守：已购 Droplet `$48/mo`、PostgreSQL 2GB/1vCPU/30GiB、Valkey 1GB/1vCPU/10GiB、R2 `user-data-prod`。升级不作为切换前提，先通过连接池、运行态清空和对象存储分层把风险压住，再观察 3-7 天决定是否扩容。

### 1.1 本轮审核补充结论

按 2026-06-06 的仓库与只读运行态复核，这份计划的目标架构和总体分阶段方向是可行的，但正式执行前必须把下面几项从“注意事项”提升为切换硬门禁：

| 审核项 | 当前事实 | 必须调整 |
| :--- | :--- | :--- |
| 云正式文件 | 仓库里尚无 `deploy/docker-compose-cloud-prod.yml`、`workers/docker-compose-cloud-prod-worker.yml`、`scripts/safe_deploy_cloud_prod.sh`、`scripts/start_cloud_prod_worker.sh`、`scripts/stop_local_prod_entry_preserve.sh` | 先补齐并完成 `config`、健康检查、dry-run，再进入维护窗口 |
| 运行态 | 二次复核时 Web/Bot `active_tasks=36`、`pending_web_finalizers=11`、`user_concurrency_keys=823`；Central `queue_size=24`、9 个 worker 中 8 个 running | 切换当天必须实时归零，不能沿用文档中的旧快照数 |
| 支付冻结 | Web API 的 `/app/MAINTENANCE` 会拦截 Web 订单；但 Bot 侧 `billing_callbacks.py` 创建 RMB/Stars 订单时不检查维护标记 | 正式窗口内不能只依赖维护模式冻结支付；需新增支付冻结开关，或在最终 dump 前停止本地 Bot |
| R2 backfill | `scripts/backfill_history_r2_objects.py` 使用 `MINIO_*` 作为读取源、`R2_*` 作为写入目标；若云正式 `MINIO_*` 已指向 R2，则脚本实际是 R2 根对象到 R2 `history/` 对象的复制 | 明确 backfill 运行模式：源 MinIO 直拷 R2，或先完成底层镜像后在 R2 内回填历史公网 key |
| Compose 工具链 | 本地主服务器只有 `docker-compose 1.29.2`，没有 `docker compose` 插件；云测试脚本依赖 v2，worker 启动脚本已有 fallback | 云正式本地脚本必须兼容 v1；云端脚本执行前必须显式检查 v2 或提供 fallback |
| 明文配置 | 现有部分生产 compose/dashboard compose 中有历史硬编码环境值或默认密钥式配置 | 云正式 compose 禁止复制这些值；所有密钥只来自 `.env.cloud.prod`，日志和验收输出不得打印值 |

因此，推荐的安全路线不是“准备好云端后一次性搬家”，而是：

```text
补齐云正式文件和支付冻结能力
  -> 云库 dry-run restore + Alembic + R2 样本/backfill dry-run
  -> R2 底层对象预镜像和公网 key 小批回填
  -> 云正式控制面预启动但不接公网、不启动 Bot polling
  -> 维护窗口冻结 Web/Bot 新任务和新订单
  -> 等待运行态归零
  -> 停本地写入口和旧 worker
  -> 最终 dump/restore、增量对象同步、增量 backfill
  -> 启云控制面和云正式 worker
  -> 切 Web/Payment 边缘入口
  -> 最后启动云 Bot polling
```

这条路线把用户可见中断主要压缩在“冻结新写入到切边缘入口”的阶段；R2 大对象迁移、云库演练、镜像构建和大部分配置验证都必须提前完成。

## 2. 当前事实基线

以下数据是 2026-06-06 复核得到的迁移前快照。它们只能作为计划依据，正式切换当天必须重新采集，不能拿本页数值直接放行。

### 2.1 本地主服务器与生产数据

生产数据库 `bot_db` 当前约 2613MB，连接状态为 active 2、idle 55、idle in transaction 1。主要体积集中在历史与日志表：`history` 约 168 万行、`user_logs` 约 231 万行、`worker_logs` 约 140 万行、`users` 约 12.5 万行。Redis 当前内存约 61.7MB，峰值约 62.0MB，策略为 `noeviction`。

对象存储不是一个小体量迁移。MinIO 后端目录复核结果约为：

| 桶 | 规模 | 迁移口径 |
| :--- | :--- | :--- |
| `bot-data` | 约 262G，约 394 万文件 | 迁到 R2 正式桶根路径，保持 object key |
| `comfyui-temp` | 约 176G，约 27.6 万文件 | 迁到 R2 正式桶根路径，保持 object key |
| `bot-template` | 约 2.5G | 迁到 R2 正式桶根路径，保留本地副本 |

初步路径集合扫描没有发现 `bot-data` 与 `comfyui-temp` 的同名 key 冲突，但正式迁移前仍必须用 S3 API 或对象清单再扫一遍。不要直接复制 MinIO 后端数据目录到 R2，执行迁移应使用 `mc`、`rclone` 或 boto3 这类 S3 API 工具。

### 2.2 Alembic 与业务表门禁

当前仓库 Alembic head 是 `0f4e7b9c1a23`，本地生产库 current 是 `24b7c9d1e3f5`。待补的两个迁移都属于正式高风险业务：

| revision | 作用 | 迁移风险 |
| :--- | :--- | :--- |
| `7e2a1b4c9d6f` | 新增 `gallery_prompt_unlocks` | 影响 Gallery 提示词付费解锁幂等和灵石转账 |
| `0f4e7b9c1a23` | 新增 `runtime_checkpoints` | 影响 TON 轮询游标，防止重启后回扫旧链上交易 |

正式云库恢复后必须执行 `alembic upgrade head`，并确认 `alembic current` 等于 `0f4e7b9c1a23`。生产切换不能依赖容器启动自动迁移，也不能在 multiple heads 状态下继续。

### 2.3 运行态与 worker 拓扑

复核时生产运行态并不为空：Redis DB1 中 `prod_bot_active_tasks` 为 80、`prod_bot_pending_web_finalizers` 为 21、`prod_bot_user_concurrency:*` 为 856；Central Redis DB2 中 pending 为 69、running 为 9，`/system/status` 队列总量为 65。DB2 中存在大量历史 `comfy:task:*` key，不能把这个 key 总量直接等同于待处理任务，但切换窗口必须同时确认 pending、running、worker current_task 与 Web finalizer 都已经归零。本页记录这些事实不是为了固定这些数，而是强调切换窗口前必须实时清空运行态。

二次只读复核时运行态仍非空，但数量已经变化：Web/Bot `prod_bot_active_tasks=36`、`prod_bot_pending_web_finalizers=11`、`prod_bot_user_concurrency:*` 为 823；Central `/system/status` 显示 `queue_size=24`，9 个 worker 全部 healthy，其中 8 个 running、1 个 idle。这个变化再次说明切换门禁只能看实时读数，不能使用任何一次历史快照放行。

当前 Central API 看到 9 个 worker heartbeat，不只是计划中的 7 个本地 worker，其中包含 `worker_remote_01` 和 `worker_remote_02`。因此 `workers/docker-compose-cloud-prod-worker.yml` 不能简单复制云测试 worker compose。云正式 worker 配置应以当前正式 `workers/docker-compose.yml` 的任务类型和 ComfyUI 端口为事实源，再决定两个 remote worker 是否纳入、下线或单独迁移。

本地主服务器当前还同时运行 `cloud-comfy-agent-test-*` 测试 worker。正式切换窗口前必须停止或限制测试 worker，避免测试任务抢占正式 GPU。`worker_remote_01`、`worker_remote_02` 在仓库 compose 中没有对应定义，迁移前必须单独确认它们的实际宿主、启动方式、任务类型、token 和网络路径。

### 2.4 云测试和边缘入口事实

云测试控制面当前健康检查通过：

```text
http://100.107.220.127:8004/health        -> Central API
http://100.107.220.127:8001/api/health    -> Web API
http://100.107.220.127:8044/api/health    -> Dashboard Backend
https://web-test.aivison.it.com/api/health -> 云测试 Web API
```

正式边缘入口仍在指向本地主服务器。根目录 `all_bot_nginx.conf` 中 `web.aivison.it.com /api/` 当前反代 `100.99.254.53:8000`，`assets.aivison.it.com` 仍代理武汉 MinIO。正式切换必须把边缘 VPS Nginx 纳入执行步骤，而不是只启动云端容器。

## 3. 目标架构

正式入口迁移完成后，请求路径应变成：

```text
用户 / Telegram / Web
  -> Cloudflare DNS / 现有 web VPS
  -> 云正式 Droplet allbot-do-sgp1-control-01
       - cloud-central-api-prod
       - cloud-web-api-prod
       - cloud-payment-api-prod
       - cloud-dashboard-backend-prod
       - cloud-imgproxy-prod
       - cloud-tg-bot-prod
  -> DigitalOcean Managed PostgreSQL bot_db_prod
  -> DigitalOcean Managed Valkey
  -> Cloudflare R2 user-data-prod
  -> Telegram Local Bot API Server 69.63.220.115:8081/8082
  -> Tailscale
  -> 本地主服务器 cloud-prod-comfy-agent-*
  -> 192.168.1.0/24 ComfyUI/GPU 节点
```

本地主服务器在稳定后只保留三类职责：本地隔离测试环境、正式 GPU worker、生产冷归档与灾备副本。它不再承载正式 Bot、Web API、Payment API、Central API、Dashboard 这类写入口。

当前 `src/bot_main.py` 的 PROD/TEST Bot 都固定使用 `http://69.63.220.115:8081/bot` 与 `http://69.63.220.115:8082` 作为 Telegram Local Bot API 和文件入口。云正式 Droplet 启动 `cloud-tg-bot-prod` 前必须验证这两个地址从云端可达；否则文本轮询、图片/视频文件收发或 Telegram 文件下载链路可能在 Bot 启动后才暴露问题。

## 4. 迁移前必须补齐的文件

正式环境必须使用独立命名。云测试文件可以作为参考，但不能直接改名继续跑。

| 文件 | 目的 | 关键要求 |
| :--- | :--- | :--- |
| `.env.cloud.prod` | 云正式环境变量，不提交 | 无重复 key，显式 `BOT_TYPE=PROD` |
| `deploy/docker-compose-cloud-prod.yml` | 云正式控制面 compose | 包含 Central/Web/Payment/Dashboard/imgproxy，Bot 用 profile |
| `scripts/safe_deploy_cloud_prod.sh` | 云正式部署脚本 | 校验 env、单 head、迁移、健康检查，不默认启动 Bot |
| `workers/docker-compose-cloud-prod-worker.yml` | 本地正式 worker 指向云 Central/R2 | 从当前正式 worker 口径派生，不照抄云测试 |
| `scripts/start_cloud_prod_worker.sh` | 启动云正式 worker | 先测云 Central、R2 S3、实际 env，再启动 |
| `scripts/stop_local_prod_entry_preserve.sh` | 停止本地正式入口但保留数据 | 停入口和旧 worker，不删除 DB/Redis/MinIO |
| `all_bot_nginx_cloud_prod.conf` 或边缘 VPS 备份配置 | 正式边缘切换模板 | Web API、Payment API、Dashboard API 明确 upstream |
| `R2 历史 backfill 执行清单` | 让历史结果公网 URL 可命中 | 使用/扩展 `scripts/backfill_history_r2_objects.py`，覆盖 Web owner `/result`、Gallery 与缩略图 |
| `frontend/.env.production` | 正式前端构建变量 | `VITE_STORAGE_URL` 切到正式 R2 公网域名或改为 API 返回全 URL |

当前仓库尚未存在上述 cloud-prod 文件。补文件时注意两条实际约束：

1. 本地主服务器只有 `docker-compose 1.29.2`，没有 `docker compose` 插件；所有在本地主服务器执行的正式 worker、停止本地入口、对象迁移辅助脚本必须像 `scripts/start_cloud_worker_test.sh` 一样支持 v1 fallback。云 Droplet 上执行的脚本可以优先使用 `docker compose`，但脚本开头必须检查版本并给出明确错误。
2. 不要从现有 `backend/docker-compose.yml`、`dashboard/docker-compose.yml` 复制明文环境值或默认密钥。云正式 compose 只声明变量契约，密钥全部来自 `.env.cloud.prod`，并在 `docker compose config` 后额外检查没有把敏感值写入仓库文件。

这里最容易遗漏的是 `AUTH_TOKEN` 与 `API_TOKEN`。Central API 读取 `AUTH_TOKEN`，Bot/Web 调用 Central 时读取 `API_TOKEN`，云正式必须保证二者一致，否则健康检查正常但提交任务会 401。`AGENT_SECRET_TOKEN` 也必须同时给 Central 和 worker，切换后要用 worker 心跳验证认证链路。

## 5. 云正式环境变量口径

`.env.cloud.prod` 只存非提交文件，值不进入 Git。下面只列变量契约，不列密钥。

```bash
BOT_TYPE=PROD
REDIS_PREFIX=prod_bot_
CLOUD_PROD_BIND_IP=100.107.220.127
CLOUD_PROD_TAILSCALE_IP=100.107.220.127

DATABASE_URL=postgresql+asyncpg://<user>:<password>@<do-pg-host>:25060/bot_db_prod?ssl=require
CLOUD_PROD_DATABASE_URL=<same as DATABASE_URL>
CLOUD_PROD_DATABASE_URL_SYNC=postgresql://<user>:<password>@<do-pg-host>:25060/bot_db_prod?sslmode=require

REDIS_URL=rediss://default:<password>@<do-valkey-host>:25061/1
WORKER_REDIS_URL=rediss://default:<password>@<do-valkey-host>:25061/2
CLOUD_PROD_REDIS_URL=<same as REDIS_URL>
CLOUD_PROD_WORKER_REDIS_URL=<same as WORKER_REDIS_URL>

API_BASE=http://central-api-prod:8003
API_TOKEN=<same value as AUTH_TOKEN>
AUTH_TOKEN=<same value as API_TOKEN>
AGENT_SECRET_TOKEN=<shared worker auth token>
```

R2 使用项目内兼容的 `MINIO_*` 变量直连 S3 endpoint，同时保留 `R2_*` 用于公网 URL 和存在性探测：

```bash
MINIO_ENDPOINT=<R2 S3 endpoint host>
MINIO_ACCESS_KEY=<R2 token access key>
MINIO_SECRET_KEY=<R2 token secret key>
MINIO_BUCKET=user-data-prod
MINIO_INPUT_BUCKET=user-data-prod
MINIO_RESULT_BUCKET=user-data-prod
MINIO_TEMPLATE_BUCKET=user-data-prod
MINIO_SECURE=true
MINIO_PUBLIC_URL=

R2_ENDPOINT=https://<R2 S3 endpoint host>
R2_ACCESS_KEY=<R2 token access key>
R2_SECRET_KEY=<R2 token secret key>
R2_BUCKET=user-data-prod
R2_PUBLIC_DOMAIN=https://r2.aivison.it.com
```

支付和 Web 入口必须明确为正式域名：

```bash
PAYMENT_API_PORT=8021
HUANYUY_NOTIFY_URL=https://rmb.aivison.it.com/api/pay/notify/huanyuy
HUANYUY_RETURN_URL=https://rmb.aivison.it.com/pay/result
MINI_APP_URL=https://web.aivison.it.com/
WEBAPP_URL=https://pay.aivison.it.com/
TON_PAYMENT_POLLING_ENABLED=true
```

Bot 入口还必须显式验证当前硬编码 Telegram Local Bot API 依赖：

```bash
set -a
source .env.cloud.prod
set +a

python - <<'PY'
import json
import os
import urllib.request

token = os.environ["BOT_TOKEN"]
with urllib.request.urlopen(
    f"http://69.63.220.115:8081/bot{token}/getMe",
    timeout=10,
) as response:
    payload = json.load(response)
    print("bot_api_ok=", bool(payload.get("ok")))

with urllib.request.urlopen("http://69.63.220.115:8082/", timeout=10) as response:
    print("file_api_http_status=", response.status)
PY
```

若云端无法访问，需要先改造 `src/bot_main.py` 让 `TELEGRAM_API_BASE_URL` / file base URL 可由环境变量覆盖，或保证云 Droplet 到该 VPS 的网络放行。

若正式切换窗口需要临时暂停 TON 轮询，必须写成单独操作决定，并在云库 `runtime_checkpoints` 验证通过后再开启。默认生产语义是开启。

## 6. 连接池预算

DigitalOcean 托管 PostgreSQL 2GB 档连接额度按 47 条口径规划。现有本地生产 `web-api` 连接池过大，云正式必须显式压缩。

| 服务 | 进程数 | `DB_POOL_SIZE` | `DB_MAX_OVERFLOW` | 估算最大连接 |
| :--- | ---: | ---: | ---: | ---: |
| Web API | 4 | 2 | 1 | 12 |
| Bot | 1 | 3 | 2 | 5 |
| Payment API | 1 | 2 | 1 | 3 |
| Dashboard Backend | 1 | 1 | 0 | 1 |
| 迁移、监控、临时排障预留 | - | - | - | 10-15 |

这让常态应用连接约 21 条，总体目标低于 35。Dashboard 只有少量管理员使用，可以牺牲一点响应速度，避免占用生产连接。若 Web API 高峰出现数据库等待，先看慢查询、前端重复轮询、SSE/result 访问模式，再把 Web 调到 `DB_POOL_SIZE=3`、`DB_MAX_OVERFLOW=1`。只有总连接长期超过 35 或数据库 CPU/IO 成为瓶颈时，再升级到 4GB/2vCPU/60GiB。

## 7. R2 与历史媒体策略

首发采用单桶 `user-data-prod`，但对象迁移要分成两层，不能只写“保持 object key”：

| 层级 | 目的 | key 口径 |
| :--- | :--- | :--- |
| 底层 S3 兼容镜像 | 让 `resolve_storage_object()`、预签名读写、worker 输入输出仍能按旧相对路径找到对象 | `bot-data/<object key>` 源桶说明不进入目标 key，目标为 `user-data-prod/<object key>` |
| 历史公网 R2 回填 | 让 Web owner `/result`、历史详情、Gallery、缩略图能命中公开 URL | 优先生成 `history/{task_id}/original.<ext>` 与 `history/{task_id}/thumb.<ext>`，无 `task_id` 时才退回 legacy basename |

当前代码决定了这两层都必须存在。`src/core/media_paths.py` 会把旧 `bot-data/`、`comfyui-temp/` 前缀解析成当前 `MINIO_BUCKET/MINIO_RESULT_BUCKET`；`src/core/media_urls.py` 与 `scripts/backfill_history_r2_objects.py` 则会为历史记录构造 `history/{task_id}/original.ext` 和缩略图 key。Web owner `/result` 对 `source=web` 的视频没有存储短签 fallback，R2 公网 URL 不存在时会继续返回 `pending_result`。因此，单纯把三个 MinIO 桶 `mc mirror` 到 R2 只能保证 S3 层可读，不能保证历史公开视频结果可读。

`scripts/backfill_history_r2_objects.py` 的环境变量语义必须提前定稿。它当前用 `MINIO_*` 作为读取源，用 `R2_*` 作为写入目标：

| 模式 | `MINIO_*` 指向 | `R2_*` 指向 | 适用时机 |
| :--- | :--- | :--- | :--- |
| 源 MinIO 直拷 R2 | 武汉 MinIO 旧桶 | R2 `user-data-prod` | 底层镜像尚未完全完成，但源 MinIO 到执行机网络稳定 |
| R2 根对象回填历史公网 key | R2 S3 endpoint 与 `user-data-prod` | 同一个 R2 `user-data-prod` | 已完成底层对象镜像后，复制 `object_key` 到 `history/{task_id}/original.ext` 与 thumb |

云正式在线配置会让 `MINIO_*` 指向 R2，因此正式切换窗口内推荐使用第二种模式。第一种模式适合切换前 dry-run 或预回填，但必须使用临时 env 文件，不得覆盖 `.env.cloud.prod` 的在线口径。

R2 CORS 必须允许正式 Web 直传：

```json
[
  {
    "AllowedOrigins": [
      "https://web.aivison.it.com",
      "https://web-test.aivison.it.com"
    ],
    "AllowedMethods": ["GET", "PUT", "HEAD"],
    "AllowedHeaders": ["*"],
    "ExposeHeaders": ["ETag"],
    "MaxAgeSeconds": 3600
  }
]
```

迁移执行时先做镜像，不做删除。底层对象镜像可以使用 `mc`、`rclone` 或 boto3 S3 API：

```text
bot-data/<object key>      -> user-data-prod/<object key>
comfyui-temp/<object key>  -> user-data-prod/<object key>
bot-template/<object key>  -> user-data-prod/<object key>
```

这里的 `bot-data/`、`comfyui-temp/`、`bot-template/` 是源桶名说明，不是目标 key 前缀。正式镜像前必须用对象清单或 S3 API 再扫一次同名 key 冲突。

历史公网 URL 回填必须单独执行。推荐先小批 dry-run，再全量 apply：

```bash
set -a
source .env.cloud.prod
export BOT_TYPE=PROD
set +a

# 小批验证：不上传，只输出应上传的 history/original 与 thumb key
python scripts/backfill_history_r2_objects.py --limit 100

# 优先验证 Web 视频，因为 /result 对视频最依赖 R2 公网 URL
python scripts/backfill_history_r2_objects.py --source web --media-type video --limit 100

# 全量执行前先按 source/media-type 分批；正式 apply 前必须确认本次采用的是哪种 backfill 模式
python scripts/backfill_history_r2_objects.py --source web --media-type video --apply
python scripts/backfill_history_r2_objects.py --source web --media-type image --apply
python scripts/backfill_history_r2_objects.py --source bot --media-type all --apply
```

R2 成为在线事实源后，再把关键对象定时镜像回本地 MinIO 归档桶。真正清理 R2 冷数据前必须补归档索引，否则旧历史详情、广场 apply-context、视频播放和缩略图都可能出现“数据库有记录但对象不可读”。

推荐归档索引最少记录：

```text
object_key
storage_tier=hot|archived|rehydrating|missing
hot_bucket=user-data-prod
archive_bucket=<local-minio-archive-bucket>
size_bytes
checksum_or_etag
archived_at
last_accessed_at
```

前端有一个额外门禁：`frontend/.env.production` 目前仍配置 `VITE_STORAGE_URL=https://assets.aivison.it.com`，而 `frontend/src/utils/storageUrl.ts` 对相对路径仍会拼旧桶名前缀。正式切换前必须二选一：

| 方案 | 动作 | 验收重点 |
| :--- | :--- | :--- |
| API 全量返回可读 URL | 保证历史、任务结果、Gallery、Wan22 链条返回完整 R2 URL | 前端不再依赖 `buildStorageFileUrl()` 拼旧路径 |
| 前端适配单桶 R2 | 修改 `VITE_STORAGE_URL` 和路径拼接逻辑 | 相对路径、旧 `bot-data/`、旧 `comfyui-temp/` 都可打开 |

无论选哪条，验收都要覆盖 Web owner `/result`、历史详情、Gallery apply-context、Wan22 链条预览、缩略图和 Bot 结果消息。

推荐首发优先选择“API 全量返回可读 URL”。这样前端生产包可以暂时少改，风险集中在后端 R2 URL 解析与 backfill 验收；待云正式稳定后再清理 `buildStorageFileUrl()` 对旧 `assets.aivison.it.com` 的依赖。

## 8. 支付与账本迁移重点

`payment-api` 是正式迁移的一等服务，不能因为云测试曾移除 `payment-api-test` 就在生产省略。正式 `deploy/docker-compose-cloud-prod.yml` 必须包含 `payment-api-prod`，监听 `8021`，并由 `rmb.aivison.it.com` 反代。

支付迁移的核心不是“回调接口能访问”，而是履约只写云库。切换后旧本地 Payment API 必须停止接收正式回调；`HUANYUY_NOTIFY_URL` 和 `HUANYUY_RETURN_URL` 必须指向云端域名；RMB 回调重复通知必须仍由订单幂等锚点收口。TON 轮询游标持久化在云库 `runtime_checkpoints`，不能因为 Valkey 空启动而误以为 TON 状态也为空。

正式维护窗口开始后必须冻结新支付创建，至少让 Web/Bot 的套餐购买入口进入维护态；同时先停止旧本地 Payment API 或让 `rmb.aivison.it.com` 临时返回维护页，再执行最终数据库 dump。否则窗口内新订单可能在旧库创建、云库恢复后不存在，或者回调命中旧 Payment API 造成履约分叉。已经创建但尚未回调的订单要在最终 dump 前列出清单，切换后通过订单号确认只在云库完成一次履约。

当前代码里的维护标记覆盖并不完整：

| 入口 | 当前维护标记效果 | 迁移要求 |
| :--- | :--- | :--- |
| Web `/api/payment/orders`、`/api/payment/ton-orders` | `src/web_api/main.py` 的 `MaintenanceMiddleware` 会返回 503 | 可以用 `/app/MAINTENANCE` 冻结 |
| Bot RMB/Stars 购买 callback | `src/handlers/callbacks/billing_callbacks.py` 不检查 `is_maintenance_mode()` | 不能靠维护标记冻结 |
| RMB 支付回调 | `payment-api` 独立服务，不读 `/app/MAINTENANCE` | 必须停旧服务或把 `rmb.aivison.it.com` 切到维护页/云端 |

因此正式切换前建议先补一个支付冻结开关，例如 `PAYMENT_CREATION_DISABLED=true`，同时覆盖 Web 订单创建、Bot RMB/Stars 订单创建和菜单提示。若不改代码，维护窗口开始后必须尽早停止本地 `tg-bot`，并在最终 dump 前确认 `orders` 表没有新的 `PENDING` 订单继续产生。

支付验收建议用低额真实链路：

1. Web 创建 RMB 订单，支付链接域名为正式域名。
2. 支付成功后 `rmb.aivison.it.com/api/pay/notify/huanyuy` 命中云 Payment API。
3. 云 `bot_db_prod.orders`、`user_logs`、会员/灵石字段更新。
4. 本地 `bot_db.orders` 不再新增履约记录。
5. TON 轮询不回扫旧交易，`runtime_checkpoints` 正常持久化。

## 9. 分阶段迁移方案

### 阶段 0：冻结执行口径

这一阶段不停止正式服务，只把迁移输入变成可验证的文件和清单。先确认云正式命名、端口、域名、worker 数量、R2 正式桶、支付域名、Dashboard 暴露方式、Telegram Local Bot API 可达性。若 `worker_remote_01`、`worker_remote_02` 仍在正式服务中接任务，必须在这里决定是否纳入云正式 worker compose；如果不纳入，必须明确哪些任务类型会由本地 7 个 worker 覆盖，哪些能力会临时下线。

验收条件是：新增云正式 compose/env/script 通过 compose 配置渲染检查，`.env.cloud.prod` 无重复 key，`API_TOKEN/AUTH_TOKEN` 成对一致，R2 CORS 已配置，R2 backfill 小批 dry-run 通过，边缘 VPS 有原配置备份，`69.63.220.115:8081/8082` 从云 Droplet 可达或已有环境变量化改造方案。

compose 配置渲染检查必须按执行地点选择命令：

```bash
# 云 Droplet，优先要求 Docker Compose v2
docker compose --env-file .env.cloud.prod -f deploy/docker-compose-cloud-prod.yml config

# 本地主服务器，当前只有 docker-compose 1.29.2
docker-compose --env-file .env.cloud.prod -f workers/docker-compose-cloud-prod-worker.yml config
```

### 阶段 1：云正式基础设施准备

创建 `bot_db_prod`，使用已购 PostgreSQL 2GB 档；Valkey 使用 DB1 作为 Bot/Web 运行态，DB2 作为 Central 队列/worker 运行态；R2 创建 `user-data-prod`，不复用测试桶。

云正式服务端口建议：

| 服务 | 容器名 | 云 Tailscale 端口 | 说明 |
| :--- | :--- | :--- | :--- |
| Central API | `cloud-central-api-prod` | `8003` | 本地正式 worker 访问 |
| Web API | `cloud-web-api-prod` | `8000` | 边缘 VPS `/api/` 回源 |
| Payment API | `cloud-payment-api-prod` | `8021` | `rmb.aivison.it.com` 回源 |
| Dashboard Backend | `cloud-dashboard-backend-prod` | `8043` | 管理后台 API |
| imgproxy | `cloud-imgproxy-prod` | `8084` | 图片代理 |
| Telegram Bot | `cloud-tg-bot-prod` | 无 | profile 管理，默认不启动 |

这一阶段可以启动控制面以外的只读验证服务，但不能启动云正式 Bot，不能切公网入口。

### 阶段 2：生产数据干跑

干跑可以在本地正式服务运行时进行，用于验证恢复、迁移和读路径。数据库导出用一致性 dump，恢复到云 `bot_db_prod` 或专门 dry-run 库：

```bash
mkdir -p backups/cloud-prod-dryrun
docker exec postgres-server pg_dump -U postgres -d bot_db -Fc \
  > backups/cloud-prod-dryrun/bot_db_prod_dryrun.dump

pg_restore \
  --clean --if-exists --no-owner --no-privileges \
  --dbname "$CLOUD_PROD_DATABASE_URL_SYNC" \
  backups/cloud-prod-dryrun/bot_db_prod_dryrun.dump
```

恢复后在云库执行：

```bash
BOT_TYPE=PROD alembic heads
BOT_TYPE=PROD alembic upgrade head
BOT_TYPE=PROD alembic current
```

对象存储干跑使用 R2 正式桶或临时 dry-run 桶镜像样本和一批大文件。底层 S3 镜像 key 必须保持原样：

```text
bot-data/<object key>      -> user-data-prod/<object key>
comfyui-temp/<object key>  -> user-data-prod/<object key>
bot-template/<object key>  -> user-data-prod/<object key>
```

这里的 `bot-data/`、`comfyui-temp/` 是源桶名说明，不是目标 key 前缀。

同时必须执行历史公网 R2 backfill 小批验证，确认 `scripts/backfill_history_r2_objects.py` 能从恢复后的云库收集候选，并能对源 MinIO/R2 目标做 `HEAD`：

```bash
set -a
source .env.cloud.prod
export BOT_TYPE=PROD
set +a

python scripts/backfill_history_r2_objects.py --source web --media-type video --limit 100
python scripts/backfill_history_r2_objects.py --source web --media-type image --limit 100
```

验收条件是：核心表行数与本地一致，Alembic 到 head，R2 样本对象 `HEAD` 成功，Web API 可以生成 R2 预签名 PUT/GET，公开 R2 URL 可以被浏览器读取，`history/{task_id}/original.ext` 和缩略图小批回填可命中，Gallery apply-context 能恢复输入图。

### 阶段 3：云正式控制面预启动

启动 `central-api-prod`、`web-api-prod`、`payment-api-prod`、`dashboard-backend-prod`、`imgproxy-prod`。仍然不启动 `cloud-tg-bot-prod`，也不切 `web.aivison.it.com` 和 `rmb.aivison.it.com`。

云正式 Bot 在这一阶段只做配置和网络探测，不进入 polling。重点验证 `BOT_TOKEN` 存在但不打印、`TON_PAYMENT_POLLING_ENABLED` 符合生产策略、云端可访问 Telegram Local Bot API `69.63.220.115:8081/8082`。

预启动验收：

```bash
curl -fsS http://100.107.220.127:8003/health
curl -fsS http://100.107.220.127:8000/api/health
curl -fsS http://100.107.220.127:8021/pay/result
curl -fsS http://100.107.220.127:8043/api/health
```

同时检查云容器实际环境变量，输出时不得打印密钥值：

```text
BOT_TYPE=PROD
REDIS_PREFIX=prod_bot_
DB_POOL_SIZE/DB_MAX_OVERFLOW 符合预算
MINIO_BUCKET=user-data-prod
MINIO_RESULT_BUCKET=user-data-prod
MINIO_SECURE=true
R2_PUBLIC_DOMAIN=https://r2.aivison.it.com
```

### 阶段 4：边缘入口预切换

在边缘 VPS 准备正式 upstream，但先不 reload 到正式用户流量。正式配置至少包含：

```nginx
location /api/ {
    proxy_pass http://100.107.220.127:8000;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header Connection '';
    proxy_http_version 1.1;
    chunked_transfer_encoding off;
    proxy_buffering off;
    proxy_cache off;
}
```

`proxy_pass` 不要加尾部斜杠，避免路径改写。`rmb.aivison.it.com` 必须反代 `http://100.107.220.127:8021`，Dashboard API 如需公网访问，建议放在独立域名并加 Cloudflare Access 或等价访问控制。

### 阶段 5：正式切换窗口

切换建议低峰执行，窗口预留 60-90 分钟。真正用户不可写的时间应尽量压缩到最终 dump/restore、增量对象同步、边缘 reload 和 Bot 切换这一段。开始前先发布维护公告，再开启本地正式维护模式和支付冻结，阻止新任务和新支付订单进入。不要直接跑完整 `safe_deploy.sh`，因为它只监控部分 Bot/Web 运行态且后续会重建本地生产服务；这里只复用它的维护和队列门禁思想。

推荐窗口内顺序：

1. 开启 Web 维护标记，开启支付冻结开关；如果尚未实现支付冻结开关，则停止本地 `tg-bot`，避免 Bot callback 继续创建订单。
2. 暂停或停止 `cloud-comfy-agent-test-*`，避免测试 worker 抢正式 GPU。
3. 等待 Bot/Web DB1、Web finalizer、Central DB2、worker `current_task` 全部归零。
4. 停止旧本地写入口：`tg-bot`、`web-api`、`payment-api`、Dashboard、`backend_api_1`。若 worker 已归零，再停止旧 `comfy-agent-*`。
5. 导出未回调订单清单，执行最终 `pg_dump`/`pg_restore` 到云正式库。
6. 在云正式库执行 `alembic upgrade head`，确认 `alembic current` 是 `0f4e7b9c1a23`。
7. 执行 R2 底层对象增量同步，再按已选模式执行历史公网 key 增量 backfill。
8. 启动云正式控制面，不启动 Bot；启动本地 `cloud-prod-comfy-agent-*`。
9. 验证云端健康、DB 连接数、R2 预签名、`/system/workers` 数量和任务类型。
10. reload 边缘 VPS Nginx，把 Web/Payment API 切到云端，确认正式域名命中云。
11. 最后启动 `cloud-tg-bot-prod`，确认无 Telegram polling conflict。

运行态清空必须同时看 Bot/Web Redis DB1、Web finalizer 和 Central Redis DB2。生产 Redis 有认证时，优先通过容器内环境变量或应用脚本查询，不要在文档或日志中打印密码。下面命令只表达检查口径：

```bash
# Redis DB1：Bot/Web 运行态
redis-cli -n 1 hlen prod_bot_active_tasks
redis-cli -n 1 hlen prod_bot_pending_web_finalizers
redis-cli -n 1 --scan --pattern 'prod_bot_user_concurrency:*' | wc -l

# Redis DB2：Central 队列/worker 运行态
redis-cli -n 2 zcard comfy:queue:pending
redis-cli -n 2 scard comfy:queue:running
redis-cli -n 2 --scan --pattern 'comfy:task:*' | wc -l
curl -fsS http://127.0.0.1:8003/system/status
curl -fsS http://127.0.0.1:8003/system/workers
```

放行条件不是“差不多没任务”，而是 active、pending、running、current_task、pending finalizer 都为 0 或已经有明确人工处理结论。确认后停止本地正式入口和旧 worker：

```text
tg-bot
web-api
payment-api
dashboard backend/frontend
backend_api_1
comfy-agent-*
cloud-comfy-agent-test-*（若仍在占用正式 GPU）
```

最终同步完成后启动云正式控制面、本地云正式 worker，确认 `/system/workers` 数量、健康状态和任务类型与迁移决策一致。最后 reload 边缘 Nginx，确认 Web/Payment 域名命中云端，再启动 `cloud-tg-bot-prod`。这个顺序可以避免 Telegram 双实例和支付回调双写。

### 阶段 6：正式业务验收

切换后第一轮验收围绕真实业务闭环，不只看健康检查：

| 场景 | 必须看到的结果 |
| :--- | :--- |
| Telegram Bot | 无 polling conflict，能响应正式用户，文件收发依赖的 Local Bot API 可用 |
| Web 登录 | JWT 正常，用户资料和余额来自云库 |
| 参考图上传 | 浏览器 R2 PUT 成功，CORS 预检 204/200 |
| 图片任务 | 提交、SSE、完成、历史落库、结果可读 |
| 视频任务 | 99% 后能等到 R2 公网 URL，不提前失败或长期 `pending_result` |
| Gallery apply-context | 输入图、prompt、时长、分辨率恢复正确 |
| 历史媒体 | `history/{task_id}/original.ext` 和缩略图公网 URL 可读 |
| 提示词解锁 | 扣买家 1 灵石，作者入账，重复解锁幂等 |
| RMB 支付 | 回调写入云库，本地库不再新增履约 |
| TON 轮询 | checkpoint 正常，不回扫旧交易 |
| Dashboard | 用户、订单、任务、worker 状态可查 |

验收期间保留本地生产 DB/Redis/MinIO，不删除、不清空。本地只作为回滚源和冷归档源。

### 阶段 7：稳定运行与本地降级

云正式稳定 24-72 小时后，本地主服务器降级为测试环境、正式 GPU worker 和冷归档节点。本地测试 worker 默认只开 1-2 个，避免测试任务抢正式 GPU。旧 `tg-bot`、`web-api`、`payment-api`、`backend_api_1` 不再作为正式入口运行。

## 10. 回滚策略

入口切换前，回滚非常简单：停止云预启动服务即可，本地正式服务仍是事实源。

入口切换后 0-2 小时内，如果云 Web API、Bot 或 worker 明显不可用，可以短窗口回滚，但要先停止云 Bot，避免 Telegram 双实例；再把边缘 Nginx 切回武汉，启动本地入口和旧 worker。此时必须检查云库是否已有新订单、新 history、新灵石流水。若已有真实支付履约，不能简单丢弃，必须人工决定反向同步还是继续修云。

超过 2 小时或已经发生真实支付履约后，不建议做简单回滚。此时云库已经是事实源，正确做法通常是修复云环境，或先导出云端增量再制定回迁方案。

## 11. 风险与处理

| 风险 | 表现 | 预防或处理 |
| :--- | :--- | :--- |
| Alembic 未到 head | 新功能表缺失，TON checkpoint 不存在 | 切换前 dry-run 恢复并 `upgrade head`，最终恢复后再次执行 |
| Bot 双实例 | Telegram polling conflict | 先停本地 Bot，再启动云 Bot |
| Payment API 双写 | 支付到账写入旧库或两边分叉 | 切 rmb 域名前停旧 Payment API，验收云库新增记录 |
| 维护窗口新订单分叉 | 订单在旧库创建，回调在云库找不到或重复履约 | 开窗后冻结购买入口，列出未回调订单清单，最终 dump 后只允许云 Payment API 接回调 |
| Bot 端支付未冻结 | Web 已维护但 Bot callback 仍创建新订单 | 补 `PAYMENT_CREATION_DISABLED` 类开关，或最终 dump 前先停本地 Bot |
| Redis 运行态搬迁 | 僵尸任务、重复退款、锁不释放 | 不迁移 Redis，维护窗口清空 DB1/DB2 运行态 |
| worker 拓扑漏迁 | 部分任务一直 pending | 以 `/system/workers` 真实拓扑生成云正式 worker compose |
| PostgreSQL 连接耗尽 | Web/Bot 卡顿、500 | 按 21 条应用连接预算启动，Dashboard 1 worker |
| R2 底层 key 错误 | apply-context 或存储短签读不到旧对象 | 单桶根路径，保持旧 object key，不额外加源桶名前缀，先做冲突扫描 |
| R2 历史公网 key 未回填 | Web 视频 `/result` 长期 `pending_result`，缩略图缺失 | 执行 `scripts/backfill_history_r2_objects.py`，验证 `history/{task_id}/original.ext` 与 thumb |
| 前端仍拼旧存储域名 | Wan22 预览、历史图片走 `assets` 旧域名 | 更新 `VITE_STORAGE_URL` 或确保 API 返回完整 URL |
| Telegram Local Bot API 不通 | Bot 启动后文件收发、轮询或下载异常 | 云端启动 Bot 前验证 `69.63.220.115:8081/8082`，必要时先环境变量化 |
| 误用 `safe_deploy.sh` | 本地生产被重建，队列门禁不完整 | 正式迁移使用云专用脚本，只复用维护/队列思想，不直接跑全量生产部署 |
| R2 读取操作费高于预期 | Class B 成本上升 | 监控 R2 Class B，热门公开资源走缓存域名 |
| 本地测试抢正式 GPU | 正式任务排队变长 | 测试 worker 默认 1-2 个，压测避开高峰 |

## 12. 执行门禁清单

正式切换前，以下全部满足才允许进入维护窗口：

```text
[ ] 用户明确确认正式切换窗口
[ ] .env.cloud.prod 无重复 key，BOT_TYPE=PROD
[ ] API_TOKEN 与 AUTH_TOKEN 一致
[ ] AGENT_SECRET_TOKEN 在 Central 和 worker 中一致
[ ] deploy/docker-compose-cloud-prod.yml、workers/docker-compose-cloud-prod-worker.yml、scripts/safe_deploy_cloud_prod.sh、scripts/start_cloud_prod_worker.sh 均已补齐并通过语法校验
[ ] 云端 docker compose v2 可用，或 safe_deploy_cloud_prod.sh 已提供 fallback
[ ] 本地主服务器 worker/停入口脚本兼容 docker-compose 1.29.2
[ ] cloud-prod compose config 渲染通过，且没有把密钥写入仓库文件
[ ] Alembic heads 只有 1 个
[ ] 云库 dry-run restore + upgrade head 成功
[ ] R2 user-data-prod CORS 配置完成
[ ] R2 样本 PUT/HEAD/GET 和公网 URL 验证成功
[ ] R2 底层对象镜像已完成样本校验，无源桶同名 key 冲突
[ ] R2 backfill 运行模式已确认：源 MinIO 直拷 R2，或 R2 根对象回填 history key
[ ] `scripts/backfill_history_r2_objects.py` dry-run 已覆盖 Web 视频、Web 图片、Bot 历史样本
[ ] 历史 R2 公网 key `history/{task_id}/original.ext` 与 thumb 小批 apply 验证成功
[ ] 前端生产存储 URL/路径策略已验收
[ ] worker_remote_01/02 是否迁移已有结论
[ ] `cloud-comfy-agent-test-*` 停止或限流方案已确认，测试 worker 不抢正式 GPU
[ ] 云端可访问 Telegram Local Bot API `69.63.220.115:8081/8082`，或 Bot API URL 已完成环境变量化
[ ] 维护窗口新支付订单冻结方案已确认，且覆盖 Web 订单创建和 Bot RMB/Stars callback
[ ] 未回调订单清单已导出
[ ] 边缘 VPS Nginx 新配置已备份并 nginx -t 通过
[ ] 本地生产运行态实时清空：DB1 active/finalizer/concurrency、DB2 pending/running、worker current_task 均为 0 或已有人工处理结论
[ ] 本地生产 DB/Redis/MinIO 备份和快照保留
```

切换后 30 分钟内必须完成：

```text
[ ] web.aivison.it.com/api/health 命中云 Web API
[ ] rmb.aivison.it.com/pay/result 命中云 Payment API
[ ] cloud-tg-bot-prod 无 polling conflict
[ ] /system/workers 中正式 worker 数量和类型符合预期
[ ] `cloud-comfy-agent-test-*` 未接入正式 Central
[ ] 图片任务和视频任务完整完成
[ ] Web owner 视频 `/result` 返回 R2 公网 URL，不停留在 `pending_result`
[ ] 历史详情、Gallery apply-context、Wan22 链条预览和缩略图均可读
[ ] RMB 小额订单到账并只写云库
[ ] 本地 `bot_db.orders` 在切换后不再新增正式履约记录
[ ] TON `runtime_checkpoints` 正常写入且不回扫旧交易
[ ] PostgreSQL 连接数低于 35，峰值不超过 40
[ ] Valkey 无 eviction，内存明显低于 60%
```

## 13. 成本与扩容观察

已购首发成本口径：

| 项目 | 月费估算 | 说明 |
| :--- | ---: | :--- |
| Droplet 8GB/4vCPU/160GB/5TB | `$48.00` | 云控制面 |
| PostgreSQL 2GB/1vCPU/30GiB | `$30.45` | 首发正式库 |
| Valkey 1GB/1vCPU/10GiB | `$15.00` | 正式缓存和队列 |
| R2 约 440GB Standard | 约 `$6.60` 存储费 | 另计 Class A/B 操作费 |

R2 不能只看存储费。Cloudflare R2 Standard 当前口径为 `$0.015/GB-month`，Class A `$4.50/million`，Class B `$0.36/million`，公网 egress free。大量历史图片和视频访问会产生 Class B 费用，正式切换后要观察 R2 metrics，必要时对公开热门资源加缓存策略。

扩容触发条件：

| 资源 | 首发策略 | 升级触发 |
| :--- | :--- | :--- |
| Droplet | 继续 `$48/mo` | CPU 持续 >70%、内存持续 >75%、Web P95 明显升高 |
| PostgreSQL | 继续 2GB 档 | 总连接长期 >35、CPU 持续 >70%、慢查询或连接等待明显 |
| Valkey | 继续 1GB 档 | 内存持续 >60%、出现 eviction、队列或 heartbeat 延迟 |
| 第二台应用节点 | 暂不采购 | 单 Droplet 故障点不可接受或需要蓝绿发布 |

## 14. 明确不做

本次迁移不把武汉家庭内网 `192.168.1.0/24` 暴露成 Tailscale subnet router；不复用 `user-data-test` 作为正式桶；不迁移旧 Redis 运行态；不把 `mc mirror` 当成历史公网媒体迁移的全部工作；不直接执行完整 `safe_deploy.sh` 作为云迁移手段；不在未验收前删除本地生产数据；不让旧本地 Payment API 和云 Payment API 同时接正式回调；不在未验证 Telegram Local Bot API 可达前启动云正式 Bot polling；不把云测试 Bot 与正式 Bot 共用 token；不把托管 PostgreSQL 或 Valkey 开放公网。
