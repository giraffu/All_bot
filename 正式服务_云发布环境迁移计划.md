# 正式服务云发布环境迁移计划

生成时间：2026-06-06 14:08:28 CST  
目标形态：云上作为正式发布环境，本地主服务器回归测试环境，本地 7 张 GPU 继续作为正式算力池。

## 1. 结论

推荐采用“云正式控制面 + DigitalOcean 托管 PostgreSQL + DigitalOcean 托管 Valkey + Cloudflare R2 + 本地 GPU worker”的形态。

当前 DigitalOcean SGP1 `$48/mo` Droplet 可以作为第一阶段正式控制面承载 Bot、Web API、Payment API、Central API、Dashboard Backend 与 imgproxy。PostgreSQL 首发固定使用已购买的 2GB/1vCPU/30GiB 档，Valkey 首发固定使用已购买的 1GB/1vCPU/10GiB 档；容器版 Redis 不再作为云正式或云测试事实源。代价是必须把应用连接池按 PostgreSQL 47 连接额度重新收紧，并把 Valkey 队列/锁/心跳保持在低内存占用口径；Dashboard 只服务 2 个管理用户且可容忍卡顿，应降到最低连接池。

云上的现有测试环境可以被覆盖，但不要直接把 `cloud-test` 命名改成生产继续跑。应该新建云正式 compose/env/script，停止并清理云测试容器后，用正式命名启动，避免后续本地测试与云正式互相污染。

## 2. 当前事实基线

### 2.1 本地生产数据快照

2026-06-06 14:08 CST 重新采集结果：

| 项目 | 当前值 |
| :--- | :--- |
| 生产数据库 `bot_db` | 2610MB |
| PostgreSQL 当前连接 | active 1，idle 56 |
| `history` | 约 169.96 万行，1582MB |
| `user_logs` | 约 244.33 万行，467MB |
| `worker_logs` | 约 147.45 万行，352MB |
| `users` | 约 12.50 万行，80MB |
| Redis 当前内存 | 59.42MB，峰值 60.98MB |
| Redis keyspace | db1 113124，db2 24821，db3 191，db4 34 |
| MinIO 本地数据 | 约 430GB |
| 高峰活跃任务 | 用户补充：200-250 |

### 2.2 已验证云测试事实

| 项目 | 当前状态 |
| :--- | :--- |
| 云 Droplet | `allbot-do-sgp1-control-01`，SGP1，4 vCPU / 8GB / 160GB / 5TB transfer |
| Tailscale IP | `100.107.220.127` |
| 云测试 PostgreSQL | `allbot-pg-sgp1-01`，PostgreSQL 18，当前 2GB / 1vCPU / 30GiB |
| 云测试 Valkey | `allbot-valkey-sgp1-01`，Valkey 8，当前 1GB / 1vCPU / 10GiB |
| 云测试对象存储 | R2 `user-data-test`，已配置 Web 直传 CORS |
| 云测试 Web 入口 | `web-test.aivison.it.com` 静态站在 `web` VPS，`/api/` 反代云 Web API |
| 云测试核心容器 | Central API、Web API、Dashboard Backend、imgproxy、bot-test |
| 云测试已移除 | 容器 Postgres、容器 Redis、容器 MinIO、前端容器、payment-api-test |

### 2.3 官方价格依据

| 服务 | 当前官方价格口径 | 用途 |
| :--- | :--- | :--- |
| DigitalOcean Droplet 8GB/4vCPU/160GB/5TB | `$48/mo`，包含带宽 | 已购云控制面 |
| DigitalOcean PostgreSQL 2GB/1vCPU/30GiB | `$30.45/mo` | 已购，正式首发使用，需严格控制连接池 |
| DigitalOcean PostgreSQL 4GB/2vCPU/60GiB | `$60.90/mo` | 后续升级档，触发条件见扩容规则 |
| DigitalOcean Valkey 1GB/1vCPU/10GiB | `$15/mo` | 已购，正式首发缓存规格 |
| DigitalOcean Valkey 2GB/1vCPU | `$30/mo` | 后续升级档，触发条件见扩容规则 |
| Cloudflare R2 Standard | `$0.015/GB-month`，Class A `$4.50/million`，Class B `$0.36/million`，公网 egress free | 正式媒体对象存储 |

参考：
- DigitalOcean Droplets: https://www.digitalocean.com/products/droplets
- DigitalOcean Managed Databases pricing: https://www.digitalocean.com/pricing/managed-databases
- Cloudflare R2 pricing: https://developers.cloudflare.com/r2/pricing/

## 3. 推荐采购与保留资源

### 3.1 立即采购/调整

| 资源 | 推荐动作 | 理由 |
| :--- | :--- | :--- |
| Droplet `allbot-do-sgp1-control-01` | 继续使用 `$48/mo` | 托管 DB/Valkey/R2 后，应用控制面内存压力可控 |
| PostgreSQL | 继续使用已购 `allbot-pg-sgp1-01` 2GB/1vCPU/30GiB | 首发先通过连接池治理压进 47 连接额度，避免立即增加固定成本 |
| Valkey | 继续使用已购 `allbot-valkey-sgp1-01` 1GB/1vCPU/10GiB | 当前 Redis 运行内存约 60MB，1GB 首发有充足空间；通过不迁移运行态和清空队列降低切换风险 |
| R2 | 新建 `user-data-prod` 正式桶 | 不复用 `user-data-test`，避免测试对象和正式对象混淆 |
| Cloudflare DNS/Tunnel/VPS | 继续使用现有 `web` VPS 作为边缘静态站和反代 | 现有模式已在测试 Web 验证，避免再引入 Cloudflare Tunnel 变量 |

### 3.2 可延后

| 资源 | 延后条件 | 升级触发 |
| :--- | :--- | :--- |
| Droplet 升级到 `$96/mo` 8vCPU/16GB | 首发先观察 3-7 天 | CPU 持续 >70%、内存持续 >75%、Web API P95 明显升高 |
| PostgreSQL 升级到 4GB/2vCPU/60GiB | 首发先使用已购 2GB 档 | 总连接长期 >35、DB CPU 持续 >70%、连接等待导致 Web/Bot 卡顿、慢查询明显增加 |
| PostgreSQL standby | 首发可不买 | 正式流水稳定后，为减少维护窗口和故障恢复时间再加 |
| Valkey 升级到 2GB/1vCPU | 首发先使用已购 1GB 档 | Valkey 内存持续 >60%、出现 eviction、队列/心跳延迟明显或高峰任务状态更新卡顿 |
| 第二台应用 Droplet | 首发可不买 | 单 Droplet 成为故障点不可接受，或需要蓝绿部署 |
| 云 GPU | 不建议常驻 | 只在本地队列长时间积压、临时活动峰值时弹性启用 |

## 4. 目标架构

```text
用户 / Telegram / Web
  -> Cloudflare DNS / 现有 web VPS
  -> 云正式 Droplet allbot-do-sgp1-control-01
       - cloud-tg-bot-prod
       - cloud-web-api-prod
       - cloud-payment-api-prod
       - cloud-central-api-prod
       - cloud-dashboard-backend-prod
       - cloud-imgproxy-prod
  -> DigitalOcean Managed PostgreSQL bot_db_prod
  -> DigitalOcean Managed Valkey
  -> Cloudflare R2 user-data-prod
  -> Tailscale
  -> 武汉本地主服务器 cloud-prod-comfy-agent-1..7
  -> 192.168.1.0/24 ComfyUI/GPU 节点
```

本地主服务器切换后角色：

```text
本地主服务器 hfy-FAEX9
  - 保留本地测试栈：tg-bot-test / web-api-test / payment-api-test / central-api-test / dashboard-test
  - 保留本地测试数据库、Redis、MinIO 测试桶
  - 运行正式 GPU worker，但 worker 指向云正式 Central API 和 R2 正式桶
  - 不再承载正式 Bot/Web/Payment/API 数据入口
```

## 5. 服务迁移映射

| 当前正式服务 | 当前位置 | 云正式目标 | 迁移动作 |
| :--- | :--- | :--- | :--- |
| `tg-bot` | 本地主服务器 | `cloud-tg-bot-prod` | 最终切换时先停止本地 Bot，再启动云 Bot，避免 Telegram token 冲突 |
| `web-api` | 本地主服务器 `8000` | `cloud-web-api-prod` | `web.aivison.it.com /api/` 反代切到云端 |
| `payment-api` | 本地主服务器 | `cloud-payment-api-prod` | `rmb.aivison.it.com` 回调/结果页必须切到云端，不能遗漏 |
| `backend_api_1` Central API | 本地主服务器 `8003` | `cloud-central-api-prod` | 绑定云 Tailscale IP，只给本地正式 worker 访问 |
| `dashboard-backend` | 本地主服务器 `8043` | `cloud-dashboard-backend-prod` | Dashboard 前端/API 反代切云端 |
| `dashboard-frontend` | 本地主服务器/边缘 | 保持边缘静态或云端容器 | 推荐继续静态站边缘部署，API 指向云端 |
| `imgproxy` | 本地主服务器 | `cloud-imgproxy-prod` | 跟随 Web API 迁云 |
| `postgres-server` | 本地主服务器 | DO Managed PostgreSQL | 正式库 `bot_db_prod` |
| `redis-server` | 本地主服务器 | DO Managed Valkey | 不迁移运行态，切换窗口清空队列后从空状态启动 |
| `minio-server` | 本地主服务器 | R2 `user-data-prod` + 本地 MinIO 冷归档 | R2 替代主在线 MinIO；本地 MinIO 转为测试、冷归档和灾备 |
| `comfy-agent-1..7` | 本地主服务器 | `cloud-prod-comfy-agent-1..7` | 容器仍在本地，`MASTER_API_URL` 改为云 Central API |

## 6. 必须新增/改造的文件

不要直接复用云测试文件名。建议新增：

| 文件 | 用途 |
| :--- | :--- |
| `.env.cloud.prod` | 云正式环境变量，不提交 |
| `deploy/docker-compose-cloud-prod.yml` | 云正式控制面 compose |
| `scripts/safe_deploy_cloud_prod.sh` | 云正式部署脚本 |
| `workers/docker-compose-cloud-prod-worker.yml` | 本地正式 GPU worker 指向云 Central/R2 |
| `scripts/start_cloud_prod_worker.sh` | 本地启动云正式 worker |
| `scripts/stop_local_prod_entry_preserve.sh` | 本地停止正式入口但保留测试/数据 |

云测试文件可以保留作参考；正式切换成功后，云端 `cloud-*test*` 容器、volume、`.env.cloud.test` 可删除或归档。

## 7. 云正式环境变量口径

### 7.1 核心环境

```bash
BOT_TYPE=PROD
REDIS_PREFIX=prod_bot_
CLOUD_PROD_BIND_IP=100.107.220.127
CLOUD_PROD_TAILSCALE_IP=100.107.220.127
DATABASE_URL=postgresql+asyncpg://<user>:<password>@<do-pg-host>:25060/bot_db_prod?ssl=require
REDIS_URL=rediss://default:<password>@<do-valkey-host>:25061/1
WORKER_REDIS_URL=rediss://default:<password>@<do-valkey-host>:25061/2
API_BASE=http://central-api-prod:8003
```

### 7.2 数据库连接池建议

首发使用已购 PostgreSQL 2GB/1vCPU/30GiB，连接上限口径为 47。目标是应用侧最大连接控制在 25-32 之间，保留 10 条以上给迁移、Dashboard 临时查询、监控和故障排查。

| 服务 | workers | `DB_POOL_SIZE` | `DB_MAX_OVERFLOW` | 估算最大连接 |
| :--- | ---: | ---: | ---: | ---: |
| Web API | 4 | 2 | 1 | 12 |
| Bot | 1 | 3 | 2 | 5 |
| Payment API | 1 | 2 | 1 | 3 |
| Dashboard Backend | 1 | 1 | 0 | 1 |
| 预留/迁移/维护/监控 | - | - | - | 10-15 |

计划内应用最大连接约 21；加上预留后仍应低于 35，留出 47 连接额度中的安全余量。Dashboard Backend 建议只保留 1 个 gunicorn worker，`DB_POOL_SIZE=1`、`DB_MAX_OVERFLOW=0`，管理员页面慢一点可以接受，不应让后台空闲连接挤占 Web/Bot/Payment 的正式额度。

若 Web API 在高峰出现明显数据库等待，再按以下顺序微调：

1. 先确认慢查询、接口轮询频率和前端重复请求，不直接加连接。
2. Web API 调到 `DB_POOL_SIZE=3`、`DB_MAX_OVERFLOW=1`，4 workers 最大 16，整体仍约 25。
3. 若总连接长期超过 35 或 CPU/IO 成为瓶颈，再升级 PostgreSQL 到 4GB/2vCPU/60GiB。

云正式 compose 建议显式写入：

```yaml
web-api-prod:
  environment:
    DB_POOL_SIZE: 2
    DB_MAX_OVERFLOW: 1
  command: ["uvicorn", "src.web_api.main:app", "--host", "0.0.0.0", "--port", "8000", "--workers", "4"]

bot-prod:
  environment:
    DB_POOL_SIZE: 3
    DB_MAX_OVERFLOW: 2

payment-api-prod:
  environment:
    DB_POOL_SIZE: 2
    DB_MAX_OVERFLOW: 1

dashboard-backend-prod:
  environment:
    DB_POOL_SIZE: 1
    DB_MAX_OVERFLOW: 0
  command: ["gunicorn", "dashboard.backend.main:app", "-w", "1", "-k", "uvicorn.workers.UvicornWorker", "--bind", "0.0.0.0:8043"]
```

### 7.3 R2 正式桶与本地冷归档

首发建议单桶：

```bash
MINIO_ENDPOINT=<R2 S3 endpoint host>
MINIO_BUCKET=user-data-prod
MINIO_INPUT_BUCKET=user-data-prod
MINIO_RESULT_BUCKET=user-data-prod
MINIO_TEMPLATE_BUCKET=user-data-prod
MINIO_SECURE=true
MINIO_PUBLIC_URL=
R2_BUCKET=user-data-prod
R2_PUBLIC_DOMAIN=<正式资产域名，未验证前留空>
```

首发单桶的原因：当前测试已经验证单桶可闭环，且历史 `output_file` 与 `resolve_storage_object()` 对桶名有兼容逻辑。正式迁移时不额外加旧桶名前缀，除非同步改代码和历史路径解析。

R2 CORS 必须包含：

```json
[
  {
    "AllowedOrigins": [
      "https://web.aivison.it.com",
      "https://web-test.aivison.it.com"
    ],
    "AllowedMethods": ["GET", "PUT", "HEAD"],
    "AllowedHeaders": ["*"],
    "ExposeHeaders": ["ETag"],
    "MaxAgeSeconds": 3600
  }
]
```

正式迁移后的长期存储分层建议：

| 层级 | 存储 | 保存内容 | 访问方式 |
| :--- | :--- | :--- | :--- |
| 热在线层 | R2 `user-data-prod` | 新上传输入、近期结果、Web 直传文件、广场公开内容、缩略图、模板资源 | 云 API 直接生成 R2 预签名 URL 或公网 R2 URL |
| 冷归档层 | 本地主 MinIO 生产归档桶 | 超过保留期的私有历史原图/原视频、大体积低频结果 | 默认不直接公网访问；按需回源/回填到 R2 |
| 本地测试层 | 本地 MinIO 测试桶 | 本地主服务器测试环境数据 | 仅测试环境使用 |
| 灾备层 | 本地主 MinIO 或另一台 MinIO | R2 关键对象副本、迁移备份 | 手动恢复或定时校验 |

这个方向可行，但有三个边界：

1. R2 可以替代当前主 MinIO 的“在线事实源”，尤其是 Web 上传、worker 输入/结果、公开结果分发。
2. 本地 MinIO 可以转为冷归档，从而降低 R2 常驻数据量。
3. 已从 R2 删除的冷数据不能继续假装在线可读，必须有“归档标记 + 回填/回源策略”，否则旧历史详情、广场应用上下文或视频播放会找不到对象。

推荐保留策略：

| 数据类型 | R2 保留 | 冷归档 |
| :--- | :--- | :--- |
| 近 30-90 天用户历史结果 | 保留 | 同步副本可选 |
| 广场公开内容、已投稿模板、缩略图 | 长期保留 | 同步副本 |
| Web 参考图和任务输入 | 至少保留到任务完成后 30 天 | 超期归档 |
| 私有历史原视频/原图 | 超过保留期可转冷 | 本地 MinIO 长期保存 |
| 模板资源 `bot-template` | 建议 R2 长期保留 | 本地 MinIO 副本 |
| 测试对象 | 不进入 `user-data-prod` | 本地测试桶或 `user-data-test` |

实施时不要立刻删除 R2 老对象。第一阶段先做“双写/镜像”：新对象写 R2，同时定时镜像到本地冷归档桶；确认本地归档对象 `HEAD` 成功并记录归档状态后，再按保留期删除 R2 中的冷对象。

后续如果要真正减少 R2 存量，需要补一个归档索引。推荐在数据库或独立清单中记录：

```text
object_key
storage_tier=hot|archived|rehydrating|missing
hot_bucket=user-data-prod
archive_bucket=<local-minio-archive-bucket>
archived_at
last_accessed_at
size_bytes
checksum/etag
```

读取策略建议：

```text
请求历史结果
  -> 先查 R2 hot 对象
  -> 命中：直接返回 R2 URL
  -> 未命中但归档存在：返回 archived/pending_restore，并后台从本地 MinIO 回填到 R2
  -> 回填完成：下一轮请求返回 R2 URL
```

不建议让公网用户直接从武汉本地 MinIO 拉冷数据。家庭宽带、跨境链路和签名 URL 代理都会让大视频体验不稳定；更稳的是“按需从本地 MinIO 回填到 R2，再让用户从 R2 读取”。

## 8. 分阶段迁移计划

### 阶段 0：冻结方案与备份云测试

目标：允许覆盖云测试，但保留一次可追溯快照。

1. 记录云测试当前容器状态、`.env.cloud.test` 非敏感摘要。
2. 导出 `bot_db_test` 为备份，保留 7 天即可。
3. 停止云测试 Bot 与控制面。
4. 删除或停止 `cloud-*test*` 容器。
5. 保留 `user-data-test` R2 桶作为以后本地测试对象桶；不要在正式服务中复用。

验收：
- 云端没有运行 `cloud-tg-bot-test`。
- 云端 8001/8004/8044 测试端口不再被旧测试容器占用。
- 本地主服务器测试环境仍可通过 `safe_deploy_test.sh` 恢复。

### 阶段 1：云正式基础设施准备

目标：先把正式基础设施准备好，但不接用户流量。

1. 继续使用已购 PostgreSQL 2GB/1vCPU/30GiB，并创建正式库 `bot_db_prod`。
2. 使用已购 Managed Valkey 1GB/1vCPU/10GiB，限制网络来源为云 Droplet/VPC。
3. 创建 R2 正式桶 `user-data-prod`，配置 CORS。
4. 准备 `.env.cloud.prod`。
5. 准备 `deploy/docker-compose-cloud-prod.yml`，包含：
   - `central-api-prod`
   - `web-api-prod`
   - `payment-api-prod`
   - `dashboard-backend-prod`
   - `imgproxy-prod`
   - `bot-prod` profile，默认不自动启动
6. 在 compose 中写入 2GB PostgreSQL 连接池预算：Web API 12、Bot 5、Payment 3、Dashboard 1。
7. 准备 `workers/docker-compose-cloud-prod-worker.yml`。

验收：
- 云端能连接 PostgreSQL/Valkey/R2。
- `docker compose -f deploy/docker-compose-cloud-prod.yml config` 通过。
- 不启动云正式 Bot，不影响本地正式服务。

### 阶段 2：正式数据干跑迁移

目标：在不影响本地正式服务的情况下，把生产数据恢复到云正式库做演练。

数据库：

```bash
mkdir -p backups/cloud-prod-dryrun
docker exec postgres-server pg_dump -U postgres -d bot_db -Fc \
  > backups/cloud-prod-dryrun/bot_db_prod_dryrun.dump

pg_restore \
  --clean --if-exists --no-owner --no-privileges \
  --dbname "$CLOUD_PROD_DATABASE_URL_SYNC" \
  backups/cloud-prod-dryrun/bot_db_prod_dryrun.dump
```

R2 与本地冷归档：

```bash
# 伪命令，实际用 mc/rclone/boto3，重点是保持对象 key，不加旧桶名前缀
mirror /home/hfy/APP/minio-deploy/data/bot-data        -> r2/user-data-prod
mirror /home/hfy/APP/minio-deploy/data/comfyui-temp    -> r2/user-data-prod
mirror /home/hfy/APP/minio-deploy/data/bot-template    -> r2/user-data-prod

# R2 成为在线事实源后，再把冷数据镜像/迁回本地主 MinIO 归档桶
mirror r2/user-data-prod/<cold-private-history-prefix>  -> minio/archive-prod
```

Valkey/Redis 运行态：
- 不迁移生产 Redis 运行态。
- 容器版 Redis 不再作为云端事实源。
- 切换前通过维护模式等活跃任务清空；云 Valkey 从空状态启动。
- 这样避免把旧 pending/running/heartbeat 搬到新 Central API 后形成僵尸任务。

验收：
- 云 `bot_db_prod` 行数和本地生产库一致。
- Alembic `heads` 只有 1 个，`upgrade head` 成功。
- R2 样本对象 HEAD 成功，Web API 可返回预签名 GET。
- 本地冷归档桶样本对象 HEAD 成功；冷对象在 R2 删除前必须已经在归档桶校验成功。
- 云 Web API / Dashboard Backend 只做内网健康检查，不接公网用户。

### 阶段 3：云正式控制面预启动

目标：除 Bot 和公网入口外，云正式控制面全部启动并自检。

1. 运行 `scripts/safe_deploy_cloud_prod.sh`。
2. 启动：
   - `cloud-central-api-prod`
   - `cloud-web-api-prod`
   - `cloud-payment-api-prod`
   - `cloud-dashboard-backend-prod`
   - `cloud-imgproxy-prod`
3. 不启动 `cloud-tg-bot-prod`。
4. 本地准备但不启动 `cloud-prod-comfy-agent-*`。
5. 用 SSH/Tailscale 内部访问验证：

```bash
curl -fsS http://100.107.220.127:8003/health
curl -fsS http://100.107.220.127:8000/api/health
curl -fsS http://100.107.220.127:8043/api/health
```

验收：
- 云端服务健康检查通过。
- 云端日志无 `Traceback`、`401`、数据库连接耗尽。
- Dashboard 登录可用。
- Payment API `/pay/result` 和回调入口存在，但还未切正式域名。

### 阶段 4：边缘入口预切换

目标：准备 DNS/VPS 反代，但不立即切正式用户。

1. `web` VPS Nginx 新增云正式 upstream：
   - `web.aivison.it.com /api/` -> `http://100.107.220.127:8000`
   - `rmb.aivison.it.com` -> `http://100.107.220.127:<payment-port>`
   - Dashboard API -> `http://100.107.220.127:8043`
2. 保留原本地主服务器 upstream 备份。
3. `nginx -t`，暂不 reload 或只灰度到测试 host。
4. 生产前端静态站仍发布到现有 `web` VPS。

验收：
- 可以通过临时 host/header 命中云 API。
- 不影响现有正式用户流量。

### 阶段 5：正式切换窗口

目标：把正式入口从本地主服务器迁到云上。

建议低峰执行，窗口 30-60 分钟。

1. 开启本地正式维护模式，阻止新任务：

```bash
./safe_deploy.sh 的维护模式逻辑可复用，但不要执行完整发布
```

2. 等待生产队列清空：
   - active tasks = 0
   - Central pending queue = 0
   - running set = 0
   - worker 正在执行任务结束
3. 停止本地正式入口：
   - `tg-bot`
   - `web-api`
   - `payment-api`
   - `dashboard-backend/frontend` 正式入口
   - `backend_api_1`
4. 停止本地正式 worker `comfy-agent-1..7`。
5. 做最终数据库 dump/restore 到 `bot_db_prod`。
6. 做最终 MinIO -> R2 增量同步。
7. 启动云正式控制面。
8. 启动本地 `cloud-prod-comfy-agent-1..7`，指向云 Central API。
9. 切换 VPS Nginx / Cloudflare DNS：
   - `web.aivison.it.com /api/` 指向云 Web API
   - `rmb.aivison.it.com` 指向云 Payment API
   - Dashboard 指向云 Dashboard Backend
10. 启动 `cloud-tg-bot-prod`。

验收：
- Telegram Bot 无 polling conflict。
- Web 可登录、上传参考图、提交任务。
- RMB 支付链接、返回页、回调入口都在云 Payment API。
- `/system/workers` 能看到 7 个正式 worker heartbeat。
- 提交 1 个图片任务、1 个视频任务，完整走通。
- 生产数据库新订单/新 history 写入云 `bot_db_prod`。

### 阶段 6：本地主服务器转测试环境

目标：云正式稳定后，把本地主服务器明确降级为测试/算力节点。

1. 本地主服务器保留：
   - `safe_deploy_test.sh`
   - `.env.test`
   - `bot_db_test`
   - Redis 测试 DB
   - MinIO 测试桶
2. 本地不再运行正式入口服务。
3. 本地正式 worker 只运行 `cloud-prod-comfy-agent-*`，不要再运行旧 `comfy-agent-*`。
4. 测试 worker 默认不要 7 个全开；建议测试只开 1-2 个，避免抢正式 GPU。
5. 测试 Bot 使用测试 token，正式 Bot 只在云端运行。

验收：
- 本地 `docker ps` 中没有 `tg-bot`、`web-api`、`payment-api` 正式入口。
- 本地测试栈可按需启动。
- 云上正式服务持续稳定 24-72 小时。

## 9. 支付迁移重点

`payment-api` 必须纳入云正式控制面。测试环境之前移除了 `payment-api-test`，但正式迁移不能省略。

必须确认：

1. `HUANYUY_NOTIFY_URL` 指向云 Payment API 的正式域名。
2. `HUANYUY_RETURN_URL` 指向正式返回页。
3. `rmb.aivison.it.com` 反代到云 Payment API。
4. 支付回调幂等逻辑在云正式库 `bot_db_prod` 生效。
5. 切换后不要让旧本地 Payment API 继续接收正式回调。
6. TON 轮询/回调 checkpoint 不得因为云端空 Valkey 或测试配置误扫旧交易。

验收方式：
- 创建小额测试订单。
- 支付返回页显示正确。
- 回调写入云正式库。
- 用户灵石/会员到账。
- 本地生产库不再新增订单履约记录。

## 10. 风险与应对

| 风险 | 影响 | 应对 |
| :--- | :--- | :--- |
| Telegram Bot 双实例 | polling conflict，Bot 无法响应 | 切换前停止本地 `tg-bot`，再启动云 `cloud-tg-bot-prod` |
| Payment API 漏迁 | 用户能打开支付链接但不到账 | 正式 compose 必须包含 `payment-api-prod`，并切 `rmb` 域名 |
| PostgreSQL 连接耗尽 | Web/Bot 卡顿或 500 | 首发按 2GB 连接预算压缩连接池；Dashboard 1 worker/1 连接；长期 >35 连接再升级 4GB |
| Redis 运行态搬迁产生僵尸任务 | worker 拉取旧任务或状态错乱 | 不迁移 Redis；切换前维护模式等待队列清空 |
| R2 对象路径错误 | 历史结果无法访问 | 生产 R2 单桶首发，迁移时保持 object key，不额外加桶名前缀 |
| 过早删除 R2 冷对象 | 旧历史结果不可访问 | 先建立归档索引和回填机制，再按保留期删除 R2 |
| 公网直读本地冷 MinIO | 大视频慢、断连、跨境不稳定 | 冷数据按需回填到 R2 后再给用户读取 |
| R2 CORS 漏配 | Web 参考图上传失败 | `user-data-prod` 配置和测试桶一致的 CORS |
| 本地测试抢正式 GPU | 高峰正式任务排队变长 | 本地测试 worker 默认只开 1-2 个，压测需避开高峰 |
| DNS/VPS 切换后回滚复杂 | 新写入数据分叉 | 切换窗口短；一旦云端产生真实支付/任务，回滚需做反向数据同步 |

## 11. 回滚策略

### 11.1 DNS/入口切换前

直接停止云正式预启动服务即可。本地正式服务仍在运行，无需数据回滚。

### 11.2 切换后 0-2 小时内

适合处理云 API 明显不可用、Bot 不响应、worker 心跳失败等问题。

1. 停止云 `cloud-tg-bot-prod`，避免 Bot 双实例。
2. VPS Nginx / Cloudflare 入口切回本地主服务器。
3. 启动本地 `tg-bot`、`web-api`、`payment-api`、`backend_api_1`。
4. 停止 `cloud-prod-comfy-agent-*`。
5. 启动旧 `comfy-agent-1..7`。
6. 若云端已有新订单/新 history，必须人工决定：
   - 丢弃切换窗口写入，还是
   - 反向同步到本地生产库。

### 11.3 切换超过 2 小时或已有真实支付履约

不建议简单回滚。此时云库已经成为事实源，需要先导出云端增量数据，再决定回迁或修复云环境。

## 12. 验收清单

### 技术验收

- 云 Web API `/api/health` 正常。
- 云 Central API `/health` 正常。
- 云 Dashboard API `/api/health` 正常。
- 云 Payment API 返回页和 notify 入口正常。
- PostgreSQL 连接数稳定低于 35，峰值不超过 40。
- Valkey 内存低于 60%，无 eviction；若持续超过 60% 再升级 2GB。
- R2 `PUT` CORS preflight 对正式 Web 域名返回 204/200。
- 7 个正式 worker heartbeat 在线。
- 图片任务、视频任务、参考图上传、历史结果访问完整通过。

### 业务验收

- Telegram Bot 可正常响应。
- Web 登录、余额、任务提交、历史记录正常。
- RMB 支付创建、回调、到账正常。
- 会员/灵石/affiliate 账本无异常。
- Dashboard 能查看用户、任务、订单、worker 状态。

## 13. 首发成本估算

### 已购托管数据库/缓存首发

| 项目 | 月费估算 |
| :--- | ---: |
| Droplet 4vCPU/8GB | `$48.00` |
| PostgreSQL 2GB/1vCPU/30GiB | `$30.45` |
| Managed Valkey 1GB/1vCPU/10GiB | `$15.00` |
| R2 430GB 标准存储 | 约 `$6.45`，另有操作费用；后续冷归档后可下降 |
| 合计 | 约 `$99.90/mo` + R2 操作费用 |

### 后续升级预算参考

| 项目 | 月费估算 |
| :--- | ---: |
| PostgreSQL 升级到 4GB/2vCPU/60GiB | `$60.90` |
| Managed Valkey 升级到 2GB/1vCPU | `$30.00` |

首发不再按“更省缓存试运行”处理，而是正式采用已购 Valkey 1GB。主要控制点是：切换时不迁移旧 Redis 运行态、清空 pending/running 队列、观察 Valkey 内存与 eviction；只有触发扩容规则时再升 2GB。

## 14. 建议执行顺序

1. 创建 `user-data-prod` R2 桶并配置 CORS。
2. 使用已购 PostgreSQL 2GB/1vCPU/30GiB 创建 `bot_db_prod`。
3. 使用已购 Managed Valkey 1GB/1vCPU/10GiB，并确认 Trusted sources 只允许云 Droplet/VPC。
4. 在本地主 MinIO 准备正式冷归档桶，例如 `archive-prod`，只允许内网/运维访问。
5. 编写并验证 `.env.cloud.prod`、`docker-compose-cloud-prod.yml`、`safe_deploy_cloud_prod.sh`。
6. 做一次生产 DB + MinIO 到云端的干跑迁移。
7. 做一次 R2 -> 本地冷归档桶镜像校验，确认对象 key、大小、etag/checksum 规则。
8. 云端预启动正式控制面，但不启动 Bot、不切公网。
9. 准备本地 `docker-compose-cloud-prod-worker.yml`。
10. 低峰维护窗口执行最终 dump/restore、R2 增量、入口切换、启动云 Bot 和正式 worker。
11. 连续观察 24-72 小时。
12. 本地主服务器正式入口退役，只保留本地测试栈、云正式 GPU worker 和本地冷归档服务。

## 15. 明确不做

- 不把武汉家庭内网 `192.168.1.0/24` 作为 Tailscale subnet router 暴露给云端。
- 不让正式服务使用 `user-data-test`。
- 不让云测试 Bot 和正式 Bot 同时使用同一个 Telegram token。
- 不把 PostgreSQL/Valkey 直接开放公网。
- 不在用户未验收前删除本地生产数据。
- 不把 Redis 旧运行态直接搬到云端正式环境。
