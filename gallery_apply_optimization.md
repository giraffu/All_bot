# 画廊一键应用 (Apply) 逻辑架构优化方案

## 1. 背景与问题描述

在当前的画廊社区模块中，前端调用 `GET /api/gallery/posts/{post_id}/apply-context` 接口来获取“一键应用”所需的 `prompt` 和 `lora` 参数。
目前该接口不仅负责读取参数，还同时执行了以下写入操作：
1. 往 `UserInteraction` 表插入 `action_type="apply"` 的记录。
2. 累加对应 `GalleryPost` 的 `applied_count`（被应用次数）。

**存在的问题**：
- **并发与报错**：由于 SQLAlchemy 的 `autoflush` 机制，如果用户对同一个作品多次点击“一键应用”，第二次请求会因为 `UserInteraction` 的联合唯一约束 (`uix_user_post_action`) 提前抛出 `IntegrityError`，导致 500 报错。
- **业务逻辑错位**：真正的“一键应用”生成过程是会扣除灵石的，而当前的 `GET` 接口是免费的。在此处统计热度，实际上统计的是“点开看看”的次数，而非真正花钱生成的次数，容易被恶意刷数据，且数据业务价值较低。

## 2. 优化方案：将“应用统计”与“真正消耗”绑定（架构最优）

既然生成才扣除灵石，就不应该在“获取参数”的无成本阶段增加作品的热度。我们将统计逻辑后置到真正的生成环节。

### 2.1 优化逻辑

1. **纯净读取 (解耦 GET 接口)**
   - 在当前的 `get_apply_context` 接口中，**完全移除**写入 `UserInteraction` 和增加 `applied_count` 的逻辑。
   - 该接口彻底变为纯净的读接口，仅负责鉴权并返回 `prompt` 和 `lora` 等参数，消除任何因并发或重复点击导致的数据库报错隐患。

2. **真实统计 (绑定生成接口)**
   - **前端改造**：在前端获取到参数后，当用户确认并调用真正的“AI生成接口”时，需要在请求体中附带一个 `source_post_id` 参数。
   - **后端改造**：在后端生成接口（如 `POST /api/generate/...`）处理逻辑中，在**成功扣除用户灵石**并将生成任务成功加入队列后，根据传入的 `source_post_id`：
     - 为对应的 `GalleryPost.applied_count` 执行原子 +1 操作。
     - （可选）记录一条关联日志或 interaction。

### 2.2 方案优势

- **系统稳定性提升**：彻底根除当前 `GET` 接口的并发与 `IntegrityError` 报错问题。
- **防刷机制天然形成**：与灵石消耗机制强绑定，杜绝了白嫖接口刷作品热度的可能性。
- **数据价值最大化**：作品的 `applied_count` 统计口径变更为**真正花钱生成过的次数**，能够极其精准地反映出哪些模板是真正受用户欢迎且愿意为其付费的，为后续的社区热度排序和创作者分成提供坚实的数据支撑。
