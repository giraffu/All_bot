# 代码清理方案

## 1. 目标

本方案面向当前仓库的全局代码清理与结构收敛，重点覆盖以下问题：

- 可抽取为公共逻辑的重复代码
- 已失效、未引用或明显腐坏的死代码
- 历史遗留的一次性脚本、兼容补丁、实验性垃圾代码
- 分层不清、入口分散、长期易漂移的架构问题

本方案不是单纯“删代码”，而是按风险和收益排序，先止血，再收敛，再重构。

## 2. 扫描范围

- 后端核心：`src/`、`backend/`、`workers/`
- Dashboard：`dashboard/backend/`、`dashboard/frontend/`
- Web 前端：`frontend/`
- 部署与运维：`deploy/`、`scripts/`、`safe_deploy.sh`、`safe_deploy_test.sh`
- 文档：`docs/`

## 3. 总体结论

当前仓库的主要问题不是单点 bug，而是“历史叠加”导致的系统性膨胀：

1. 双前端、双认证、双 API 基建并行演化，基础设施没有收口。
2. 任务提交、恢复、清理、补偿存在多套实现，生命周期边界不统一。
3. 部署脚本、compose 拆得过散，生产/测试逻辑高度复制，文档已明显漂移。
4. 仓库中保留了多份一次性运维脚本、前端实验脚本、遗留兼容逻辑和废弃链路。
5. 文档中已有较多过时说明，继续放大会直接误导后续研发和运维。

如果现在不做一次系统清理，后续新增功能会继续把重复逻辑固化成新的基础设施负担。

## 4. 量化快照

以下数据用于帮助排序，不代表全部都是必须删除的垃圾代码：

- 发现 `TODO/FIXME/deprecated/legacy` 类标记共约 `282` 处，分布于 `53` 个文件。
- 发现一次性用户合并脚本 `4` 个：`scripts/merge_user_*.py`。
- 发现分散的 compose 文件 `8` 个：覆盖 `backend`、`workers`、`dashboard`、`deploy`、`cs_bot`。
- 发现双前端体系 `2` 套：`frontend/` 与 `dashboard/frontend/`。
- 发现明确重复 helper `2` 份：`_resolve_legacy_requested_duration`。
- 发现疑似未被引用的公共函数 `3` 个：`get_system_task_stats`、`force_terminate_task`、`sync_user_concurrency`。
- 发现已腐坏且未引用的旧辅助函数 `1` 个：`TaskService._submit_generic_task`。
- 发现仍在线的废弃链路 `1` 条：TG `gallery_apply_*`。
- 发现明显无业务价值的示例/实验文件至少 `3` 个：`HelloWorld.vue`、`replace_bg.py`、`test_middleware.py`。

## 5. 优先级分级

### P0：立即处理

这些问题应优先处理，因为它们要么有安全风险，要么会持续制造错误操作。

#### 5.2 生产/测试部署文档与真实脚本严重漂移

- 涉及文件：`docs/SAFE_DEPLOY_GUIDE.md`、`docs/子模块_运维指南与容器管理_ops_deployment.md`、`docs/子模块_容灾与持久化_database_recovery.md`
- 真实入口：`safe_deploy.sh`、`safe_deploy_test.sh`
- 问题性质：运维误导，高概率造成错误发布和错误恢复
- 建议动作：
  - 以 `safe_deploy.sh` / `safe_deploy_test.sh` 为唯一事实源
  - 统一重写部署文档，不再保留旧的“9 步发布”描述
  - 为生产、测试、数据库迁移分别给出“最短正确路径”
  - 在文档头部显式标注适用环境和废弃流程


## 6. 高优先清理项

### 6.3 提示词复制逻辑重复散落

- 文件：
  - `dashboard/frontend/src/components/GalleryTable.vue`
  - `frontend/src/views/MyFavorites.vue`
  - `frontend/src/components/MySubmissionsPanel.vue`
- 问题：重复实现 `clipboard + execCommand` 降级逻辑
- 建议动作：
  - 抽到单一工具函数，如 `copyTextWithFallback()`
  - 统一成功/失败提示文案
  - 避免后续再出现第 4 份实现

### 6.4 任务状态清理/补偿逻辑未收口

- 涉及文件：
  - `src/core/task_core.py`
  - `src/services/recovery_service.py`
  - `src/services/zombie_cleaner_service.py`
- 问题：
  - 释放并发锁
  - 删除活动任务
  - 退款
  - 清理注册表
  - 调中控取消
  这些动作分散在多处，以不同顺序和组合执行
- 风险：
  - 重复补偿
  - 状态漂移
  - 部分流程修复后，其他流程继续保留旧行为
- 建议动作：
  - 抽象统一的任务终态补偿 primitive，例如：
    - `finalize_task_success(...)`
    - `finalize_task_failure(...)`
    - `finalize_task_cancel(...)`
  - 明确谁负责退款、谁负责删注册、谁负责并发计数回收
  - 所有恢复、清尸、强制终止逻辑统一调用同一组 primitive

### 6.5 `TaskService` 与 `task_dispatcher` 双轨并存

- 文件：`src/services/task_service.py`、`src/core/task_dispatcher.py`
- 问题：
  - 新旧提交流程共存
  - 同类模板代码重复出现在多个任务处理函数中
  - `_submit_generic_task` 已未引用且参数签名过期
- 建议动作：
  - 删除 `_submit_generic_task`
  - 把重复的“提交 -> 监控 -> 完成/退款 -> 清理”提炼成统一 orchestration helper
  - `TaskService` 只保留 Telegram 表示层适配
  - 业务提交逻辑继续向 `src/core/` 收口

## 7. 中优先清理项

### 7.1 废弃 TG Gallery Apply 链路仍在线

- 文件：
  - `src/handlers/fsm/gallery_apply_fsm.py`
  - `src/handlers/callbacks/gallery_callbacks.py`
  - `src/bot_test.py`
- 现状：
  - 文件头已声明 deprecated/legacy
  - 但 handler 仍在注册
  - callback 按钮仍在生成
- 建议动作：
  - 明确是否还要保留该入口
  - 若已由 Web 模板应用链路取代，则彻底下线
  - 同步删除相关按钮、handler、测试与无效文案

### 7.2 Web Router 中相同 legacy helper 重复两份

- 文件：
  - `src/web_api/routers/gallery.py`
  - `src/web_api/routers/users.py`
- 重复对象：`_resolve_legacy_requested_duration`
- 建议动作：
  - 提取到 `src/core/video_billing.py` 或 `src/web_api/routers/utils.py`
  - 路由层只保留调用，不再各自维护同名 helper

### 7.3 Core 层仍混入平台细节

- 文件：`src/core/auth_core.py`
- 问题：核心层直接调用 Telegram HTTP API 发送安全通知
- 风险：违反 core isolation，未来接 Web、Dashboard 或别的通知通道时会继续耦合
- 建议动作：
  - 将通知发送下沉到 adapter/service 层
  - Core 只产出“应该通知”的领域事件或通知命令

### 7.4 恢复链路依赖 Telegram 表示层服务

- 文件：`src/services/task_recovery_runtime.py`
- 问题：通过 `RecoveryMessageAdapter` / `RecoveryContextAdapter` 伪装上下文去调用 `TaskService`
- 风险：恢复链路对 Telegram 表示层实现产生隐藏依赖
- 建议动作：
  - 把恢复完成后的公共收尾逻辑抽到 core/service 层
  - `TaskService` 只负责真人交互场景
  - `task_recovery_runtime` 不再依赖 Telegram 专属服务

## 8. 低优先清理项

这些问题不会立刻出事故，但会持续制造噪音，建议在第一轮收敛后处理。

### 8.1 明显死代码和示例代码

- `frontend/src/components/HelloWorld.vue`
- `src/core/task_core.py` 中疑似未引用函数：
  - `get_system_task_stats`
  - `force_terminate_task`
  - `sync_user_concurrency`
- `src/services/task_service.py` 中未引用且已腐坏的 `_submit_generic_task`

建议动作：

- 先做一次全仓库引用确认
- 对确认无引用且无外部入口的对象直接删除
- 对“可能由运维/脚本调用”的对象先标记观察，再补 grep 与运行期验证

### 8.2 前端目录中的实验性和离线脚本

- `frontend/replace_bg.py`
- `frontend/test_middleware.py`

建议动作：

- 如果仍有价值，移到 `scripts/experiments/`
- 如果已无价值，直接删除
- 前端目录只保留参与构建、测试、部署的文件

### 8.3 Dashboard 占位菜单与兼容残留

- `dashboard/frontend/src/composables/useDashboardNavigation.ts` 中 `settings` 为固定 disabled
- `dashboard/frontend/src/api/api.js` 中 `fetchUsers` 仍保留旧 string 查询兼容分支
- `frontend/src/views/Gallery.vue` 中仍留有“兼容后端还没重启”类注释/逻辑

建议动作：

- 清理永远不会开放的占位菜单
- 为兼容逻辑设置删除条件和截止时间
- 避免“临时兼容”永久化

## 9. 建议删除清单

### 9.1 立即删除

- `src/services/task_service.py` 中 `_submit_generic_task`

### 9.2 确认后删除

- `frontend/src/components/HelloWorld.vue`
- `frontend/replace_bg.py`
- `frontend/test_middleware.py`
- `src/handlers/fsm/gallery_apply_fsm.py` 及其关联 wiring
- `scripts/merge_user_*.py`

### 9.3 不建议直接批量删除

- 全仓库 `TODO/FIXME/deprecated/legacy` 标记
- 原因：其中有一部分是有效提醒、测试说明、兼容过渡记录，不能机械清理

## 10. 建议抽取的公共逻辑

优先抽取以下重复点，收益最高：

### 10.1 前端公共层

- `copyTextWithFallback()`
- `getAuthToken()`
- `attachAuthHeader()`
- `handleUnauthorized()`
- `buildQueryParams()`

### 10.2 任务生命周期公共层

- `submit_task_or_raise()`
- `finalize_task_success()`
- `finalize_task_failure()`
- `finalize_task_cancel()`
- `cleanup_task_runtime_state()`

### 10.3 Web Router 公共层

- `resolve_legacy_requested_duration()`
- 其他类似的历史兼容参数归一化逻辑

### 10.4 部署公共层

- `wait_for_http_ready`
- 维护模式开关
- 队列清理与僵尸任务清理
- compose 调用入口封装

建议将 `safe_deploy.sh` 与 `safe_deploy_test.sh` 共享逻辑抽到一个公共 shell 库，例如 `deploy/lib/common.sh`。

## 11. 架构收敛方向

### 11.1 前端

目标不是马上把两个前端合并成一个项目，而是先统一底层约定：

- 统一认证模型
- 统一 API client 约定
- 统一错误处理
- 统一公共工具函数
- 统一类型和接口定义的命名规范

### 11.2 后端

目标是把“业务编排”和“平台适配”彻底分开：

- `src/core/` 负责稳定业务 primitive
- `src/services/` 负责应用层编排
- `src/handlers/` 和 `src/web_api/routers/` 只做平台适配
- 恢复、清尸、取消、退款必须复用同一套终态处理能力

### 11.3 部署

目标是减少入口数量和复制维护：

- 文档只有一个事实源
- 发布脚本公共逻辑下沉
- 生产/测试差异通过参数或环境变量体现
- 历史一次性运维脚本归档，不继续混在主入口附近

## 12. 推荐执行顺序

### 第一阶段：立即止血（1 天内）

- 修正文档漂移
- 删除 `_submit_generic_task`
- 把 Dashboard 绕过 API 层的直接 `fetch` 收回统一请求层

### 第二阶段：清理噪音（2 到 4 天）

- 删除 `HelloWorld.vue`
- 处理 `replace_bg.py`、`test_middleware.py`
- 清理 disabled 菜单和明显过期兼容分支
- 逐个核查 `deprecated/legacy` 标记，形成保留/删除名单

### 第三阶段：抽公共层（约 1 周）

- 抽复制提示词逻辑
- 抽路由层重复 helper
- 抽任务终态补偿 primitive
- 收敛 `TaskService` 中重复模板代码

### 第四阶段：结构重构（持续演进）

- 让恢复链路脱离 Telegram 表示层
- 清理废弃 TG gallery apply 链路
- 推动 Dashboard 前端逐步 TS 化
- 收敛部署脚本和 compose 入口

## 13. 验收标准

本轮清理建议至少达到以下结果：

- 前端不再出现绕过统一 API 层的请求
- 明确废弃链路不再继续注册和暴露入口
- 任务成功/失败/取消的清理逻辑收口到统一 primitive
- 部署文档与真实脚本保持一致
- 至少删除一批已确认的死代码和实验性垃圾文件

## 14. 建议的文档后续动作

建议在本方案执行时同步维护两份产物：

- `代码清理方案.md`：作为总方案和阶段路线图
- `代码清理执行清单.md`：逐项记录已删除、已抽取、待验证对象

这样可以避免“分析做完了，但清理过程中又重新失控”。
