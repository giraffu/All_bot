# All_Bot 安全更新与部署脚本 (`safe_deploy.sh`) 使用说明

`safe_deploy.sh` 是一键式自动化部署与更新脚本，专门用于 All_Bot 系统的生产和测试环境平滑升级。该脚本采用了**熔断保护**和**队列监控**机制，确保在更新服务时不会中断正在执行的用户任务，也不会引发 Redis 并发死锁。

---

## ⚠️ 执行前置条件 (Pre-requisites)

在执行该脚本前，请务必确认以下事项：

1. **代码已是最新**：请确保已经通过 `git pull` 等方式获取了最新的代码。
2. **环境变量已就绪**：确保根目录下的 `.env` 文件已正确配置。

---

## 🚀 如何执行脚本

在项目根目录 (`/home/hfy/APP/All_bot`) 下，直接运行以下命令：

```bash
bash safe_deploy.sh
# 或者
./safe_deploy.sh
```

*(注意：脚本执行过程中请勿强制中断 `Ctrl+C`，以免造成中间状态不一致)*

---

## 🔄 脚本执行流程解析 (Step-by-Step)

脚本分为 8 个自动化步骤，全程无需人工干预：

### 1️⃣ 开启系统维护模式
- **原理**：在主机器人容器 (`tg-bot`) 内部创建一个 `/app/MAINTENANCE` 文件。
- **效果**：机器人将立即进入维护模式，拒绝接收用户提交的新生图/生视频任务（不影响常规查询和菜单浏览），从源头切断增量任务。

### 2️⃣ 智能监控 Redis 活跃队列
- **原理**：注入 Python 脚本，轮询检查 Redis 中的 `active_tasks` 哈希表长度。
- **效果**：脚本会每隔 10 秒检测一次，直到当前正在被 Comfy Agent 处理的所有任务全部生成完毕。这保证了用户已经付款并排队的任务不会因为重启而丢失。

### 3️⃣ 僵尸任务与并发锁清理
- **原理**：调用 `src/services/zombie_cleaner_service.py`。
- **效果**：在确认活跃任务为 0 后，系统会清理所有可能因为之前异常崩溃而遗留的 `user_concurrency:{user_id}` Redis 并发锁，防止用户被永久卡住（死锁）。

### 4️⃣ 重建 Agent 容器服务 (底层工作节点)
- **原理**：删除并重新构建 `workers/` 目录下的所有 `comfy-agent` 容器。
- **效果**：应用最新的工作流 (workflows) 和 Agent 逻辑。

### 5️⃣ 重建中控 API (Central API)
- **原理**：删除并重新构建 `backend/` 目录下的中控任务分发 API。
- **效果**：应用最新的排队与分发调度逻辑。

### 6️⃣ 重建主服务群
- **原理**：基于 `deploy/docker-compose.yml`，删除并重新构建 `tg-bot` (主机器人)、`payment-api` (支付回调服务) 和 `web-api` (前端接口服务)。
- **效果**：正式环境的核心业务逻辑更新。*(容器重启后，维护模式文件自动消失，服务恢复接收新任务)*。

### 7️⃣ 重建 Dashboard 服务
- **原理**：进入 `dashboard/` 目录，删除并重新构建前后端管理面板。
- **效果**：更新管理员控制台。

### 8️⃣ 重建测试环境服务群
- **原理**：基于 `deploy/docker-compose-test.yml` 重建测试机器人及测试环境相关接口。
- **效果**：同步更新测试环境逻辑。

---

## 🛠️ 部署后置操作 (Post-requisites)

**【极度重要】数据库迁移（按需）**：
如果本次 `git pull` 的代码更新包含了数据库表结构的变更（即 `migrations/versions/` 目录下有新文件），**千万不要在部署前执行迁移**。因为生产环境在构建镜像时才会打入新代码，旧容器内尚未拉取新脚本，会导致迁移指令变成无效空跑。

请在**本脚本（`safe_deploy.sh`）执行完毕、新容器启动后**，立刻手动执行以下命令应用迁移：
```bash
docker exec -it tg-bot alembic upgrade head
```
*(注：测试环境与正式环境共用同一个数据库实例，因此只需在主容器 `tg-bot` 执行一次迁移即可，无需在 `tg-bot-test` 中重复执行。)*

---

## 💡 常见问题与排障 (Troubleshooting)

**Q1: 脚本卡在第 2 步（监控活跃队列）很久不动怎么办？**
- **原因**：可能有一个耗时极长的视频任务正在生成，或者有底层 Worker 已经宕机导致任务永远无法完成（变成僵尸任务）。
- **解决办法**：
  1. 打开一个新的终端，使用 `docker logs -f <对应的comfy-agent容器名>` 查看底层 Worker 是否还在工作。
  2. 如果确认 Worker 已经死机卡死，你可以手动在 Dashboard 或者 Redis 中清理该僵尸任务，脚本检测到数量归零后会自动继续往下执行。

**Q2: 执行时提示 `docker-compose: command not found`？**
- **原因**：您的系统可能使用的是新版 Docker 插件 `docker compose`（不带连字符）。
- **解决办法**：可以手动修改 `safe_deploy.sh` 脚本，将所有的 `docker-compose` 替换为 `docker compose`。

**Q3: 为什么 CS Bot (智能客服) 没有被更新？**
- **原因**：CS Bot 属于相对独立的子模块，为避免非必要的中断，默认未加入此脚本的全局重建流中。
- **解决办法**：如果您的更新涉及了 CS Bot，请在部署完成后手动执行：
  ```bash
  cd cs_bot && docker rm -f cs-bot && docker-compose up -d --build
  ```

---
*本文档由 AI 自动生成，对应脚本版本: `safe_deploy.sh`*