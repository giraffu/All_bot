# Web 端生成结果 R2 热缓存实施方案

## 1. 目标

只解决一个核心问题，不做大范围重构：

1. Web 端用户生成完成后，结果文件在写入 MinIO 并返回给用户的同时，异步同步一份到 R2。
2. 用户打开“闪回瓶”时，最近 8 条 Web 生成记录的缩略图优先走 R2，降低本地 MinIO 读取压力。
3. 当某个用户在 R2 中缓存的“普通 Web 历史内容”超过 8 条时，自动删除更老的数据。
4. `收藏` 和 `已发布到广场` 的内容不受“仅保留 8 条”限制，允许长期保留在 R2。

---

## 2. 范围

### 本次纳入

- Web 端任务完成后的异步 R2 同步
- 闪回瓶最近 8 条记录的缩略图 R2 优先读取
- 用户级 R2 热缓存清理
- 收藏/投稿内容的保留例外规则

### 本次不纳入

- 广场接口整体读链路改造
- SSE 返回字段大改
- Bot 端任务完成链路改造
- 全站 R2 Key 体系大迁移
- 历史数据一次性补迁脚本

---

## 3. 现状判断

当前真实代码链路中：

1. Web 任务完成后，最终结果是由核心完成链路写入 MinIO 和 `History`。
2. `users/history` 当前主要返回 MinIO URL，没有形成“Web 最近 8 条缩略图优先走 R2”的稳定机制。
3. 现有 R2 上传逻辑分散，且存在只在收藏/投稿时触发的情况，不满足“Web 一生成就热缓存”的要求。
4. R2 生命周期管理还没有做到“普通历史仅保留最近 8 条”。

因此，本次方案采用“最小改动、只打通 Web 历史热缓存主链”的方式实施。

---

## 4. 设计原则

1. **源站不变**：MinIO 继续作为唯一真实源站。
2. **异步同步**：用户完成任务时，不等待 R2 上传完成，避免影响返回速度。
3. **只缓存 Web 历史热点**：仅对 Web 端生成结果做“最近 8 条热缓存”治理。
4. **缩略图优先降压**：闪回瓶列表优先使用 R2 缩略图，不强制历史详情页优先走 R2 原文件。
5. **保护收藏与投稿**：收藏/广场内容不参与“只保留 8 条普通历史”的淘汰。
6. **最小触点**：优先复用已有 `History`、存储服务和 `users/history` 接口，不扩散到更多模块。

---

## 5. 核心方案

## 5.1 触发点

### 触发位置

以 Web 任务完成后的核心保存链路为唯一触发点：

- `src/core/task_core.py`

触发时机：

1. 任务完成
2. 结果文件已成功写入 MinIO
3. `History` 已成功落库
4. 立即启动后台异步任务：
   - 同步原文件到 R2
   - 生成并同步缩略图到 R2
   - 执行该用户的 R2 普通历史淘汰

### 为什么放这里

原因很简单：

- 这是 Web 端真实完成链路
- 能拿到 `user_id`、`task_id`、`output_file`、`type`、`source`
- 不需要让 Router 或前端承担写操作
- 能确保“生成完成后立刻进入热缓存流程”

---

## 5.2 R2 存储对象范围

### 需要同步的对象

每条 Web 生成历史，至少同步两类对象：

1. 原始结果文件
2. 对应缩略图

### 建议 R2 Key

本次不引入新的全站 Key 体系，直接沿用现有代码已经在使用的任务级 Key：

- 原文件：`history/{task_id}/original.{ext}`
- 图片缩略图：`history/{task_id}/thumb.webp`
- 视频缩略图：`history/{task_id}/thumb.jpg`

这样做的好处：

1. 与当前 `users.py` / 收藏链路已使用的历史任务 R2 Key 保持一致
2. 不需要额外做 `history/{user_id}/{task_id}` 的体系迁移
3. 仍然可以按 `task_id` 精准删除，满足用户级淘汰诉求

---

## 5.3 闪回瓶读取策略

### 目标接口

- `src/web_api/routers/users.py`
- 目标接口：`GET /users/history`

### 返回策略

对最近 8 条历史记录，列表卡片只关心缩略图：

1. 如果该记录存在 R2 缩略图，则 `thumbnail_url` 返回 R2 公网地址
2. 如果 R2 缩略图不存在，则回退 MinIO 缩略图地址
3. `output_file_url` 本次可以保持现状，不强制改成 R2 优先
4. 闪回瓶卡片层面只消费 `thumbnail_url`，避免首屏直接拉原图/原视频
5. 保持当前“先取最近 8 条窗口，再过滤不可见记录”的产品语义，不额外补满 8 条可见记录
6. `GET /users/history` 只读，不在读取时触发 MinIO -> R2 补传

### 本次重点

本次只要求：

- “前 8 条内容的缩略图优先走 R2”

不要求：

- 历史详情页的原图/原视频一定走 R2
- 所有历史接口都做回填修复
- 在 GET 接口中发现缺失后再即时上传 R2

---

## 5.4 R2 热缓存保留策略

### 保留规则

对每个用户，仅对“普通 Web 历史内容”执行 8 条保留：

保留集合由三部分组成：

1. 最近 8 条 `source='web'` 的历史记录
2. 所有 `is_favorited = true` 的记录
3. 所有 `is_public = true` 的记录

### 淘汰规则

对该用户在 R2 中的 Web 历史对象进行清理时：

1. 如果某任务属于最近 8 条普通历史，保留
2. 如果某任务已收藏，保留
3. 如果某任务已发布到广场，保留
4. 其他更老的普通历史任务，从 R2 删除其：
   - 原文件
   - 缩略图

### 计算原则

保留集合的计算以数据库中的 `History` 最新状态为准，而不是依赖任务完成瞬间传入的状态快照：

1. 先查询该用户最近 8 条“普通 Web 历史”
2. 再合并所有 `is_favorited = true` 的记录
3. 再合并所有 `is_public = true` 的记录
4. 最后对不在保留集合中的普通历史任务执行 R2 删除

### 注意点

这里删除的是：

- R2 热缓存副本

这里不删除的是：

- MinIO 源文件
- 数据库 `History`
- 收藏或广场保留内容

---

## 6. 代码改造点

## 6.1 `src/core/task_core.py`

新增一个“Web 完成后热缓存调度”步骤。

### 需要做的事

在 Web 任务完成并成功写入 `History` 后，异步触发一个统一方法，例如：

- `schedule_web_history_r2_warmup(...)`

该方法内部负责：

1. 同步原文件到 R2
2. 生成并同步缩略图到 R2
3. 清理该用户超出 8 条的普通历史 R2 对象

### 输入信息

至少需要这些参数：

- `user_id`
- `task_id`
- `output_file`
- `media_type`
- `source`

其中 `source != 'web'` 时直接跳过。

### 额外约束

1. 只有在 `output_file` 有效且 `History` 已成功落库后，才允许进入 warmup
2. `is_favorited` / `is_public` 不从完成态快照透传，而是在清理阶段重新查库获取最新状态
3. warmup 内任何失败只记录日志，不影响主任务返回

---

## 6.2 `src/services/storage.py`

新增或集中封装以下能力：

### 能力 A：上传原文件到 R2

输入：

- MinIO bucket
- object_name
- r2_key

要求：

- 异步执行
- 成功后记录日志
- 失败只告警，不影响主流程
- 实现时避免整文件一次性 `read()` 到内存，尤其是视频文件，优先采用流式/分段上传思路

### 能力 B：生成或同步缩略图到 R2

逻辑：

1. 先确保 MinIO 中已有缩略图
2. 再把缩略图同步到 R2
3. 若 MinIO 缩略图不存在，则先生成再上传

### 能力 C：删除 R2 对象

用于淘汰普通历史缓存：

- 删除原文件 key
- 删除缩略图 key

### 能力 D：用户级普通历史淘汰

输入：

- `user_id`

处理流程：

1. 查询该用户最近 8 条普通 Web 历史任务
2. 合并该用户所有已收藏任务
3. 合并该用户所有已公开投稿任务
4. 以任务维度计算应删除的普通历史任务
5. 删除其原文件和缩略图
6. 记录删除结果日志

### 兼容要求

由于历史上存在 legacy R2 Key，本次至少要保证：

1. 新写入统一使用 `history/{task_id}/...`
2. 读取链路优先读取当前历史任务 Key
3. 如代码中仍存在旧的 basename R2 副本，删除逻辑需预留兼容处理，避免旧缓存长期残留

---

## 6.3 `src/web_api/routers/users.py`

只调整 `get_user_history` 的缩略图返回逻辑。

### 调整内容

对于每条历史记录：

1. 计算该任务的历史缩略图 R2 Key
2. 如果 R2 中存在，则返回 R2 缩略图 URL
3. 否则回退 MinIO 缩略图 URL
4. 不在这个 GET 接口里触发上传动作
5. 保持当前“不补满 8 条可见记录”的返回语义

### 原则

`users/history` 只读，不负责写，不负责同步，不负责清理。

这样可以避免：

- 用户频繁刷新列表触发重复上传
- GET 接口承担副作用
- 读写耦合过重

---

## 7. 数据判定规则

## 7.1 什么算“普通 Web 历史”

满足以下条件：

1. `History.source == 'web'`
2. 非收藏
3. 非公开投稿

### 可表达为

- 普通历史：`source='web' AND is_favorited=false AND is_public=false`

这类记录只保留最近 8 条热缓存。

---

## 7.2 什么算“长期保留”

满足以下任一条件即可：

1. `is_favorited = true`
2. `is_public = true`

这类记录在 R2 中不受“仅保留 8 条”的限制。

---

## 8. 执行流程

## 8.1 生成完成后的写路径

1. Web 用户提交任务
2. 任务完成
3. 结果写入 MinIO
4. 写入 `History`
5. 返回前端成功状态
6. 后台异步执行：
   - 原文件同步到 R2
   - 缩略图同步到 R2
   - 清理用户超过 8 条的普通历史 R2 对象

## 8.2 闪回瓶读取路径

1. 前端请求 `GET /users/history`
2. 后端查询最近 8 条记录
3. 对每条记录优先组装 R2 缩略图 URL
4. 若 R2 缩略图不存在，则回退 MinIO 缩略图 URL
5. 整个 GET 流程不触发 MinIO -> R2 上传
6. 前端列表页只使用 `thumbnail_url`

---

## 9. 实施步骤

## Phase 1：打通 Web 完成后的异步热缓存

1. 在核心完成链路中挂载 Web 热缓存异步任务
2. 原文件同步到 R2
3. 缩略图同步到 R2
4. 确保失败不影响主任务返回

## Phase 2：打通闪回瓶缩略图 R2 优先读取

1. 修改 `users/history`
2. 缩略图优先返回 R2
3. 不存在时回退 MinIO

## Phase 3：补上用户级 8 条淘汰

1. 查询用户最近 8 条普通 Web 历史
2. 合并收藏/公开记录作为保留集合
3. 删除 R2 中其他更老的普通历史对象

---

## 10. 验收标准

### 功能验收

1. Web 用户生成完成后，不阻塞页面成功返回
2. 后台日志能看到该任务原文件和缩略图进入 R2
3. 打开闪回瓶时，最近 8 条列表卡片优先命中 R2 缩略图
4. R2 缩略图缺失时，能自动回退到 MinIO 缩略图
5. 当某用户普通 Web 历史超过 8 条时，R2 中更老普通历史对象被删除
6. 收藏内容不会因“超过 8 条”被删
7. 已投稿广场内容不会因“超过 8 条”被删
8. 最近 8 条窗口中夹杂不可见记录时，接口允许返回少于 8 条，不额外补齐
9. `GET /users/history` 不触发任何上传或清理副作用

### 压测验收

1. 连续生成 20 条 Web 历史后，R2 中普通历史仅保留最近 8 条
2. 同时存在收藏和投稿数据时，这些对象仍保留
3. 闪回瓶列表首屏主要访问 `_thumb` 资源，而不是原图/原视频
4. MinIO 历史列表读取压力明显下降
5. 高频刷新闪回瓶不会引发重复上传风暴

---

## 11. 风险与规避

### 风险 1：R2 上传失败导致用户无感知

这是允许的。

处理策略：

- 只记录告警日志
- 不影响用户主流程
- 列表读取时回退 MinIO 缩略图

### 风险 2：淘汰误删收藏或投稿对象

处理策略：

- 先计算完整保留集合，再做删除
- 删除逻辑只针对“普通 Web 历史”
- 收藏/公开数据永远排除在淘汰集之外

### 风险 3：缩略图尚未生成时列表回退原文件

处理策略：

- 后端优先保证缩略图生成和同步
- 前端列表严格使用 `thumbnail_url`
- 缩略图不存在时优先回退 MinIO 缩略图，而不是原文件

### 风险 4：在 GET 接口里回源补传导致重复上传和读写耦合

这是本次明确规避的做法。

处理策略：

- `GET /users/history` 只读
- R2 缺失时只回退 MinIO，不在 GET 内触发上传
- 所有 R2 写入统一收敛到任务完成后的后台异步链路

### 风险 5：R2 Key 与历史旧副本不一致导致命中率下降或删不干净

处理策略：

- 本次统一新写入使用 `history/{task_id}/...`
- 读取时优先判断当前历史任务 Key
- 淘汰逻辑预留 legacy 副本兼容删除，避免遗留垃圾对象

### 风险 6：视频同步时整文件读入内存导致峰值过高

处理策略：

- 原文件同步实现避免一次性全量读入内存
- 大文件采用流式或分段上传方式
- 上传失败只记录告警，不影响主流程

---

## 12. 最终结论

本次实施只做一条最核心的闭环：

1. Web 生成完成后，异步把结果和缩略图同步到 R2
2. 闪回瓶前 8 条列表优先读取 R2 缩略图
3. 普通 Web 历史在 R2 中仅保留最近 8 条
4. 收藏和已发布内容长期保留，不参与淘汰

这样可以在不大改全站读写链路的前提下，优先降低 MinIO 在“用户最近历史浏览”场景下的读取压力。
