# 修仙主题 AI 工作台 Web 端 - 社区广场与一键应用实现方案

## 1. 背景与目标
在 Telegram Bot 端，我们已经成功实现了基于 `telegram_file_id` 0流量转发机制的社区广场（Gallery）、排行榜、点赞/点踩以及一键应用功能。
考虑到 Web 端（Vue3 SPA）具有更强的视觉表现力和多媒体交互能力，本方案旨在将“社区广场”功能无缝延伸至 Web 端，打造全平台的社区生态。针对 Web 端无法使用 TG 缓存的特点，重点解决媒体加载的带宽成本与前端渲染性能问题。

## 2. 核心挑战与带宽优化策略

**核心挑战**：Web 端无法直接使用 Telegram 的 `file_id` 机制，所有媒体文件必须走标准 HTTP 请求。如果用户滚动页面时直接加载大量原画质的高清图片与长视频，将面临巨大的服务器出站带宽压力和 CDN 成本，同时也会导致前端浏览器卡顿。

**应对方案（不修改底层 Worker 节点的替代策略）**：
1. **图片缩略图动态生成 (Image Thumbnails)**：
   - 在 MinIO 存储前置一套轻量级的图片处理网关（如免费开源的 `imgproxy` 或 Cloudflare Image Resizing）。前端请求图片时通过 URL 参数动态获取缩略图（如宽度 400px 的 WebP），完全不干扰底层生成逻辑。
2. **视频首帧与懒加载策略 (Video Cover & Lazy Load)**：
   - **方案A（前端 Range Request，零后处理）**：完全不生成额外的视频封面图片文件。利用浏览器原生的 `<video preload="metadata">` 属性，配合 MinIO 的 HTTP Range 请求特性，浏览器会自动只下载 `.mp4` 文件头部的几十 KB 来渲染第一帧。
   - **方案B（BFF 后端异步提取）**：在用户点击“一键投稿”时，由 FastAPI BFF 触发一个轻量的后台协程 (`BackgroundTasks`)，使用 `ffmpeg` 从 MinIO 提取该视频的第一帧生成 `.jpg` 封面并存入同目录，前端列表页优先加载该 `.jpg`。
3. **交互式无缝播放 (Interactive Playback)**：
   - 瀑布流使用 `IntersectionObserver` 实现可视区域懒加载。
   - **Hover 预览**：由于不另外生成低清视频流，当用户鼠标悬停 (Hover) 时，前端直接对已加载 metadata 的 `<video>` 元素调用 `.play()`（静音循环播放），移开时调用 `.pause()`。
   - **详情查看**：点击进入详情 Modal 时，直接放大该媒体组件并复用已加载的流，避免二次请求。

## 3. Web 端 UI/UX 设计方案

界面需契合工作台现有的“玄青冷翠”修仙主题，采用现代化的高级感设计。

### 3.1 发现/广场页 (Gallery View)
- **布局设计**：采用响应式瀑布流 (Masonry Grid) 布局，以适配不同宽高比的生成作品，最大化视觉冲击力。
- **卡片组件 (Media Card)**：
  - 外观采用毛玻璃 (`backdrop-filter: blur`) 与半透明暗色背景。
  - 右上角标识媒体类型角标（🎬 视频 / 🖼️ 图片）。
  - 底部悬浮展示数据统计栏：👍 赞、👎 踩、🔥 应用次数。
  - **交互效果**：Hover 时卡片微微上浮（加入阴影发光效果），并在封面上方浮现经过本地化映射的中文 LoRA 标签（如 `#巨乳膨胀`）以及部分作者打码信息。
- **过滤与导航**：
  - 顶部栏提供 Tab 切换（全部 / 纯图 / 动态）。
  - 右侧提供排序下拉框（最新发布、最多点赞、最多应用）。

### 3.2 详情与一键应用弹窗 (Detail Modal)
- **触发机制**：点击卡片后，在页面中央弹出带有暗色遮罩的详情 Modal（沉浸式浏览模式）。
- **左侧视觉区**：高清图片查看器或完整的视频播放器，此时才向 `assets.aivison.it.com` 请求真实的完整大文件。
- **右侧信息与操作区**：
  - **元数据**：展示时长、分辨率、中文化标签集、原作者等。
  - **互动区**：大尺寸的“👍 点赞”与“👎 点踩”按钮，点击后带有灵气扩散等粒子动画。
  - **一键应用入口**：醒目的“✨ 一键应用此模板”主按钮。

### 3.3 一键应用流转逻辑 (Apply Workflow)
1. 点击“一键应用此模板”后，Modal 关闭，前端携带被选中帖子的 `post_id` 或 `task_id`，路由跳转至对应的生成功能页（如“幻想换脸”）。
2. 生成页通过获取上下文，自动填入并锁定原始 Prompt、模型权重、宽高及时长等参数。
3. 界面提示用户“已载入模板，请上传您的参考图”。
4. **防套娃机制**：前端在向 BFF 提交任务时，附加标志位；BFF 在向底层 Worker 提交时强制锁定 `allow_contribute=False`，防止复制产生的派生作品被再次投稿。

## 4. 后端 API 扩展方案 (FastAPI BFF)

在 `src/web_api/routers/` 目录下新增 `gallery.py` 路由模块：

### 4.1 `GET /api/gallery/posts`
- **功能**：获取广场帖子分页列表。
- **参数**：`page`, `size`, `media_type`, `sort_by` (latest/likes/applied)。
- **返回**：包含帖子基础信息、已翻译的中文 tags、互动计数、以及由 BFF 拼接好的**缩略图直连 URL**。

### 4.2 `POST /api/gallery/posts/{post_id}/interact`
- **功能**：记录用户点赞或点踩。
- **实现要求**：需修复现有 Bot 端的并发覆盖 Bug（Lost Update），必须使用 SQLAlchemy 层面的原子递增更新：`UPDATE gallery_posts SET likes_count = likes_count + 1 WHERE id = :id`，并严格校验同一用户对同一帖子的互斥互动记录。

### 4.3 `GET /api/gallery/posts/{post_id}/apply-context`
- **功能**：获取一键应用模板的上下文详情。
- **逻辑**：通过 `post_id` 关联 `History` 表，提取原任务的核心参数（`prompt`, 尺寸, 节点配置等）返回给前端回显。同时触发降级计费逻辑预检（例如低等级用户应用 10s 视频模板时，提示将会降级到 5s）。

## 5. 实施路线图 (Roadmap)

- **第一阶段 (后端支撑与修复)**：
  - 修复数据库中点赞并发的 Bug，以及完善用户互动记录的互斥校验。
  - 部署图片代理网关（如 `imgproxy`），若采用视频方案B，则在 Web API BFF 中编写基于 `ffmpeg` 的封面提取后台任务。
  - 编写并测试 FastAPI BFF 中的 `/api/gallery` 接口群。
- **第二阶段 (前端视图与交互)**：
  - 在 Vue3 中引入瀑布流组件，构建“发现/广场”主页。
  - 实现懒加载、媒体卡片及点赞动效，对接列表与互动 API。
- **第三阶段 (一键应用生态闭环)**：
  - 开发详情 Modal 弹窗，实现高清大文件的按需播放。
  - 打通从广场卡片跳转至工作台表单的路由传参（带参跳转）。
  - 前后端联调防二次投稿（防套娃）限制，完成端到端测试。
