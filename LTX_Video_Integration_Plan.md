# LTX Video (高级图生视频) 接入 Telegram Bot 方案设计

## 1. 核心需求总结
根据您的要求，新功能将按照以下核心指标进行设计：
- **产品形态**：方案 B（开放自定义），用户可以发送图片并自定义正向提示词。
- **UI 命名**：`高级图生视频`。
- **参数规格**：
  - **时长**：开放 4 个档位：`5s`、`10s`、`15s`、`20s`。
  - **分辨率**：以您跑通的 `1280x704` 为最高档，向下划分出 3 个档位，且必须满足 **宽和高均能被 32 整除** 的底层模型硬性约束。
- **定价基准**：最低档（最低分辨率 + 5s）的基准价格定为 **8 灵石**。

---

## 2. 规格与定价体系设计

### 2.1 分辨率档位划分 (Resolution Tiers)
LTX 2.3 模型对分辨率要求严格（必须是 32 的倍数）。以 `1280x704` 为最高画质，我们将其等比例或近似等比例向下缩放，划分为以下三个档位：

| 档位名称 | 实际分辨率 (高 x 宽) | 像素量约数 | 计费基数 (Base Cost) |
| :--- | :--- | :--- | :--- |
| **标清 (SD)** | `640 x 352` | ~22.5 万 | **8 灵石** (基准) |
| **高清 (HD)** | `960 x 544` | ~52.2 万 | **12 灵石** (1.5 倍) |
| **超清 (FHD)** | `1280 x 704` | ~90.1 万 | **18 灵石** (2.25 倍) |
*(注：以上宽高数值均能被 32 完美整除，确保模型不会在预处理阶段报错或产生形变。)*

### 2.2 时长倍率计算 (Duration Multiplier)
视频生成所需的显存和时间与帧数（时长）呈线性或指数级增长。参考系统现有的倍率模型，设定如下：

| 时长档位 | 倍率 (Multiplier) | 说明 |
| :--- | :--- | :--- |
| **5s** | `1.0x` | 基础时长 |
| **10s** | `2.0x` | 翻倍时长 |
| **15s** | `3.0x` | 超长时长 |
| **20s** | `4.0x` | 极限时长 (极耗算力) |

### 2.3 综合价格表 (Pricing Matrix)
根据 `总价 = 计费基数 × 时长倍率`，得出用户界面最终展示的价格表：

| | 5s (1.0x) | 10s (2.0x) | 15s (3.0x) | 20s (4.0x) |
| :--- | :--- | :--- | :--- | :--- |
| **标清 (640x352)** | **8 灵石** | 16 灵石 | 24 灵石 | 32 灵石 |
| **高清 (960x544)** | **12 灵石** | 24 灵石 | 36 灵石 | 48 灵石 |
| **超清 (1280x704)** | **18 灵石** | 36 灵石 | 54 灵石 | 72 灵石 |

*(该矩阵不仅在业务上逻辑自洽，且能有效利用价格杠杆抑制免费/低级用户滥用顶级算力资源。)*

---

## 3. 技术实现路径 (Bot 侧代码修改)

### 3.1 常量与计费注册 (`src/constants.py`)
需要增加专属的常量字典，防止与普通的 `AnimateDiff` 视频计费逻辑冲突：
```python
MODE_LTX_VIDEO = "ltx_video"

LTX_RESOLUTION_COST = {
    "640x352": 8,
    "960x544": 12,
    "1280x704": 18
}

LTX_DURATION_MULTIPLIER = {
    5: 1.0,
    10: 2.0,
    15: 3.0,
    20: 4.0
}
```

### 3.2 交互入口 (`src/handlers/message_handler.py`)
在主菜单的键盘布局中，增加入口：
```python
if text == "🎬 自定义视频":
    # 现有的菜单结构
    # ...
    # 增加一个新的独立大按钮
    keyboard.append(["🎬 高级图生视频"])
```

### 3.3 状态机流转 (FSM - `custom_video_fsm.py`)
创建一个独立的 FSM 分支或在现有的 `custom_video_fsm` 中增加模式判断：
1. **触发**：拦截文本 `🎬 高级图生视频`。
2. **第一步 (收图)**：提示用户发送一张底图（长宽比建议接近 9:16）。
3. **第二步 (收词)**：提示用户输入视频的详细描述（Prompt），可以提供一些 LTX 专用的书写建议（如：以动作描述为主）。
4. **第三步 (配置)**：弹出 Inline 键盘，让用户选择 **分辨率** 和 **时长**。按钮上实时显示对应的灵石价格。
5. **第四步 (提交)**：用户点击确认后，调用任务服务。

### 3.4 核心任务调度 (`src/services/task_service.py` & `src/core/task_core.py`)
- 在 `TaskService` 中新增 `process_ltx_video_task` 方法。
- 提取用户选择的宽高尺寸和时长，计算最终费用，进行并发锁检查和灵石扣除。
- 通过 `api_client.submit_ltx_video` 将 `image_path`、`prompt`、`length`、`width`、`height` 投递到后端的 `/api/v1/ltx_video` 接口。
- 沿用现有的 `_monitor_task_progress` 逻辑，通过 Redis Pub/Sub 监听进度并把最终合成的 MP4 发给用户。

---

## 4. 潜在风险与注意事项
1. **长视频的显存溢出 (OOM) 风险**：
   - 20s 的视频在 LTX 2.3 模型下意味着需要一次性生成近 500 帧（如果以 24fps 计）。这不仅极其耗时，而且极易导致 Worker 节点的显存溢出（OOM）。
   - **建议**：在初期上线时，可以先在代码中对 `15s` 和 `20s` 的选项增加**权限门槛**（例如仅限“核心弟子”及以上可用），或者观察几天的节点稳定性后再逐步放开。
2. **图片比例裁剪**：
   - 用户上传的图片比例千奇百怪。工作流中我们使用的是 `ImageResizeKJv2` 节点，配置了 `keep_proportion: resize` 和 `crop_position: center`。这意味着如果用户传了一张正方形的图，但选择了 `1280x704` 的竖屏规格，图片会被**中心裁剪**。
   - **建议**：在 FSM 的提示语中，明确告知用户“请尽量上传 9:16 比例的竖屏图片，否则图片将被自动裁剪”。