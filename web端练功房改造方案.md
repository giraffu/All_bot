# Web 端练功房改造方案

## 1. 改造目标

将当前 Web 端“练功房”从“功能卡片列表页 + 分散工作台页”的模式，升级为类似参考图三的统一 AI 创作主界面。

目标效果：

- 练功房首页直接承载创作，不再先看到大面积卡片宫格。
- 顶部使用可横向拖动的功能选项条切换不同创作模式，默认选中“自由P图”。
- 中心主体是大提示词输入框，输入体验优先。
- 参考图上传后，显示在提示词输入框上方，形成“素材 + 提示词”的自然工作流。
- 附加模型、分辨率、生成时长、模型强度等次级参数默认收起，通过“三点按钮”展开，降低首屏复杂度。
- 保留现有任务提交、结果预览、模板回填、上传能力与计费逻辑，尽量前端重组，不轻易改后端协议。

## 2. 当前现状

当前实现可概括为两层：

### 2.1 练功房入口层

- `frontend/src/views/CustomFeatures.vue`
- 目前是九宫格/卡片式入口。
- 每张卡片跳转到独立路由页面，如：
  - `TextToImage.vue`
  - `ImageAndPrompt.vue`
  - `SingleImageToVideo.vue`
  - `FaceSwap.vue`
  - `VideoSwap.vue`
  - `Wan22VideoV2.vue`

### 2.2 具体工作台层

- 图像类工作台主要在 `TextToImage.vue`、`ImageAndPrompt.vue`
- 视频类工作台主要在 `SingleImageToVideo.vue`
- 统一容器已存在：`frontend/src/components/GenerationWorkbenchShell.vue`
- 底部提交栏已存在：`frontend/src/components/GenerationActionBar.vue`
- 横向滚动标签基础组件已存在：`frontend/src/components/SegmentedTabsRail.vue`
- 任务提交通用载荷构建已存在：`frontend/src/features/generation/buildGenerationTaskPayload.ts`

结论：

- 现有工程已经具备“统一工作台”改造基础，不需要推翻重做。
- 这次改造重点应放在“页面组织方式”和“交互层重构”，而不是任务协议重写。

## 3. 核心问题

当前练功房存在以下体验问题：

- 首屏先展示功能卡片，用户还要再点一次才能进入创作，路径偏长。
- 不同模式拆散在多个页面，整体体验割裂。
- 主输入区不够突出，提示词输入不是视觉中心。
- 高低频参数层级混在一起，首屏认知负担偏大。
- 上传素材与提示词之间的关系不够自然，不像“对话式创作面板”。
- 当功能逐步增多时，卡片入口扩展性差，移动端也容易显得拥挤。

## 4. 目标体验

参考图三、图四后的目标练功房应满足：

### 4.1 首屏结构

从上到下建议为：

1. 页面标题区
2. 横向拖动功能选项条
3. 创作主输入面板
4. 快捷功能推荐区或模式快捷卡片区
5. 结果预览区 / 最近结果区

### 4.2 创作主输入面板

主输入面板作为页面核心，建议包含：

- 已上传参考图缩略图区域
- 多行提示词输入框
- 底部快捷操作栏
  - 模式类型入口
  - 添加参考图
  - 三点展开更多参数
  - 提交按钮

### 4.3 三点展开区

三点按钮展开后显示“高级参数抽屉”或“浮层菜单”，承载低频配置：

- 附加模型 LoRA
- 分辨率
- 生成时长
- 模型强度
- 比例
- 风格
- 负面提示词
- 其他仅特定模式可见的参数

设计原则：

- 高频动作始终可见。
- 低频参数延迟暴露。
- 先让用户能写和传，再让用户调细节。

## 5. 改造方向

## 5.1 产品层改造

将“练功房”从“目录页”改成“统一创作页”：

- 原 `CustomFeatures.vue` 不再以卡片宫格为主。
- 默认进入时直接落在“自由P图”工作台。
- 其他功能通过顶部横向标签切换，不强制跳子页面。

建议保留的模式分组：

- 图像类
  - 自由P图
  - 文生图
  - 幻想换脸
  - 局部重绘
- 视频类
  - 图生视频
  - 高级图生视频
  - 图生视频 v2
- 换脸类
  - 快速换脸
  - 视频换脸

说明：

- 第一阶段不建议把所有旧功能一次性全并入主面板。
- 优先接入和你需求最贴近的几类：`自由P图`、`文生图`、`幻想换脸`、`局部重绘`、`图生视频`。
- 其余高复杂模式可以先保留“切换后跳旧页”作为兼容方案。

## 5.2 信息架构改造

建议把页面拆成 3 层信息结构：

### A. 模式层

负责回答“我现在在做什么”：

- 当前模式名称
- 模式说明
- 当前消耗

### B. 创作层

负责回答“我要输入什么、上传什么”：

- 参考图
- 提示词
- 主提交按钮

### C. 参数层

负责回答“我还可以怎么细调”：

- LoRA
- 分辨率
- 时长
- 风格/比例
- 模板锁定状态

## 6. 推荐页面结构

```text
练功房
├─ 标题区
├─ 横向模式标签栏
├─ 主创作卡片
│  ├─ 已上传参考图缩略图区
│  ├─ 提示词输入框
│  ├─ 底部快捷栏
│  │  ├─ 图像/视频标签
│  │  ├─ 添加参考图
│  │  ├─ 更多参数（三点）
│  │  └─ 提交按钮
│  └─ 高级参数展开区
├─ 推荐功能快捷滑块
└─ 结果区 / 最近生成结果
```

## 7. 交互方案

### 7.1 默认进入

- 用户打开“练功房”
- 默认选中“自由P图”
- 焦点落在提示词输入框
- 页面不再要求先点击功能卡片

### 7.2 切换模式

- 顶部模式使用横向可拖动标签栏
- 点击后切换当前模式配置
- 页面主体结构不变，只替换字段与参数
- 切换时如当前表单已有内容，需做未保存确认

### 7.3 添加参考图

- 点击“添加参考图”或拖拽上传
- 上传完成后在输入框上方显示缩略图
- 单图模式显示 1 张
- 双图模式显示 2 张
- 支持删除、替换、重新上传

### 7.4 展开更多参数

点击“三点”后，展开高级参数区。

推荐两种实现方案：

- 方案 A：输入框下方内联展开
  - 优点：开发简单，和现有页面结构接近
  - 缺点：页面会被撑高
- 方案 B：右侧弹出抽屉 / Popover 面板
  - 优点：更贴近参考图四，视觉更轻
  - 缺点：移动端适配要更仔细

建议：

- 移动端优先使用底部抽屉或 Popover
- 桌面端使用右侧 Popover 或下拉浮层

### 7.5 模板回填状态

已有模板应用链路需要保留：

- 模板带入时自动回填提示词
- 模板锁定时隐藏或禁用对应参数
- UI 上明确显示“模板已锁定”与“仍可调整”的边界

## 8. 模式映射建议

建议定义统一模式配置表，而不是继续把逻辑散落在多个页面中。

### 8.1 建议新增统一配置

建议新增类似：

- `frontend/src/features/generation/labModeConfig.ts`

每个模式配置内容建议包括：

- `id`
- `label`
- `routeTaskType`
- `submitTaskType`
- `defaultCost`
- `inputType`
- `maxImages`
- `supportsPrompt`
- `supportsNegativePrompt`
- `supportsLora`
- `supportsResolution`
- `supportsDuration`
- `supportsRatio`
- `supportsTemplateApply`
- `submitButtonText`

### 8.2 建议首批纳入统一面板的模式

| 模式 | 任务类型建议 | 说明 |
| --- | --- | --- |
| 自由P图 | `edit` / `img2img_lora` | 默认模式，重点承载 |
| 文生图 | `txt2img` | 无需上传图 |
| 幻想换脸 | `i2i_pro` | 单图 + 提示词 |
| 局部重绘 | `i2i_draw` | 单图 + 提示词 |
| 图生视频 | `custom_video` / `video_lora` | 单图 + 描述 + 参数 |

### 8.3 暂缓直接统一的模式

以下模式建议先保留原页面兼容：

- `face_swap`
- `face_video`
- `wan22_video_v2`

原因：

- 这些模式的字段结构、上传对象或交互步骤更特殊。
- 若第一期全部揉进统一工作台，复杂度会明显上升。

## 9. 前端实现建议

## 9.1 页面级方案

建议保留当前路由 `custom-features`，但改造其职责：

- 由“功能列表页”改成“练功房统一工作台页”
- 原卡片网格缩减为次级推荐区，而非主结构

建议新增或重构：

- `frontend/src/views/CustomFeatures.vue`
- `frontend/src/components/lab/LabModeRail.vue`
- `frontend/src/components/lab/LabPromptComposer.vue`
- `frontend/src/components/lab/LabReferenceTray.vue`
- `frontend/src/components/lab/LabAdvancedOptionsPanel.vue`
- `frontend/src/components/lab/LabModeQuickCards.vue`

## 9.2 可复用现有能力

可直接复用：

- `SegmentedTabsRail.vue`
  - 作为顶部横向模式栏基础
- `GenerationActionBar.vue`
  - 作为底部消耗与提交区域基础
- `buildGenerationTaskPayload.ts`
  - 作为统一提交载荷构建基础
- `useUpload.ts`
  - 作为素材上传基础
- `useTaskStream.ts`
  - 作为任务提交和进度流基础
- `useTaskResult.ts`
  - 作为结果展示基础
- `useGalleryApplyContext.ts`
  - 作为模板回填基础

## 9.3 组件职责建议

### `LabModeRail.vue`

职责：

- 横向展示模式
- 支持拖动/滑动
- 高亮当前模式
- 可展示成本小标签

### `LabPromptComposer.vue`

职责：

- 提示词输入框
- 输入框上方参考图展示
- 底部快捷操作栏
- 管理更多参数展开入口

### `LabReferenceTray.vue`

职责：

- 统一管理图片缩略图
- 兼容 0/1/2 图状态
- 处理删除、替换、继续添加

### `LabAdvancedOptionsPanel.vue`

职责：

- 根据模式动态渲染高级参数
- 承载 LoRA、时长、分辨率、比例、负面提示词等
- 处理模板锁定态

## 10. 状态管理建议

当前分散页面大多各自维护局部 `ref` 状态。统一工作台后，建议把状态收拢为一个组合式函数：

- `frontend/src/composables/useLabWorkbench.ts`

建议统一管理：

- 当前模式
- 提示词
- 负面提示词
- 上传文件列表
- LoRA 选择
- 分辨率
- 时长
- 比例
- 模板应用状态
- 提交中状态
- 当前任务结果状态

这样可以解决：

- 切模式时状态切换混乱
- 组件之间 props 传递过深
- 多模式共用提交逻辑分散

## 11. 提交流程建议

统一工作台不建议改变后端协议，仍沿用当前 `buildGenerationTaskPayload` 能力。

推荐流程：

1. 根据当前模式读取模式配置
2. 校验最小输入条件
3. 将 UI 字段映射到当前 payload
4. 调用 `submitTask`
5. 任务结果继续由现有结果面板承接

关键原则：

- UI 统一，不代表 payload 强行统一
- 前端只做“模式配置驱动映射”
- 后端 task_type、inputs 契约尽量不动

## 12. 视觉建议

## 12.1 风格方向

按参考图三建议采用：

- 浅底或轻雾化卡片背景
- 更大的留白
- 更像“创作输入器”而不是“后台配置表单”
- 首屏减少边框分区感，强调单个核心创作框

## 12.2 视觉重点

- 顶部标签栏要轻量，可滑动，不要太像传统导航 Tab
- 提示词框要成为主视觉中心
- 上传图缩略图要贴近输入框上沿
- “三点按钮”要明显但不抢主按钮
- 主提交按钮要固定在输入框右下角或底部操作栏右侧

## 12.3 移动端要点

- 模式栏支持横向滑动
- 更多参数优先走抽屉
- 提交按钮保持可达，不被键盘遮挡
- 上传后的缩略图控制高度，避免把输入框挤没

## 13. 分阶段实施建议

### 第一阶段：页面骨架改造

- 改造 `CustomFeatures.vue`
- 接入顶部横向模式栏
- 搭建统一输入框主面板
- 默认模式改为“自由P图”
- 卡片网格降级为次级区域

目标：

- 先完成样式和交互骨架，页面看起来已经接近参考图三

### 第二阶段：统一图像类模式

- 接入 `自由P图`
- 接入 `文生图`
- 接入 `幻想换脸`
- 接入 `局部重绘`
- 打通参考图、提示词、LoRA、提交逻辑

目标：

- 图像类模式在一个统一工作台中完成切换与提交

### 第三阶段：接入视频类模式

- 接入 `图生视频`
- 接入 `高级图生视频`
- 评估 `图生视频 v2` 是否独立保留

目标：

- 统一工作台支持图片创作和部分视频创作

### 第四阶段：体验打磨

- 模板锁定态优化
- 未提交离开确认
- 快捷推荐模式区
- 空状态与最近结果展示
- 移动端细节打磨

## 14. 风险与注意事项

### 14.1 模式差异过大

不同模式的输入要求差异明显，统一界面时容易出现大量 `if/else`。

规避方式：

- 必须使用“模式配置驱动”
- 把字段可见性、校验规则和 payload 映射从视图里抽离

### 14.2 模板应用兼容

当前已有模板回填逻辑，若统一工作台处理不当，容易出现：

- 提示词被误清空
- 锁定态失效
- 模板来源模式和当前模式不匹配

规避方式：

- 模板状态单独建模
- 切模式时增加模式一致性校验

### 14.3 结果区布局冲突

参考图三强调“输入优先”，而现有工作台是明显左右双栏。

建议：

- 桌面端保留右侧结果区
- 移动端结果区放到输入面板下方
- 不强制沿用当前左右 50% 布局

### 14.4 首期范围失控

如果第一期试图统一所有模式，改造周期会拉长。

建议：

- 首期聚焦高频图像模式
- 特殊模式先保留兼容跳转

## 15. 验收标准

完成后建议以以下标准验收：

- 打开练功房后，默认直接进入“自由P图”创作界面
- 顶部可横向滑动切换不同功能模式
- 提示词输入框成为首屏主体
- 添加参考图后，能显示在输入框上方
- 点击“三点”能展开高级参数
- 高级参数可按模式动态显示
- 图像类模式至少 3 到 4 个可直接在统一界面提交
- 模板回填、上传、提交、结果预览不回归
- 移动端操作路径明显短于当前版本

## 16. 推荐落地结论

综合当前代码结构和你的目标，推荐采用：

- 以 `CustomFeatures.vue` 为主战场重构
- 用“统一工作台 + 横向模式切换”替代当前卡片首页
- 首期先做图像类模式统一
- 视频类和高复杂模式分阶段并入
- 充分复用现有上传、提交、结果、模板回填能力

这是当前性价比最高、风险最低，也最容易快速接近参考图三视觉形态的方案。

---

## 17. 建议的实施清单

- 重构 `CustomFeatures.vue` 为统一练功房页面
- 新增 `labModeConfig.ts` 管理模式配置
- 新增 `useLabWorkbench.ts` 管理统一状态
- 新增练功房专用组件目录 `frontend/src/components/lab/`
- 复用现有上传、提交、结果能力
- 第一阶段先打通自由P图与文生图
- 第二阶段并入幻想换脸、局部重绘、图生视频
- 第三阶段评估是否继续合并视频换脸与 v2 工作台
