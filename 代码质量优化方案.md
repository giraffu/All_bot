# AllBot 代码质量优化方案

**编写日期**：2026-05-02
**背景**：基于最新执行的静态代码分析与质量评估报告，结合对实际代码（如 `task_service.py`、`gallery_callbacks.py` 等）的深度审查，针对系统中暴露的高复杂度逻辑、潜在死代码、架构耦合风险及 DRY 原则违反等问题，制定本重构与优化方案。

---

## 一、 核心高复杂度模块优化 (Critical & High 级别)

### 1. 画廊回调逻辑过载 (Level F/E)
**涉及文件**：`src/handlers/callbacks/gallery_callbacks.py`
- **目标对象**：`gallery_sort_page_callback` (F级), `public_share_callback` (E级)
- **问题分析**：
  - `gallery_sort_page_callback` 函数内部嵌套过深，将**请求解析、排序规则判断、分页游标计算、数据库查询以及 UI (Keyboard) 重新渲染**全部糅合在一个函数中。
  - `public_share_callback` 包含过多前置状态检查（是否已公开、权限等），导致回调层过厚。
  - **数据库连接复用低效**：同一次回调生命周期内多次独立开启了 `async with AsyncSessionLocal() as session:`（如先查询 `History`，后又开新 Session 更新 `telegram_file_id`），容易导致连接池枯竭。
- **解决方案**：
  1. **下沉业务逻辑**：将分享检查、状态更新等逻辑移至 `src/core/gallery_core.py`，回调层仅负责“接收参数 -> 调用 Core -> 更新 UI”。
  2. **引入查询对象模式 (Query Object)**：对于排序和分页，创建一个独立的 `GalleryQuery` 数据类来封装页码、每页数量、排序字段，在外部组装好后直接传递给 DB 层，消除多重 `if-else`。
  3. **UI 渲染组件化与 i18n 改造**：将 Inline Keyboard 的构建逻辑抽离为独立的 `build_gallery_pagination_keyboard(current_page, total_pages, current_sort)` 辅助函数，并**同步引入 `I18nTranslator` 进行多语言解耦**。
  4. **Session 生命周期管理**：强制要求在同一请求/回调的生命周期内复用同一个 DB Session，避免频繁开闭连接。

### 2. 任务提交流程臃肿与 Saga 嵌套过深 (Level E)
**涉及文件**：`src/core/task_core.py`
- **目标对象**：`process_and_submit_task`
- **问题分析**：
  - 该函数负责了：用户状态检查、并发锁限制、参数校验组装、队列推送等多个流程，缺乏结构化分层。
  - **严重违反策略模式 (Strategy Pattern)**：作为通用基础函数，硬编码了特定于视频任务的参数解析逻辑（如强制解析 `1024p` 分辨率和 `10s` 时长）。
  - **分布式事务 (Saga) 嵌套过深**：退款、清理 Redis、释放并发锁等回滚操作依赖多层 `try-except-finally` 和 `asyncio.shield`。
- **解决方案**：
  - **完善策略模式**：将特定业务领域（如视频）的参数校验与解析下沉到具体的 `VideoStrategy` 类中实现。
  - **引入 Pipeline/拦截器模式**：将前置校验拆分为独立的校验器（如 `ConcurrencyValidator`, `QuotaValidator`）。
  - **扁平化退出清理**：引入 `contextlib.AsyncExitStack` 来扁平化管理退款、锁释放等清理逻辑，消除深层嵌套。
  - 主函数重构为按序执行校验器链，校验通过后再统一执行 Redis 队列推送操作，将圈复杂度降低到 C 级以内。

### 3. 任务完成后的善后逻辑耦合 (Level E)
**涉及文件**：`src/services/task_service.py`
- **目标对象**：`TaskService._handle_task_completion`
- **问题分析**：
  - 该函数严重糅合了：媒体文件下载与落盘、数据库日志记录（`user_logger.log_task`）、**动态构建极其复杂的 Inline Keyboard UI**，以及向 TG 发送多媒体消息。
- **解决方案（修正优化）**：
  - **采用委托模式 (Delegation) / 外观模式 (Facade)**：考虑到媒体下载、记日志、发消息具有严格的顺序强依赖，避免使用事件总线（Event Bus）导致的控制流过度碎片化。建议将其拆分为 `MediaDownloader`、`UILogger` 和 `NotificationSender` 三个独立的类或纯函数，由 `_handle_task_completion` 进行线性调用。既实现解耦，又保留直观的线性阅读体验。

### 4. 大量样板代码严重违反 DRY 原则 (Level E)
**涉及文件**：`src/services/task_service.py`
- **目标对象**：`process_ltx_video_task`, `process_face_video_task`, `process_generation_task` 等
- **问题分析**：多个方法中包含了完全相同的 `try...except ConcurrencyLimitError... except InsufficientCreditsError` 错误捕获与退款兜底逻辑。
- **解决方案**：
  - 提取统一的**异常处理装饰器 (Decorator)** 或**上下文管理器 (Context Manager)**，彻底消除表现层（Service 层）的异常处理与退款样板代码。

### 5. 状态机入口过长 (Level E)
**涉及文件**：`src/handlers/fsm/gallery_apply_fsm.py`
- **目标对象**：`start_gallery_apply`
- **问题分析**：FSM (有限状态机) 的入口函数包含了太多初始化状态的判定和上下文数据注入。
- **解决方案**：提取一个 `initialize_fsm_context` 方法专门处理 `context.user_data` 的清理与赋初值，让 `start_gallery_apply` 仅保留路由引导逻辑。

---

## 二、 潜在死代码与冗余清理 (Medium 级别)

**问题分析**：
静态扫描发现了部分未被调用的历史废弃函数（如旧版的 Google 登录逻辑 `get_or_create_user_by_google`）。

**解决方案**：
1. **扩大扫描范围**：未来的静态分析和 CI 门禁必须将 `dashboard` 和 `src` 一并纳入扫描范围，防止跨模块引用的死代码误报。
2. **人工二次核对**：检查函数是否被动态反射调用（如 `getattr`）或是否为提供给未来使用的公共 API。
3. **白名单标记**：若确为保留 API，在模块级别使用 `__all__ = [...]` 显式声明，或增加 `# noqa` 抑制警告。
4. **安全删除**：对于确认已经废弃的历史版本代码，直接在下一次迭代中移除。

---

## 三、 架构规范化与长期治理措施

1. **持续集成 (CI) 门禁**：
   - 在 GitHub Actions 或 GitLab CI 中引入 `radon` 和 `vulture` 检查。新增或修改的代码，圈复杂度（CC）不得高于 D 级。
2. **防腐层 (Anti-Corruption Layer) 保持**：
   - 必须继续坚持 `AGENTS.md` 中规定的 **Core Isolation** 原则。确保 `src/core/` 目录下的重构不会引入 `telegram.Update` 等框架级对象。
3. **单元测试护航**：
   - 在重构核心模块前，必须先编写对应的单测用例（覆盖率 > 80%）。
4. **异常处理与样板代码收敛**：
   - 统一捕获并处理 `telegram.error.BadRequest`。
   - 通过装饰器收敛 `task_service.py` 中的重复异常处理与退款逻辑。
5. **全局 i18n 改造补全**：
   - 不仅在回调和 FSM 中引入多语言，必须将 `task_service.py` 等表现层中的硬编码交互文案（如 "🚀 正在处理视频生成任务..."）全面接入 `context.t()` 进行彻底解耦。
6. **上下文状态强类型化**：
   - 重构 FSM 和 `task_service.py` 时，引入 Pydantic 模型作为 FSM Context 状态管理器。
7. **架构红线遵守**：
   - 任何重构操作必须提前阅读 `AGENTS.md`，并在开发过程中严格遵循“按需加载 Skill”的原则。

---

## 四、 实施路线图 (Roadmap)

- **Phase 1 (Quick Wins)**
  - 清理 `vulture` 报告中的确认死代码，完成 Medium 级别的优化。
  - 重构 `start_gallery_apply` 等简单的 FSM 入口。
  - 提取统一的**异常处理装饰器**，消除 `task_service.py` 中的重复样板代码。
  - 补全 `task_service.py` 等全局文件的 i18n 文本替换。
- **Phase 2 (Core Refactoring)**
  - 针对 `gallery_callbacks.py` 进行彻底拆分，抽取 UI 构建组件、Query Object，并**优化 DB Session 复用机制**。
  - 完善 `process_and_submit_task` 中的策略模式，并引入 `AsyncExitStack` 优化深层嵌套。
- **Phase 3 (Architecture Upgrade)**
  - 在 `task_service.py` 引入 **委托/外观模式 (Facade)** 线性重构任务完成后的善后逻辑（`_handle_task_completion`），替代原有过度设计的 Event Bus 方案。
  - 将 `APIClient.listen_for_progress` 等高复杂度函数纳入重构计划，逐步降级至 C 级以内。
  - 部署 CI 静态代码分析检查流程。